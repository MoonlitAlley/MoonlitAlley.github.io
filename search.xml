<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Broken pipe的出现原因</title>
    <url>/2021/04/20/Broken-pipe%E7%9A%84%E5%87%BA%E7%8E%B0%E5%8E%9F%E5%9B%A0/</url>
    <content><![CDATA[<h1 id="背景说明"><a href="#背景说明" class="headerlink" title="背景说明"></a>背景说明</h1><p>最近在公司的私有化业务中碰到了一个读写Redis的Broken pipe问题，花了大半天时间查阅了各种文档，同时又复习了一遍计算机网络的三次握手、四次挥手等基础知识，才算勉强理解了问题出现的原因，也找到了几种解决办法。<br>由于私有化环境中很多系统操作都是没有权限执行的，向strace、tcpdump等等，所有下文中有用到相关的内容时，我会贴一下其他网站上找来的图，同时把源地址放到笔记最后的引用中。</p>
<p>先来看一下驻场同时传来的错误日志:<br><img src="/2021/04/20/Broken-pipe%E7%9A%84%E5%87%BA%E7%8E%B0%E5%8E%9F%E5%9B%A0/problem-1.png"><br>对应的业务代码块如下(C++项目+ redis官方库hiredis)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">redisGetReply(redis, (void **)(&amp;raw_reply));</span><br><span class="line">redis_reply_ptr reply(raw_reply);</span><br><span class="line">json result(json::value_t::object);</span><br><span class="line">if (!reply || reply-&gt;type &#x3D;&#x3D; REDIS_REPLY_ERROR) &#123;</span><br><span class="line">    stringstream ss;</span><br><span class="line">    ss &lt;&lt; &quot;failed to retrieve profile for key &quot; &lt;&lt; key &lt;&lt; &quot;: &quot;</span><br><span class="line">       &lt;&lt; redis-&gt;errstr;</span><br><span class="line">    throw runtime_error(ss.str());</span><br><span class="line">&#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<h1 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h1><p>日志中有两种类型的错误，第一种是：Server close the connection; 第二种是：Broken pipe<br>我们先看来出现次数较多的Broken pipe问题。 </p>
<blockquote>
<p>这里需要把自己总结的问题处理方案记录一下：通常我们拿到一个错误的时候，快速定位且不容易跑偏的方法应该是有明确步骤的。 不然的话就会向一个刚开始工作的新手一样，猜测各种结论，然后说服自己接受，最后发现给出的是一个跑偏的或者无关的答案。<br>所以步骤应该是：1. 这个错误是哪里出现的（这一步要做到根故障定位，不只是这个文件、这个函数就够了）； 2. 为什么会出现这个错误？</p>
</blockquote>
<h2 id="错误是哪来的？"><a href="#错误是哪来的？" class="headerlink" title="错误是哪来的？"></a>错误是哪来的？</h2><p>首先我们能根据服务日志清楚的定位到抛出错误的代码块，再深入的定位感兴趣的同学可以翻一下hiredis的源码，hiredis.c中redisGetReply从fd里读取数据时处理了多种异常，其中的EAGAIN就是在另外一篇<a href>Resource temporarily unavailable的出现原因</a>中要分析的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">nread &#x3D; read(c-&gt;fd,buf,sizeof(buf));</span><br><span class="line">if (nread &#x3D;&#x3D; -1) &#123;</span><br><span class="line">    if ((errno &#x3D;&#x3D; EAGAIN &amp;&amp; !(c-&gt;flags &amp; REDIS_BLOCK)) || (errno &#x3D;&#x3D; EINTR)) &#123;</span><br><span class="line">        &#x2F;* Try again later *&#x2F;</span><br><span class="line">    &#125;</span><br><span class="line">&#125; else if (nread &#x3D;&#x3D; 0) &#123;</span><br><span class="line">    __redisSetError(c,REDIS_ERR_EOF,&quot;Server closed the connection&quot;);</span><br><span class="line">    return REDIS_ERR;</span><br><span class="line">&#125; else &#123;</span><br><span class="line">    if (redisReaderFeed(c-&gt;reader,buf,nread) !&#x3D; REDIS_OK) &#123;</span><br><span class="line">        __redisSetError(c,c-&gt;reader-&gt;err,c-&gt;reader-&gt;errstr);</span><br><span class="line">        return REDIS_ERR;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>代码中没有找到<code>Broken pipe</code>出现的具体位置，但是根据读取时此处读取redis数据时多种网络错误的判断。到这个地步，应该能猜测与网络错误有关了。</p>
<h2 id="为什么出现这个错误？"><a href="#为什么出现这个错误？" class="headerlink" title="为什么出现这个错误？"></a>为什么出现这个错误？</h2><p>首先是引用1中的一个截图<br><img src="/2021/04/20/Broken-pipe%E7%9A%84%E5%87%BA%E7%8E%B0%E5%8E%9F%E5%9B%A0/TCP-guide.jpeg"></p>
<ol>
<li>复现</li>
<li>抓包观察<br>tcpdump + wireshark</li>
</ol>
<p>// todo</p>
<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ol>
<li><a href="https://www.cnblogs.com/metoy/p/6565486.html">Broken pipe错误终极解释</a></li>
<li><a href="https://angela.blog.csdn.net/article/details/106399521">一个生产网络问题Broken Pipe引发的思考</a></li>
<li></li>
</ol>
]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>C11 ThreadPool及包含的C11特性</title>
    <url>/2021/04/25/C11-ThreadPool%E5%8F%8A%E5%8C%85%E5%90%AB%E7%9A%84C11%E7%89%B9%E6%80%A7/</url>
    <content><![CDATA[<h1 id><a href="#" class="headerlink" title></a></h1><h1 id="源代码"><a href="#源代码" class="headerlink" title="源代码"></a>源代码</h1><p>源码是github上搜索C++ ThreadPool排名第一个的实现，具体项目链接我放到了引用1中，代码中用到了很多C11特性，能通过这篇源码很好的学习一下线程池的概念以及C11一些新特性的用法。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#ifndef THREAD_POOL_H</span><br><span class="line">#define THREAD_POOL_H</span><br><span class="line"></span><br><span class="line">#include &lt;vector&gt;</span><br><span class="line">#include &lt;queue&gt;</span><br><span class="line">#include &lt;memory&gt;</span><br><span class="line">#include &lt;thread&gt;</span><br><span class="line">#include &lt;mutex&gt;</span><br><span class="line">#include &lt;condition_variable&gt;</span><br><span class="line">#include &lt;future&gt;</span><br><span class="line">#include &lt;functional&gt;</span><br><span class="line">#include &lt;stdexcept&gt;</span><br><span class="line"></span><br><span class="line">class ThreadPool &#123;</span><br><span class="line">public:</span><br><span class="line">    ThreadPool(size_t);</span><br><span class="line">    template&lt;class F, class... Args&gt;</span><br><span class="line">    auto enqueue(F&amp;&amp; f, Args&amp;&amp;... args)</span><br><span class="line">        -&gt; std::future&lt;typename std::result_of&lt;F(Args...)&gt;::type&gt;;</span><br><span class="line">    ~ThreadPool();</span><br><span class="line">private:</span><br><span class="line">    &#x2F;&#x2F; need to keep track of threads so we can join them</span><br><span class="line">    std::vector&lt; std::thread &gt; workers;</span><br><span class="line">    &#x2F;&#x2F; the task queue</span><br><span class="line">    std::queue&lt; std::function&lt;void()&gt; &gt; tasks;</span><br><span class="line">    </span><br><span class="line">    &#x2F;&#x2F; synchronization</span><br><span class="line">    std::mutex queue_mutex;</span><br><span class="line">    std::condition_variable condition;</span><br><span class="line">    bool stop;</span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">&#x2F;&#x2F; the constructor just launches some amount of workers</span><br><span class="line">inline ThreadPool::ThreadPool(size_t threads)</span><br><span class="line">    :   stop(false)</span><br><span class="line">&#123;</span><br><span class="line">    for(size_t i &#x3D; 0;i&lt;threads;++i)</span><br><span class="line">        workers.emplace_back(</span><br><span class="line">            [this]</span><br><span class="line">            &#123;</span><br><span class="line">                for(;;)</span><br><span class="line">                &#123;</span><br><span class="line">                    std::function&lt;void()&gt; task;</span><br><span class="line"></span><br><span class="line">                    &#123;</span><br><span class="line">                        std::unique_lock&lt;std::mutex&gt; lock(this-&gt;queue_mutex);</span><br><span class="line">                        this-&gt;condition.wait(lock, [this]&#123; return this-&gt;stop || !this-&gt;tasks.empty(); &#125;);</span><br><span class="line">                        </span><br><span class="line">                        if(this-&gt;stop &amp;&amp; this-&gt;tasks.empty())</span><br><span class="line">                            return;</span><br><span class="line">                        </span><br><span class="line">                        task &#x3D; std::move(this-&gt;tasks.front());</span><br><span class="line">                        this-&gt;tasks.pop();</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    task();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; add new work item to the pool</span><br><span class="line">template&lt;class F, class... Args&gt;</span><br><span class="line">auto ThreadPool::enqueue(F&amp;&amp; f, Args&amp;&amp;... args) -&gt; std::future&lt;typename std::result_of&lt;F(Args...)&gt;::type&gt;</span><br><span class="line">&#123;</span><br><span class="line">    using return_type &#x3D; typename std::result_of&lt;F(Args...)&gt;::type;</span><br><span class="line"></span><br><span class="line">    auto task &#x3D; std::make_shared&lt; std::packaged_task&lt;return_type()&gt; &gt;(</span><br><span class="line">            std::bind(std::forward&lt;F&gt;(f), std::forward&lt;Args&gt;(args)...)</span><br><span class="line">        );</span><br><span class="line">        </span><br><span class="line">    std::future&lt;return_type&gt; res &#x3D; task-&gt;get_future();</span><br><span class="line">    &#123;</span><br><span class="line">        std::unique_lock&lt;std::mutex&gt; lock(queue_mutex);</span><br><span class="line"></span><br><span class="line">        &#x2F;&#x2F; don&#39;t allow enqueueing after stopping the pool</span><br><span class="line">        if(stop)</span><br><span class="line">            throw std::runtime_error(&quot;enqueue on stopped ThreadPool&quot;);</span><br><span class="line"></span><br><span class="line">        tasks.emplace(</span><br><span class="line">                      [task]()&#123; (*task)(); &#125;</span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">    condition.notify_one();</span><br><span class="line">    return res;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; the destructor joins all threads</span><br><span class="line">inline ThreadPool::~ThreadPool()</span><br><span class="line">&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        std::unique_lock&lt;std::mutex&gt; lock(queue_mutex);</span><br><span class="line">        stop &#x3D; true;</span><br><span class="line">    &#125;</span><br><span class="line">    condition.notify_all();</span><br><span class="line">    for(std::thread &amp;worker: workers)</span><br><span class="line">        worker.join();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">#endif</span><br><span class="line"></span><br></pre></td></tr></table></figure>


<ol>
<li>function&amp;bind</li>
<li>unique_lock</li>
<li>condition_variable</li>
<li>result_of&amp;future</li>
</ol>
<h1 id="function-amp-bind"><a href="#function-amp-bind" class="headerlink" title="function&amp;bind"></a>function&amp;bind</h1><p>待补充…</p>
<h1 id="unique-lock"><a href="#unique-lock" class="headerlink" title="unique_lock"></a>unique_lock</h1><p>待补充…</p>
<h1 id="condition-variable"><a href="#condition-variable" class="headerlink" title="condition_variable"></a>condition_variable</h1><p>待补充…</p>
<h2 id="头文件"><a href="#头文件" class="headerlink" title="头文件"></a>头文件</h2><p>C++11 标准中 <condition_variable> 头文件主要包含了与条件变量相关的类和函数。<br>相关的类包括 </condition_variable></p>
<ul>
<li>std::condition_variable</li>
<li>std::condition_variable_any</li>
<li>枚举类型std::cv_status</li>
<li>std::notify_all_at_thread_exit()</li>
</ul>
<h2 id="condition-variable的工作原理"><a href="#condition-variable的工作原理" class="headerlink" title="condition_variable的工作原理"></a>condition_variable的工作原理</h2><p>当 std::condition_variable 对象的某个 wait 函数被调用的时候，它使用 std::unique_lock(通过 std::mutex) 来锁住当前线程。当前线程会一直被阻塞，直到另外一个线程在相同的 std::condition_variable 对象上调用了 notification 函数来唤醒当前线程。<br>（condition_variable通常使用std::unique_lock(std::mutex)来等待，也可以使用其他lockable类型。）</p>
<h2 id="成员函数介绍"><a href="#成员函数介绍" class="headerlink" title="成员函数介绍"></a>成员函数介绍</h2><h3 id="std-condition-variable-构造函数"><a href="#std-condition-variable-构造函数" class="headerlink" title="std::condition_variable 构造函数"></a>std::condition_variable 构造函数</h3><table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>default (1)</td>
<td>condition_variable();</td>
</tr>
<tr>
<td>copy [deleted] (2)</td>
<td>condition_variable (const condition_variable&amp;) = delete;</td>
</tr>
</tbody></table>
<p>std::condition_variable 的拷贝构造函数被禁用，只提供了默认构造函数。</p>
<h3 id="std-condition-variable-wait-介绍"><a href="#std-condition-variable-wait-介绍" class="headerlink" title="std::condition_variable::wait() 介绍"></a>std::condition_variable::wait() 介绍</h3><table>
<thead>
<tr>
<th align="left"></th>
<th align="left"></th>
</tr>
</thead>
<tbody><tr>
<td align="left">unconditional (1)</td>
<td align="left">void wait (unique_lock<mutex>&amp; lck);</mutex></td>
</tr>
<tr>
<td align="left">predicate (2)</td>
<td align="left">template <class predicate> void wait (unique_lock<mutex>&amp; lck, Predicate pred);</mutex></class></td>
</tr>
</tbody></table>
<p>std::condition_variable 提供了两种 wait() 函数。</p>
<ul>
<li>当前线程调用 wait() 后将被阻塞(此时当前线程应该获得了锁（mutex），不妨设获得锁 lck)，直到另外某个线程调用 notify_* 唤醒了当前线程。<br>在线程被阻塞时，该函数会自动调用 lck.unlock() 释放锁，使得其他被阻塞在锁竞争上的线程得以继续执行。另外，一旦当前线程获得通知(notified，通常是另外某个线程调用 notify_* 唤醒了当前线程)，wait() 函数也是自动调用 lck.lock()，使得 lck 的状态和 wait 函数被调用时相同。</li>
<li>在第二种情况下（即设置了 Predicate），只有当 pred 条件为 false 时调用 wait() 才会阻塞当前线程，并且在收到其他线程的通知后只有当 pred 为 true 时才会被解除阻塞。</li>
</ul>
<p>pred可以为函数或者lamda表达式，类似于代码中的等待唤醒线程时，需要等待unique_lock且任务对列不为空</p>
<blockquote>
<p><code>this-&gt;condition.wait(lock, [this]&#123; return this-&gt;stop || !this-&gt;tasks.empty(); &#125;);</code></p>
</blockquote>
<h3 id="std-condition-variable-wait-for-介绍"><a href="#std-condition-variable-wait-for-介绍" class="headerlink" title="std::condition_variable::wait_for() 介绍"></a>std::condition_variable::wait_for() 介绍</h3><table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>unconditional (1)</td>
<td>template &lt;class Rep, class Period&gt; <br> cv_status wait_for (unique_lock<mutex>&amp; lck, const chrono::duration&lt;Rep,Period&gt;&amp; rel_time);</mutex></td>
</tr>
<tr>
<td>predicate (2)</td>
<td>template &lt;class Rep, class Period, class Predicate&gt; <br> bool wait_for (unique_lock<mutex>&amp; lck, const chrono::duration&lt;Rep,Period&gt;&amp; rel_time, Predicate pred);</mutex></td>
</tr>
</tbody></table>
<p>与 std::condition_variable::wait() 类似，不过 wait_for 可以指定一个时间段，在当前线程收到通知或者指定的时间 rel_time 超时之前，该线程都会处于阻塞状态。而一旦超时或者收到了其他线程的通知，wait_for 返回，剩下的处理步骤和 wait() 类似。</p>
<p>另外，wait_for 的重载版本（predicte(2)）的最后一个参数 pred 表示 wait_for 的预测条件，只有当 pred 条件为 false 时调用 wait() 才会阻塞当前线程，并且在收到其他线程的通知后只有当 pred 为 true 时才会被解除阻塞，因此相当于如下代码：<br><code>return wait_until (lck, chrono::steady_clock::now() + rel_time, std::move(pred));</code><br>下面这个示例相当于该线程再收到lck之前，每秒超时打印一次’.’字符。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">std::unique_lock&lt;std::mutex&gt; lck(mtx);</span><br><span class="line">while (cv.wait_for(lck,std::chrono::seconds(1)) &#x3D;&#x3D; std::cv_status::timeout) &#123;</span><br><span class="line">    std::cout &lt;&lt; &#39;.&#39;;</span><br><span class="line">    std::cout.flush();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="std-condition-variable-wait-until-介绍"><a href="#std-condition-variable-wait-until-介绍" class="headerlink" title="std::condition_variable::wait_until 介绍"></a>std::condition_variable::wait_until 介绍</h3><table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>unconditional (1)</td>
<td>template &lt;class Clock, class Duration&gt; <br> cv_status wait_until (unique_lock<mutex>&amp; lck, const chrono::time_point&lt;Clock,Duration&gt;&amp; abs_time);</mutex></td>
</tr>
<tr>
<td>predicate (2)</td>
<td>template &lt;class Clock, class Duration, class Predicate&gt; <br> bool wait_until (unique_lock<mutex>&amp; lck, const chrono::time_point&lt;Clock,Duration&gt;&amp; abs_time, Predicate pred);｜</mutex></td>
</tr>
</tbody></table>
<p>与 std::condition_variable::wait_for 类似，但是 wait_until 可以指定一个时间点，在当前线程收到通知或者指定的时间点 abs_time 超时之前，该线程都会处于阻塞状态。而一旦超时或者收到了其他线程的通知，wait_until 返回，剩下的处理步骤和 wait_until() 类似。</p>
<p>另外，wait_until 的重载版本（predicte(2)）的最后一个参数 pred 表示 wait_until 的预测条件，只有当 pred 条件为 false 时调用 wait() 才会阻塞当前线程，并且在收到其他线程的通知后只有当 pred 为 true 时才会被解除阻塞，因此相当于如下代码：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while (!pred())</span><br><span class="line">  if ( wait_until(lck,abs_time) &#x3D;&#x3D; cv_status::timeout)</span><br><span class="line">    return pred();</span><br><span class="line">return true;</span><br></pre></td></tr></table></figure>

<h3 id="std-condition-variable-notify-one"><a href="#std-condition-variable-notify-one" class="headerlink" title="std::condition_variable::notify_one()"></a>std::condition_variable::notify_one()</h3><p>唤醒某个等待(wait)线程。如果当前没有等待线程，则该函数什么也不做，如果同时存在多个等待线程，则唤醒某个线程是不确定的(unspecified)。</p>
<h3 id="std-condition-variable-notify-all"><a href="#std-condition-variable-notify-all" class="headerlink" title="std::condition_variable::notify_all()"></a>std::condition_variable::notify_all()</h3><p>唤醒所有的等待(wait)线程。如果当前没有等待线程，则该函数什么也不做。</p>
<h3 id="std-cv-status-枚举类型介绍"><a href="#std-cv-status-枚举类型介绍" class="headerlink" title="std::cv_status 枚举类型介绍"></a>std::cv_status 枚举类型介绍</h3><table>
<thead>
<tr>
<th></th>
<th></th>
</tr>
</thead>
<tbody><tr>
<td>cv_status::no_timeout</td>
<td>wait_for 或者 wait_until 没有超时，即在规定的时间段内线程收到了通知。</td>
</tr>
<tr>
<td>cv_status::timeout</td>
<td>wait_for 或者 wait_until 超时。</td>
</tr>
</tbody></table>
<h3 id="std-notify-all-at-thread-exit"><a href="#std-notify-all-at-thread-exit" class="headerlink" title="std::notify_all_at_thread_exit"></a>std::notify_all_at_thread_exit</h3><p>函数原型为：<br>void notify_all_at_thread_exit (condition_variable&amp; cond, unique_lock<mutex> lck);<br>当调用该函数的线程退出时，所有在 cond 条件变量上等待的线程都会收到通知。<br>这个成员函数的示例还需要再研究一下。<br>// TODO</mutex></p>
<h1 id="result-of-amp-future"><a href="#result-of-amp-future" class="headerlink" title="result_of &amp; future"></a>result_of &amp; future</h1><p>待补充…<br>result_of和decltype<br>示例如下</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include &lt;iostream&gt;</span><br><span class="line">using namespace std;</span><br><span class="line"></span><br><span class="line">template&lt;class Func, class... Args&gt;</span><br><span class="line">auto getResult(Func &amp;&amp; func, Args&amp;&amp;... args) -&gt; typename std::result_of&lt;Func(Args...)&gt;::type &#123;</span><br><span class="line">    return func(args...);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">int main(int argc, const char * argv[]) &#123;</span><br><span class="line"></span><br><span class="line">    auto sum_result &#x3D; getResult([](int a, int b) -&gt;decltype(a+b)&#123; return a + b; &#125;, 3, 2);</span><br><span class="line">    cout&lt;&lt; &quot;we get sum result: &quot; &lt;&lt; sum_result &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">    auto float_result &#x3D; getResult([](int a, int b) -&gt;decltype(float(a)&#x2F; float(b)) &#123; return float(a) &#x2F; float(b); &#125;, 3, 2);</span><br><span class="line">    auto int_result &#x3D; getResult([](int a, int b) -&gt;decltype(a&#x2F;b) &#123; return float(a) &#x2F; float(b); &#125;, 3, 2);</span><br><span class="line">    cout &lt;&lt; &quot;we get float result: &quot; &lt;&lt; float_result &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; &quot;we get int result: &quot; &lt;&lt; int_result &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出内容如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">we get sum result: 5</span><br><span class="line">we get float result: 1.5</span><br><span class="line">we get int result: 1</span><br></pre></td></tr></table></figure>



<h1 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h1><ol>
<li><a href="https://github.com/progschj/ThreadPool">progschj/ThreadPool</a></li>
<li><a href="https://www.cnblogs.com/haippy/p/3237213.html">C++11 并发指南三(std::mutex 详解)</a></li>
<li><a href="https://www.cnblogs.com/haippy/p/3252041.html">C++11 并发指南五(std::condition_variable 详解)</a></li>
<li><a href="https://www.cnblogs.com/ailumiyana/p/10016965.html">基于C++11实现线程池的工作原理</a></li>
</ol>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>ThreadPool</tag>
      </tags>
  </entry>
  <entry>
    <title>Golang项目的常用库</title>
    <url>/2021/01/31/Golang%E9%A1%B9%E7%9B%AE%E7%9A%84%E5%B8%B8%E7%94%A8%E5%BA%93/</url>
    <content><![CDATA[<p>[TOC]</p>
<h2 id="类型转化"><a href="#类型转化" class="headerlink" title="类型转化"></a>类型转化</h2><p><a href="https://github.com/spf13/cast">spf13/cast</a></p>
<h4 id="官方说明"><a href="#官方说明" class="headerlink" title="官方说明"></a>官方说明</h4><blockquote>
<p>Easy and safe casting from one type to another in Go</p>
<blockquote>
<p>Cast is a library to convert between different go types in a consistent and easy way.</p>
</blockquote>
</blockquote>
<h4 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h4><p>Example ‘ToString’:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cast.ToString(&quot;mayonegg&quot;)         &#x2F;&#x2F; &quot;mayonegg&quot;</span><br><span class="line">cast.ToString(8)                  &#x2F;&#x2F; &quot;8&quot;</span><br><span class="line">cast.ToString(8.31)               &#x2F;&#x2F; &quot;8.31&quot;</span><br><span class="line">cast.ToString([]byte(&quot;one time&quot;)) &#x2F;&#x2F; &quot;one time&quot;</span><br><span class="line">cast.ToString(nil)                &#x2F;&#x2F; &quot;&quot;</span><br><span class="line"></span><br><span class="line">var foo interface&#123;&#125; &#x3D; &quot;one more time&quot;</span><br><span class="line">cast.ToString(foo)                &#x2F;&#x2F; &quot;one more time&quot;</span><br></pre></td></tr></table></figure>
<p>当我们在项目（be-derive）中需要根据配置来决定返回值类型时，可以使用cast在最后阶段完成一个“统一的操作”</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func (this *Derive) formatCalcResult(val interface&#123;&#125;, typeName string) interface&#123;&#125; &#123;</span><br><span class="line">	switch typeName &#123;</span><br><span class="line">	case &quot;float&quot;:</span><br><span class="line">		fallthrough</span><br><span class="line">	case &quot;double&quot;:</span><br><span class="line">		return math.Trunc(cast.ToFloat64(val)*1e2+0.5) * 1e-2</span><br><span class="line">	case &quot;int&quot;:</span><br><span class="line">		return math.Trunc(cast.ToFloat64(val))</span><br><span class="line">	default:</span><br><span class="line">		return val</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="json处理"><a href="#json处理" class="headerlink" title="json处理"></a>json处理</h2><p><a href="https://github.com/tidwall/gjson">tidwall/gjson</a><br><a href="https://github.com/tidwall/sjson">tidwall/sjon</a></p>
<h4 id="官方说明-1"><a href="#官方说明-1" class="headerlink" title="官方说明"></a>官方说明</h4><blockquote>
<p> GJSON is a Go package that provides a fast and simple way to get values from a json document. It has features such as one line retrieval, dot notation paths, iteration, and parsing json lines.</p>
</blockquote>
<blockquote>
<p> SJSON is a Go package that provides a very fast and simple way to set a value in a json document. The purpose for this library is to provide efficient json updating for the SummitDB project. For quickly retrieving json values check out GJSON.</p>
</blockquote>
<h4 id="使用示例-1"><a href="#使用示例-1" class="headerlink" title="使用示例"></a>使用示例</h4><p>To unmarshal to a map[string]interface{}:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">m, ok :&#x3D; gjson.Parse(json).Value().(map[string]interface&#123;&#125;)</span><br><span class="line">if !ok &#123;</span><br><span class="line">	&#x2F;&#x2F; not a map</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Get a json value</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">if !gjson.Valid(json) &#123;</span><br><span class="line">	return errors.New(&quot;invalid json&quot;)</span><br><span class="line">&#125;</span><br><span class="line">value :&#x3D; gjson.Get(json, &quot;name.last&quot;)</span><br></pre></td></tr></table></figure>
<p>Set vlaue to a empty string or json string</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">value, _ :&#x3D; sjson.Set(&quot;&quot;, &quot;name.last&quot;, &quot;Anderson&quot;)</span><br><span class="line">println(value)</span><br><span class="line">&#x2F;&#x2F; Output:</span><br><span class="line">&#x2F;&#x2F; &#123;&quot;name&quot;:&#123;&quot;last&quot;:&quot;Anderson&quot;&#125;&#125;</span><br><span class="line"></span><br><span class="line">value, _ :&#x3D; sjson.Set(&#96;&#123;&quot;name&quot;:&#123;&quot;last&quot;:&quot;Anderson&quot;&#125;&#125;&#96;, &quot;name.first&quot;, &quot;Sara&quot;)</span><br><span class="line">println(value)</span><br><span class="line">&#x2F;&#x2F; Output:</span><br><span class="line">&#x2F;&#x2F; &#123;&quot;name&quot;:&#123;&quot;first&quot;:&quot;Sara&quot;,&quot;last&quot;:&quot;Anderson&quot;&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>以下代码（arch-tool）的功能为：将kv为jsonpath类型的字符串，转化为标准的json格式<br>from <code>&#123;&quot;a.b.c&quot;:1, &quot;a.d&quot;:true&#125;</code> to <code>&#123;&quot;a&quot;:&#123;&quot;b&quot;:&#123;&quot;c&quot;:1&#125;, &quot;d&quot;:true&#125;&#125;</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">aeFeatures :&#x3D; make(map[string]interface&#123;&#125;)</span><br><span class="line">err :&#x3D; json.Unmarshal([]byte(gjson.Get(aeItemStr, &quot;features&quot;).Raw), &amp;aeFeatures)</span><br><span class="line">if err !&#x3D; nil &#123;</span><br><span class="line">	log.Println(fmt.Sprintf(&quot;json.Unmarshal aeItemStr features err&#x3D;%v&quot;, err))</span><br><span class="line">	continue</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">profileFeaturesStr :&#x3D; &#96;&#123; &#125;&#96;</span><br><span class="line">for featuresKey, featuresValue :&#x3D; range aeFeatures &#123;</span><br><span class="line">	profileFeaturesStr, _ &#x3D; sjson.Set(profileFeaturesStr, featuresKey, featuresValue)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="http请求"><a href="#http请求" class="headerlink" title="http请求"></a>http请求</h1><p><a href="https://github.com/parnurzeal/gorequest">parnurzeal/gorequest</a></p>
<h4 id="官方说明-2"><a href="#官方说明-2" class="headerlink" title="官方说明"></a>官方说明</h4><blockquote>
<p>GoRequest – Simplified HTTP client ( inspired by famous SuperAgent lib in Node.js )</p>
<blockquote>
<p>GoRequest makes thing much more simple for you, making http client more awesome and fun like SuperAgent + golang style usage.</p>
</blockquote>
</blockquote>
<h4 id="使用示例-2"><a href="#使用示例-2" class="headerlink" title="使用示例"></a>使用示例</h4><p>Sth in oneline<br><code>resp, body, errs = gorequest.New().Timeout(time.Duration(inter.Timeout) * time.Millisecond).Proxy(&quot;http://proxy:999&quot;).Get(&quot;http://example-no-proxy.com&quot;).End()</code><br>Call Back:<br>Moreover, GoRequest also supports callback function. This gives you much more flexibility on using it. You can use it any way to match your own style! Let’s see a bit of callback example:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func printStatus(resp gorequest.Response, body string, errs []error)&#123;</span><br><span class="line">  fmt.Println(resp.Status)</span><br><span class="line">&#125;</span><br><span class="line">gorequest.New().Get(&quot;http:&#x2F;&#x2F;example.com&quot;).End(printStatus)</span><br></pre></td></tr></table></figure>

<p>For a JSON POST with standard libraries, you might need to marshal map data structure to json format, set headers to ‘application/json’ (and other headers if you need to) and declare http.Client. So, your code becomes longer and harder to maintain:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">m :&#x3D; map[string]interface&#123;&#125;&#123;</span><br><span class="line">  &quot;name&quot;: &quot;backy&quot;,</span><br><span class="line">  &quot;species&quot;: &quot;dog&quot;,</span><br><span class="line">&#125;</span><br><span class="line">mJson, _ :&#x3D; json.Marshal(m)</span><br><span class="line">contentReader :&#x3D; bytes.NewReader(mJson)</span><br><span class="line">req, _ :&#x3D; http.NewRequest(&quot;POST&quot;, &quot;http:&#x2F;&#x2F;example.com&quot;, contentReader)</span><br><span class="line">req.Header.Set(&quot;Content-Type&quot;, &quot;application&#x2F;json&quot;)</span><br><span class="line">req.Header.Set(&quot;Notes&quot;,&quot;GoRequest is coming!&quot;)</span><br><span class="line">client :&#x3D; &amp;http.Client&#123;&#125;</span><br><span class="line">resp, _ :&#x3D; client.Do(req)</span><br></pre></td></tr></table></figure>

<p>摘取了项目（ext-profile）中的使用示例</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">request :&#x3D; gorequest.New().Timeout(time.Duration(inter.Timeout) * time.Millisecond)</span><br><span class="line">for key, value :&#x3D; range header &#123;</span><br><span class="line">	request.Header.Set(key, value)</span><br><span class="line">&#125;</span><br><span class="line">for i :&#x3D; inter.Retries; i &gt; 0; i-- &#123;</span><br><span class="line">	resp, result, errs :&#x3D; request.Post(inter.Url).Send(body).End()</span><br><span class="line">	if err :&#x3D; resp.Body.Close(); err!&#x3D; nil &#123;</span><br><span class="line">		ilog.LogReqErr(ilog.LL_ERROR, &quot;async&quot;, feature.RequestId, &quot;resp body close err&quot;, err)</span><br><span class="line">	&#125;</span><br><span class="line">	if errs !&#x3D; nil &#123;</span><br><span class="line">		&#x2F;&#x2F;TODO log</span><br><span class="line">		ilog.LogReqErr(ilog.LL_ERROR, &quot;async&quot;, feature.RequestId, &quot;get third party result fail&quot;, errs)</span><br><span class="line">		continue</span><br><span class="line">	&#125;</span><br><span class="line">	return &amp;resultItem&#123;result: result, identification: inter.UrlName&#125;, nil</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Golang module 收集</category>
      </categories>
      <tags>
        <tag>Gloang</tag>
      </tags>
  </entry>
  <entry>
    <title>Resource temporarily unavailable的出现原因</title>
    <url>/2021/04/20/Resource-temporarily-unavailable%E7%9A%84%E5%87%BA%E7%8E%B0%E5%8E%9F%E5%9B%A0/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>网络</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake书写（二）</title>
    <url>/2020/05/27/cmake%E4%B9%A6%E5%86%99/</url>
    <content><![CDATA[<h3 id="1-Makefile里有什么？"><a href="#1-Makefile里有什么？" class="headerlink" title="1. Makefile里有什么？"></a>1. Makefile里有什么？</h3><p>Makefile里主要包含了五个东西：显式规则、隐晦规则、变量定义、文件指示和注释。</p>
<ol>
<li>显式规则。显式规则说明了，如何生成一个或多的的目标文件。这是由Makefile的书写者明显指出，要生成的文件，文件的依赖文件，生成的命令。</li>
<li>隐晦规则。由于我们的make有自动推导的功能，所以隐晦的规则可以让我们比较粗糙地简略地书写Makefile，这是由make所支持的。</li>
<li>变量的定义。在Makefile中我们要定义一系列的变量，变量一般都是字符串，这个有点你C语言中的宏，当Makefile被执行时，其中的变量都会被扩展到相应的引用位置上。</li>
<li>文件指示。其包括了三个部分，一个是在一个Makefile中引用另一个Makefile，就像C语言中的include一样；另一个是指根据某些情况指定Makefile中的有效部分，就像C语言中的预编译#if一样；还有就是定义一个多行的命令。有关这一部分的内容，我会在后续的部分中讲述。</li>
<li>注释。Makefile中只有行注释，和UNIX的Shell脚本一样，其注释是用“#”字符，这个就像C/C++中的“//”一样。如果你要在你的Makefile中使用“#”字符，可以用反斜框进行转义，如：“#”。</li>
</ol>
<p>最后，还值得一提的是，在Makefile中的命令，必须要以[Tab]键开始。</p>
<h3 id="2-Makefile的文件名"><a href="#2-Makefile的文件名" class="headerlink" title="2. Makefile的文件名"></a>2. Makefile的文件名</h3><p>默认的情况下， make命令会在当前目录下按照顺序寻找文件名，<br>分别为“GNUmakefile”、“makefile”、“Makefile”的文件，找到了解释这个文件。在这三个文件名中，最好使用“Makefile”这个文件名，因为，这个文件名第一个字符为大写，这样有一种显目的感觉。最好不要用“GNUmakefile”，这个文件是GNU的make识别的。有另外一些make只对全小写的“makefile”文件名敏感，但是基本上来说，大多数的make都支持“makefile”和“Makefile”这两种默认文件名。</p>
<p>当然，你可以使用别的文件名来书写Makefile，比如：“Make.Linux”，“Make.Solaris”，“Make.AIX”等，如果要指定特定的Makefile，你可以使用make的“-f”和“–file”参数，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make -f Make.Linux或make --file Make.AIX。</span><br></pre></td></tr></table></figure>
<h3 id="3-引用其它的Makefile"><a href="#3-引用其它的Makefile" class="headerlink" title="3. 引用其它的Makefile"></a>3. 引用其它的Makefile</h3><p>在Makefile使用include关键字可以把别的Makefile包含进来，这很像C语言的#include，被包含的文件会原模原样的放在当前文件的包含位置。include的语法是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">include &lt;filename&gt;;</span><br><span class="line">filename可以是当前操作系统Shell的文件模式（可以保含路径和通配符）</span><br></pre></td></tr></table></figure>
<p>在include前面可以有一些空字符，但是绝不能是[Tab]键开始。include和<filename>;可以用一个或多个空格隔开。举个例子，你有这样几个Makefile：a.mk、b.mk、c.mk，还有一个文件叫foo.make，以及一个变量$(bar)，其包含了e.mk和f.mk，那么，下面的语句：</filename></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">include foo.make *.mk $(bar)</span><br><span class="line">等价于：</span><br><span class="line">include foo.make a.mk b.mk c.mk e.mk f.mk</span><br></pre></td></tr></table></figure>
<p>make命令开始时，会把找寻include所指出的其它Makefile，并把其内容安置在当前的位置。就好像C/C++的#include指令一样。如果文件都没有指定绝对路径或是相对路径的话，make会在当前目录下首先寻找，如果当前目录下没有找到，那么，make还会在下面的几个目录下找：</p>
<ol>
<li>如果make执行时，有“-I”或“–include-dir”参数，那么make就会在这个参数所指定的目录下去寻找。</li>
<li>如果目录<prefix>;/include（一般是：/usr/local/bin或/usr/include）存在的话，make也会去找。</prefix></li>
</ol>
<p>如果有文件没有找到的话，make会生成一条警告信息，但不会马上出现致命错误。它会继续载入其它的文件，一旦完成makefile的读取，make会再重试这些没有找到，或是不能读取的文件，如果还是不行，make才会出现一条致命信息。如果你想让make不理那些无法读取的文件，而继续执行，你可以在include前加一个减号“-”。如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-include &lt;filename&gt;;</span><br><span class="line">其表示，无论include过程中出现什么错误，都不要报错继续执行。和其它版本make兼容的相关命令是sinclude，其作用和这一个是一样的。</span><br></pre></td></tr></table></figure>
<h3 id="4-环境变量-MAKEFILES"><a href="#4-环境变量-MAKEFILES" class="headerlink" title="4. 环境变量 MAKEFILES"></a>4. 环境变量 MAKEFILES</h3><p>如果你的当前环境中定义了环境变量MAKEFILES，那么，make会把这个变量中的值做一个类似于include的动作。这个变量中的值是其它的Makefile，用空格分隔。只是，它和include不同的是，从这个环境变中引入的Makefile的“目标”不会起作用，如果环境变量中定义的文件发现错误，make也会不理。</p>
<p>但是在这里我还是建议不要使用这个环境变量，因为只要这个变量一被定义，那么当你使用make时，所有的Makefile都会受到它的影响，这绝不是你想看到的。在这里提这个事，只是为了告诉大家，也许有时候你的Makefile出现了怪事，那么你可以看看当前环境中有没有定义这个变量。</p>
<h3 id="5-make的工作方式"><a href="#5-make的工作方式" class="headerlink" title="5. make的工作方式"></a>5. make的工作方式</h3><p>GNU的make工作时的执行步骤入下：（想来其它的make也是类似）</p>
<ol>
<li>读入所有的Makefile。</li>
<li>读入被include的其它Makefile。</li>
<li>初始化文件中的变量。</li>
<li>推导隐晦规则，并分析所有规则。</li>
<li>为所有的目标文件创建依赖关系链。</li>
<li>根据依赖关系，决定哪些目标要重新生成。</li>
<li>执行生成命令。<br>1-5步为第一个阶段，6-7为第二个阶段。<br>第一个阶段中，如果定义的变量被使用了，那么，make会把其展开在使用的位置。但make并不会完全马上展开，make使用的是拖延战术，如果变量出现在依赖关系的规则中，那么仅当这条依赖被决定要使用了，变量才会在其内部展开。</li>
</ol>
<p>当然，这个工作方式你不一定要清楚，但是知道这个方式你也会对make更为熟悉。有了这个基础，后续部分也就容易看懂了。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake书写命令（五）</title>
    <url>/2020/05/27/cmake%E4%B9%A6%E5%86%99%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<p>书写命令<br>————</p>
<p>每条规则中的命令和操作系统Shell的命令行是一致的。make会一按顺序一条一条的执行命令，每条命令的开头必须以[Tab]键开头，除非，命令是紧跟在依赖规则后面的分号后的。在命令行之间中的空格或是空行会被忽略，但是如果该空格或空行是以Tab键开头的，那么make会认为其是一个空命令。</p>
<p>我们在UNIX下可能会使用不同的Shell，但是make的命令默认是被“/bin/sh”——UNIX的标准Shell解释执行的。除非你特别指定一个其它的Shell。Makefile中，“#”是注释符，很像C/C++中的“//”，其后的本行字符都被注释。</p>
<p>一、显示命令</p>
<p>通常，make会把其要执行的命令行在命令执行前输出到屏幕上。当我们用“@”字符在命令行前，那么，这个命令将不被make显示出来，最具代表性的例子是，我们用这个功能来像屏幕显示一些信息。如：</p>
<pre><code>@echo 正在编译XXX模块......</code></pre>
<p>当make执行时，会输出“正在编译XXX模块……”字串，但不会输出命令，如果没有“@”，那么，make将输出：</p>
<pre><code>echo 正在编译XXX模块......
正在编译XXX模块......</code></pre>
<p>如果make执行时，带入make参数“-n”或“–just-print”，那么其只是显示命令，但不会执行命令，这个功能很有利于我们调试我们的Makefile，看看我们书写的命令是执行起来是什么样子的或是什么顺序的。</p>
<p>而make参数“-s”或“–slient”则是全面禁止命令的显示。</p>
<p>二、命令执行</p>
<p>当依赖目标新于目标时，也就是当规则的目标需要被更新时，make会一条一条的执行其后的命令。需要注意的是，如果你要让上一条命令的结果应用在下一条命令时，你应该使用分号分隔这两条命令。比如你的第一条命令是cd命令，你希望第二条命令得在cd之后的基础上运行，那么你就不能把这两条命令写在两行上，而应该把这两条命令写在一行上，用分号分隔。如：</p>
<pre><code>示例一：
    exec:
            cd /home/hchen
            pwd

示例二：
    exec:
            cd /home/hchen; pwd</code></pre>
<p>当我们执行“make exec”时，第一个例子中的cd没有作用，pwd会打印出当前的Makefile目录，而第二个例子中，cd就起作用了，pwd会打印出“/home/hchen”。</p>
<p>make一般是使用环境变量SHELL中所定义的系统Shell来执行命令，默认情况下使用UNIX的标准Shell——/bin/sh来执行命令。但在MS-DOS下有点特殊，因为MS-DOS下没有SHELL环境变量，当然你也可以指定。如果你指定了UNIX风格的目录形式，首先，make会在SHELL所指定的路径中找寻命令解释器，如果找不到，其会在当前盘符中的当前目录中寻找，如果再找不到，其会在PATH环境变量中所定义的所有路径中寻找。MS-DOS中，如果你定义的命令解释器没有找到，其会给你的命令解释器加上诸如“.exe”、“.com”、“.bat”、“.sh”等后缀。</p>
<p>三、命令出错</p>
<p>每当命令运行完后，make会检测每个命令的返回码，如果命令返回成功，那么make会执行下一条命令，当规则中所有的命令成功返回后，这个规则就算是成功完成了。如果一个规则中的某个命令出错了（命令退出码非零），那么make就会终止执行当前规则，这将有可能终止所有规则的执行。</p>
<p>有些时候，命令的出错并不表示就是错误的。例如mkdir命令，我们一定需要建立一个目录，如果目录不存在，那么mkdir就成功执行，万事大吉，如果目录存在，那么就出错了。我们之所以使用mkdir的意思就是一定要有这样的一个目录，于是我们就不希望mkdir出错而终止规则的运行。</p>
<p>为了做到这一点，忽略命令的出错，我们可以在Makefile的命令行前加一个减号“-”（在Tab键之后），标记为不管命令出不出错都认为是成功的。如：</p>
<p>   clean:<br>            -rm -f *.o</p>
<p>还有一个全局的办法是，给make加上“-i”或是“–ignore-errors”参数，那么，Makefile中所有命令都会忽略错误。而如果一个规则是以“.IGNORE”作为目标的，那么这个规则中的所有命令将会忽略错误。这些是不同级别的防止命令出错的方法，你可以根据你的不同喜欢设置。</p>
<p>还有一个要提一下的make的参数的是“-k”或是“–keep-going”，这个参数的意思是，如果某规则中的命令出错了，那么就终目该规则的执行，但继续执行其它规则。</p>
<p>四、嵌套执行make</p>
<p>在一些大的工程中，我们会把我们不同模块或是不同功能的源文件放在不同的目录中，我们可以在每个目录中都书写一个该目录的Makefile，这有利于让我们的Makefile变得更加地简洁，而不至于把所有的东西全部写在一个Makefile中，这样会很难维护我们的Makefile，这个技术对于我们模块编译和分段编译有着非常大的好处。</p>
<p>例如，我们有一个子目录叫subdir，这个目录下有个Makefile文件，来指明了这个目录下文件的编译规则。那么我们总控的Makefile可以这样书写：</p>
<pre><code>subsystem:
        cd subdir &amp;&amp; $(MAKE)</code></pre>
<p>其等价于：</p>
<pre><code>subsystem:
        $(MAKE) -C subdir</code></pre>
<p>定义$(MAKE)宏变量的意思是，也许我们的make需要一些参数，所以定义成一个变量比较利于维护。这两个例子的意思都是先进入“subdir”目录，然后执行make命令。</p>
<p>我们把这个Makefile叫做“总控Makefile”，总控Makefile的变量可以传递到下级的Makefile中（如果你显示的声明），但是不会覆盖下层的Makefile中所定义的变量，除非指定了“-e”参数。</p>
<p>如果你要传递变量到下级Makefile中，那么你可以使用这样的声明：</p>
<pre><code>export &lt;variable ...&gt;;</code></pre>
<p>如果你不想让某些变量传递到下级Makefile中，那么你可以这样声明： </p>
<pre><code>unexport &lt;variable ...&gt;;</code></pre>
<p>如：</p>
<pre><code>示例一：

    export variable = value

    其等价于：

    variable = value
    export variable

    其等价于：

    export variable := value

    其等价于：

    variable := value
    export variable

示例二：

    export variable += value

    其等价于：

    variable += value
    export variable</code></pre>
<p>如果你要传递所有的变量，那么，只要一个export就行了。后面什么也不用跟，表示传递所有的变量。</p>
<p>需要注意的是，有两个变量，一个是SHELL，一个是MAKEFLAGS，这两个变量不管你是否export，其总是要传递到下层Makefile中，特别是MAKEFILES变量，其中包含了make的参数信息，如果我们执行“总控Makefile”时有make参数或是在上层Makefile中定义了这个变量，那么MAKEFILES变量将会是这些参数，并会传递到下层Makefile中，这是一个系统级的环境变量。</p>
<p>但是make命令中的有几个参数并不往下传递，它们是“-C”,“-f”,“-h”“-o”和“-W”（有关Makefile参数的细节将在后面说明），如果你不想往下层传递参数，那么，你可以这样来：</p>
<pre><code>subsystem:
        cd subdir &amp;&amp; $(MAKE) MAKEFLAGS=</code></pre>
<p>如果你定义了环境变量MAKEFLAGS，那么你得确信其中的选项是大家都会用到的，如果其中有“-t”,“-n”,和“-q”参数，那么将会有让你意想不到的结果，或许会让你异常地恐慌。</p>
<p>还有一个在“嵌套执行”中比较有用的参数，“-w”或是“–print-directory”会在make的过程中输出一些信息，让你看到目前的工作目录。比如，如果我们的下级make目录是“/home/hchen/gnu/make”，如果我们使用“make -w”来执行，那么当进入该目录时，我们会看到：</p>
<pre><code>make: Entering directory `/home/hchen/gnu/make&#39;.</code></pre>
<p>而在完成下层make后离开目录时，我们会看到：</p>
<pre><code>make: Leaving directory `/home/hchen/gnu/make&#39;</code></pre>
<p>当你使用“-C”参数来指定make下层Makefile时，“-w”会被自动打开的。如果参数中有“-s”（“–slient”）或是“–no-print-directory”，那么，“-w”总是失效的。</p>
<p>五、定义命令包</p>
<p>如果Makefile中出现一些相同命令序列，那么我们可以为这些相同的命令序列定义一个变量。定义这种命令序列的语法以“define”开始，以“endef”结束，如：</p>
<pre><code>define run-yacc
yacc $(firstword $^)
mv y.tab.c $@
endef</code></pre>
<p>这里，“run-yacc”是这个命令包的名字，其不要和Makefile中的变量重名。在“define”和“endef”中的两行就是命令序列。这个命令包中的第一个命令是运行Yacc程序，因为Yacc程序总是生成“y.tab.c”的文件，所以第二行的命令就是把这个文件改改名字。还是把这个命令包放到一个示例中来看看吧。</p>
<pre><code>foo.c : foo.y
        $(run-yacc)</code></pre>
<p>我们可以看见，要使用这个命令包，我们就好像使用变量一样。在这个命令包的使用中，命令包“run-yacc”中的“$^”就是“foo.y”，“$@”就是“foo.c”（有关这种以“$”开头的特殊变量，我们会在后面介绍），make在执行命令包时，命令包中的每个命令会被依次独立执行。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake乱七八糟</title>
    <url>/2020/06/03/cmake%E4%B9%B1%E4%B8%83%E5%85%AB%E7%B3%9F/</url>
    <content><![CDATA[<p>cmake 编译安装库到指定目录<br>1.设置安装路径（.必不可少）</p>
<p>cmake -DCMAKE_INSTALL_PREFIX=/opt/cpp-netlib .</p>
<p>2.编译</p>
<p>make -j2</p>
<p>3.安装</p>
<p>sudo make install</p>
<h1 id="Cmake-编译静库-动态库-指定输出路径"><a href="#Cmake-编译静库-动态库-指定输出路径" class="headerlink" title="Cmake 编译静库 动态库 指定输出路径"></a>Cmake 编译静库 动态库 指定输出路径</h1><p>cmake_minimum_required(VERSION 3.11)</p>
<p>define_source_files（）</p>
<p>Project(CmakeTest)<br>aux_source_directory(. src)<br>#add_executable(project1 ${src})                        #编译为可执行程序<br>#add_library(project1 ${src})                           #编译为静态库<br>#add_library(project1 SHARED ${src})                    #编译为动态链接库</p>
<p>#add_executable(project1 MACOSX_BUNDLE ${src})          #编译为可执行程序 *.app</p>
<p>#add_library(project1 MODULE ${src})                    #编译为程序资源包 *.bundle<br>#set_target_properties(project1 PROPERTIES BUNDLE TRUE)</p>
<p>#add_library(project1 SHARED ${src})                     #编译为程序资源包 *.framework<br>#set_target_properties(project1 PROPERTIES FRAMEWORK TRUE)</p>
<p>SET(CMAKE_CXX_FLAGS_DEBUG “$ENV{CXXFLAGS} -O0 -Wall -g -ggdb”)  # Debug模式下的编译指令<br>SET(CMAKE_CXX_FLAGS_RELEASE “$ENV{CXXFLAGS} -O3 -Wall”)         # Release模式下的编译指令</p>
<p>#SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/../bin)       #设置可执行文件的输出目录</p>
<p>#SET(LIBRARY_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/../lib)           #设置库文件的输出目录</p>
<p>#set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_DEBUG ${PROJECT_SOURCE_DIR}/bin)   </p>
<p>#set(CMAKE_RUNTIME_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/../bin) </p>
<p>#上面两条语句分别设置了Debug版本和Release版本可执行文件的输出目录,</p>
<p>#一旦设置上面的属性,在任何环境下生成的可执行文件都将直接放在你所设置的目录.</p>
<p>#四. set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_DEBUG ${PROJECT_SOURCE_DIR}/../lib)    </p>
<h1 id="set-CMAKE-ARCHIVE-OUTPUT-DIRECTORY-RELEASE-PROJECT-SOURCE-DIR-lib"><a href="#set-CMAKE-ARCHIVE-OUTPUT-DIRECTORY-RELEASE-PROJECT-SOURCE-DIR-lib" class="headerlink" title="set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/../lib)"></a>set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/../lib)</h1><p>#上面两条语句分别设置了Debug版本和Release版本库文件的输出目录,</p>
<p>#一旦设置上面的属性,在任何环境下生成的库文件都将直接放在你所设置的目录.<br>————————————————<br>版权声明：本文为CSDN博主「LitteBlueSea」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。<br>原文链接：<a href="https://blog.csdn.net/nan_feng_yu/article/details/80808773">https://blog.csdn.net/nan_feng_yu/article/details/80808773</a></p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake使用变量（四）</title>
    <url>/2020/05/27/cmake%E4%BD%BF%E7%94%A8%E5%8F%98%E9%87%8F/</url>
    <content><![CDATA[<h2 id="使用变量"><a href="#使用变量" class="headerlink" title="使用变量"></a>使用变量</h2><p>在Makefile中的定义的变量，就像是C/C++语言中的宏一样，他代表了一个文本字串，在Makefile中执行的时候其会自动原模原样地展开在所使用的地方。其与C/C++所不同的是，你可以在Makefile中改变其值。在Makefile中，变量可以使用在“目标”，“依赖目标”，“命令”或是Makefile的其它部分中。</p>
<p>变量的命名字可以包含字符、数字，下划线（可以是数字开头），但不应该含有“:”、“#”、“=”或是空字符（空格、回车等）。变量是大小写敏感的，“foo”、“Foo”和“FOO”是三个不同的变量名。传统的Makefile的变量名是全大写的命名方式，但我推荐使用大小写搭配的变量名，如：MakeFlags。这样可以避免和系统的变量冲突，而发生意外的事情。</p>
<p>有一些变量是很奇怪字串，如“$&lt;”、“$@”等，这些是自动化变量，我会在后面介绍。</p>
<h3 id="1-变量的基础"><a href="#1-变量的基础" class="headerlink" title="1. 变量的基础"></a>1. 变量的基础</h3><p>变量在声明时需要给予初值，而在使用时，需要给在变量名前加上“$”符号，但最好用小括号“（）”或是大括号“{}”把变量给包括起来。如果你要使用真实的“$”字符，那么你需要用“$$”来表示。</p>
<p>变量可以使用在许多地方，如规则中的“目标”、“依赖”、“命令”以及新的变量中。先看一个例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; program.o foo.o utils.o</span><br><span class="line">program : $(objects)</span><br><span class="line">        cc -o program $(objects)</span><br><span class="line"></span><br><span class="line">$(objects) : defs.h</span><br></pre></td></tr></table></figure>
<p>变量会在使用它的地方精确地展开，就像C/C++中的宏一样，例如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    foo &#x3D; c</span><br><span class="line">    prog.o : prog.$(foo)</span><br><span class="line">            $(foo)$(foo) -$(foo) prog.$(foo)</span><br><span class="line"></span><br><span class="line">展开后得到：</span><br><span class="line"></span><br><span class="line">    prog.o : prog.c</span><br><span class="line">            cc -c prog.c</span><br></pre></td></tr></table></figure>
<p>当然，千万不要在你的Makefile中这样干，这里只是举个例子来表明Makefile中的变量在使用处展开的真实样子。可见其就是一个“替代”的原理。<br>另外，给变量加上括号完全是为了更加安全地使用这个变量，在上面的例子中，如果你不想给变量加上括号，那也可以，但我还是强烈建议你给变量加上括号。</p>
<h3 id="2-变量中的变量"><a href="#2-变量中的变量" class="headerlink" title="2. 变量中的变量"></a>2. 变量中的变量</h3><p>在定义变量的值时，我们可以使用其它变量来构造变量的值，在Makefile中有两种方式来在用变量定义变量的值。<br>先看第一种方式，也就是简单的使用“=”号，在“=”左侧是变量，右侧是变量的值，右侧变量的值可以定义在文件的任何一处，也就是说，右侧中的变量不一定非要是已定义好的值，其也可以使用后面定义的值。如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">foo &#x3D; $(bar)</span><br><span class="line">bar &#x3D; $(ugh)</span><br><span class="line">ugh &#x3D; Huh?</span><br><span class="line"></span><br><span class="line">all:</span><br><span class="line">        echo $(foo)</span><br></pre></td></tr></table></figure>
<p>我们执行“make all”将会打出变量$(foo)的值是“Huh?”（ $(foo)的值是$(bar)，$(bar)的值是$(ugh)，$(ugh)的值是“Huh?”）可见，变量是可以使用后面的变量来定义的。</p>
<p>这个功能有好的地方，也有不好的地方，好的地方是，我们可以把变量的真实值推到后面来定义，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CFLAGS &#x3D; $(include_dirs) -O</span><br><span class="line">include_dirs &#x3D; -Ifoo -Ibar</span><br></pre></td></tr></table></figure>
<p>当“CFLAGS”在命令中被展开时，会是“-Ifoo -Ibar -O”。但这种形式也有不好的地方，那就是递归定义，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CFLAGS &#x3D; $(CFLAGS) -O</span><br><span class="line">A &#x3D; $(B)</span><br><span class="line">B &#x3D; $(A)</span><br></pre></td></tr></table></figure>
<p>这会让make陷入无限的变量展开过程中去，当然，我们的make是有能力检测这样的定义，并会报错。还有就是如果在变量中使用函数，那么，这种方式会让我们的make运行时非常慢，更糟糕的是，他会使用得两个make的函数“wildcard”和“shell”发生不可预知的错误。因为你不会知道这两个函数会被调用多少次。</p>
<p>为了避免上面的这种方法，我们可以使用make中的另一种用变量来定义变量的方法。这种方法使用的是“:=”操作符，如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    x :&#x3D; foo</span><br><span class="line">    y :&#x3D; $(x) bar</span><br><span class="line">    x :&#x3D; later</span><br><span class="line">其等价于：</span><br><span class="line">    y :&#x3D; foo bar</span><br><span class="line">    x :&#x3D; later</span><br></pre></td></tr></table></figure>
<p>值得一提的是，这种方法，前面的变量不能使用后面的变量，只能使用前面已定义好了的变量。如果是这样：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">y :&#x3D; $(x) bar</span><br><span class="line">x :&#x3D; foo</span><br></pre></td></tr></table></figure>
<p>那么，y的值是“bar”，而不是“foo bar”。<br>上面都是一些比较简单的变量使用了，让我们来看一个复杂的例子，其中包括了make的函数、条件表达式和一个系统变量“MAKELEVEL”的使用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ifeq (0,$&#123;MAKELEVEL&#125;)</span><br><span class="line">cur-dir   :&#x3D; $(shell pwd)</span><br><span class="line">whoami    :&#x3D; $(shell whoami)</span><br><span class="line">host-type :&#x3D; $(shell arch)</span><br><span class="line">MAKE :&#x3D; $&#123;MAKE&#125; host-type&#x3D;$&#123;host-type&#125; whoami&#x3D;$&#123;whoami&#125;</span><br><span class="line">endif</span><br></pre></td></tr></table></figure>
<p>关于条件表达式和函数，我们在后面再说，对于系统变量“MAKELEVEL”，其意思是，如果我们的make有一个嵌套执行的动作（参见前面的“嵌套使用make”），那么，这个变量会记录了我们的当前Makefile的调用层数。</p>
<p>下面再介绍两个定义变量时我们需要知道的，请先看一个例子，如果我们要定义一个变量，其值是一个空格，那么我们可以这样来：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nullstring :&#x3D;</span><br><span class="line">space :&#x3D; $(nullstring) # end of the line</span><br></pre></td></tr></table></figure>
<p>nullstring是一个Empty变量，其中什么也没有，而我们的space的值是一个空格。因为在操作符的右边是很难描述一个空格的，这里采用的技术很管用，先用一个Empty变量来标明变量的值开始了，而后面采用“#”注释符来表示变量定义的终止，这样，我们可以定义出其值是一个空格的变量。请注意这里关于“#”的使用，注释符“#”的这种特性值得我们注意，如果我们这样定义一个变量：</p>
<pre><code>dir := /foo/bar    # directory to put the frobs in</code></pre>
<p>dir这个变量的值是“/foo/bar”，后面还跟了4个空格，如果我们这样使用这样变量来指定别的目录——“$(dir)/file”那么就完蛋了。</p>
<p>还有一个比较有用的操作符是“?=”，先看示例：</p>
<pre><code>FOO ?= bar</code></pre>
<p>其含义是，如果FOO没有被定义过，那么变量FOO的值就是“bar”，如果FOO先前被定义过，那么这条语将什么也不做，其等价于：</p>
<pre><code>ifeq ($(origin FOO), undefined)
  FOO = bar
endif</code></pre>
<p>三、变量高级用法</p>
<p>这里介绍两种变量的高级使用方法，第一种是变量值的替换。</p>
<p>我们可以替换变量中的共有的部分，其格式是“$(var:a=b)”或是“${var:a=b}”，其意思是，把变量“var”中所有以“a”字串“结尾”的“a”替换成“b”字串。这里的“结尾”意思是“空格”或是“结束符”。</p>
<p>还是看一个示例吧：</p>
<pre><code>foo := a.o b.o c.o
bar := $(foo:.o=.c)</code></pre>
<p>这个示例中，我们先定义了一个“$(foo)”变量，而第二行的意思是把“$(foo)”中所有以“.o”字串“结尾”全部替换成“.c”，所以我们的“$(bar)”的值就是“a.c b.c c.c”。</p>
<p>另外一种变量替换的技术是以“静态模式”（参见前面章节）定义的，如：</p>
<pre><code>foo := a.o b.o c.o
bar := $(foo:%.o=%.c)</code></pre>
<p>这依赖于被替换字串中的有相同的模式，模式中必须包含一个“%”字符，这个例子同样让$(bar)变量的值为“a.c b.c c.c”。 </p>
<p>第二种高级用法是——“把变量的值再当成变量”。先看一个例子：</p>
<pre><code>x = y
y = z
a := $($(x))</code></pre>
<p>在这个例子中，$(x)的值是“y”，所以$($(x))就是$(y)，于是$(a)的值就是“z”。（注意，是“x=y”，而不是“x=$(y)”）</p>
<p>我们还可以使用更多的层次：</p>
<pre><code>x = y
y = z
z = u
a := $($($(x)))</code></pre>
<p>这里的$(a)的值是“u”，相关的推导留给读者自己去做吧。</p>
<p>让我们再复杂一点，使用上“在变量定义中使用变量”的第一个方式，来看一个例子：</p>
<pre><code>x = $(y)
y = z
z = Hello
a := $($(x))</code></pre>
<p>这里的$($(x))被替换成了$($(y))，因为$(y)值是“z”，所以，最终结果是：a:=$(z)，也就是“Hello”。</p>
<p>再复杂一点，我们再加上函数：</p>
<pre><code>x = variable1
variable2 := Hello
y = $(subst 1,2,$(x))
z = y
a := $($($(z)))</code></pre>
<p>这个例子中，“$($($(z)))”扩展为“$($(y))”，而其再次被扩展为“$($(subst 1,2,$(x)))”。$(x)的值是“variable1”，subst函数把“variable1”中的所有“1”字串替换成“2”字串，于是，“variable1”变成“variable2”，再取其值，所以，最终，$(a)的值就是$(variable2)的值——“Hello”。（喔，好不容易）</p>
<p>在这种方式中，或要可以使用多个变量来组成一个变量的名字，然后再取其值：</p>
<pre><code>first_second = Hello
a = first
b = second
all = $($a_$b)</code></pre>
<p>这里的“$a_$b”组成了“first_second”，于是，$(all)的值就是“Hello”。</p>
<p>再来看看结合第一种技术的例子：</p>
<pre><code>a_objects := a.o b.o c.o
1_objects := 1.o 2.o 3.o

sources := $($(a1)_objects:.o=.c)</code></pre>
<p>这个例子中，如果$(a1)的值是“a”的话，那么，$(sources)的值就是“a.c b.c c.c”；如果$(a1)的值是“1”，那么$(sources)的值是“1.c 2.c 3.c”。</p>
<p>再来看一个这种技术和“函数”与“条件语句”一同使用的例子：</p>
<pre><code>ifdef do_sort
func := sort
else
func := strip
endif

bar := a d b g q c

foo := $($(func) $(bar))</code></pre>
<p>这个示例中，如果定义了“do_sort”，那么：foo := $(sort a d b g q c)，于是$(foo)的值就是“a b c d g q”，而如果没有定义“do_sort”，那么：foo := $(sort a d b g q c)，调用的就是strip函数。</p>
<p>当然，“把变量的值再当成变量”这种技术，同样可以用在操作符的左边：</p>
<pre><code>dir = foo
$(dir)_sources := $(wildcard $(dir)/*.c)
define $(dir)_print
lpr $($(dir)_sources)
endef</code></pre>
<p>这个例子中定义了三个变量：“dir”，“foo_sources”和“foo_print”。</p>
<p>四、追加变量值</p>
<p>我们可以使用“+=”操作符给变量追加值，如：</p>
<pre><code>objects = main.o foo.o bar.o utils.o
objects += another.o</code></pre>
<p>于是，我们的$(objects)值变成：“main.o foo.o bar.o utils.o another.o”（another.o被追加进去了）</p>
<p>使用“+=”操作符，可以模拟为下面的这种例子：</p>
<pre><code>objects = main.o foo.o bar.o utils.o
objects := $(objects) another.o</code></pre>
<p>所不同的是，用“+=”更为简洁。</p>
<p>如果变量之前没有定义过，那么，“+=”会自动变成“=”，如果前面有变量定义，那么“+=”会继承于前次操作的赋值符。如果前一次的是“:=”，那么“+=”会以“:=”作为其赋值符，如：</p>
<pre><code>variable := value
variable += more</code></pre>
<p>等价于：</p>
<pre><code>variable := value
variable := $(variable) more</code></pre>
<p>但如果是这种情况： </p>
<pre><code>variable = value
variable += more</code></pre>
<p>由于前次的赋值符是“=”，所以“+=”也会以“=”来做为赋值，那么岂不会发生变量的递补归定义，这是很不好的，所以make会自动为我们解决这个问题，我们不必担心这个问题。</p>
<p>五、override 指示符</p>
<p>如果有变量是通常make的命令行参数设置的，那么Makefile中对这个变量的赋值会被忽略。如果你想在Makefile中设置这类参数的值，那么，你可以使用“override”指示符。其语法是：</p>
<pre><code>override &lt;variable&gt;; = &lt;value&gt;;

override &lt;variable&gt;; := &lt;value&gt;;</code></pre>
<p>当然，你还可以追加：</p>
<pre><code>override &lt;variable&gt;; += &lt;more text&gt;;</code></pre>
<p>对于多行的变量定义，我们用define指示符，在define指示符前，也同样可以使用ovveride指示符，如：</p>
<pre><code>override define foo
bar
endef</code></pre>
<p>六、多行变量</p>
<p>还有一种设置变量值的方法是使用define关键字。使用define关键字设置变量的值可以有换行，这有利于定义一系列的命令（前面我们讲过“命令包”的技术就是利用这个关键字）。</p>
<p>define指示符后面跟的是变量的名字，而重起一行定义变量的值，定义是以endef关键字结束。其工作方式和“=”操作符一样。变量的值可以包含函数、命令、文字，或是其它变量。因为命令需要以[Tab]键开头，所以如果你用define定义的命令变量中没有以[Tab]键开头，那么make就不会把其认为是命令。</p>
<p>下面的这个示例展示了define的用法：</p>
<pre><code>define two-lines
echo foo
echo $(bar)
endef</code></pre>
<p>七、环境变量</p>
<p>make运行时的系统环境变量可以在make开始运行时被载入到Makefile文件中，但是如果Makefile中已定义了这个变量，或是这个变量由make命令行带入，那么系统的环境变量的值将被覆盖。（如果make指定了“-e”参数，那么，系统环境变量将覆盖Makefile中定义的变量）</p>
<p>因此，如果我们在环境变量中设置了“CFLAGS”环境变量，那么我们就可以在所有的Makefile中使用这个变量了。这对于我们使用统一的编译参数有比较大的好处。如果Makefile中定义了CFLAGS，那么则会使用Makefile中的这个变量，如果没有定义则使用系统环境变量的值，一个共性和个性的统一，很像“全局变量”和“局部变量”的特性。</p>
<p>当make嵌套调用时（参见前面的“嵌套调用”章节），上层Makefile中定义的变量会以系统环境变量的方式传递到下层的Makefile中。当然，默认情况下，只有通过命令行设置的变量会被传递。而定义在文件中的变量，如果要向下层Makefile传递，则需要使用exprot关键字来声明。（参见前面章节）</p>
<p>当然，我并不推荐把许多的变量都定义在系统环境中，这样，在我们执行不用的Makefile时，拥有的是同一套系统变量，这可能会带来更多的麻烦。</p>
<p>八、目标变量</p>
<p>前面我们所讲的在Makefile中定义的变量都是“全局变量”，在整个文件，我们都可以访问这些变量。当然，“自动化变量”除外，如“$&lt;”等这种类量的自动化变量就属于“规则型变量”，这种变量的值依赖于规则的目标和依赖目标的定义。</p>
<p>当然，我样同样可以为某个目标设置局部变量，这种变量被称为“Target-specific Variable”，它可以和“全局变量”同名，因为它的作用范围只在这条规则以及连带规则中，所以其值也只在作用范围内有效。而不会影响规则链以外的全局变量的值。</p>
<p>其语法是：</p>
<pre><code>&lt;target ...&gt;; : &lt;variable-assignment&gt;;

&lt;target ...&gt;; : overide &lt;variable-assignment&gt;;</code></pre>
<p><variable-assignment>;可以是前面讲过的各种赋值表达式，如“=”、“:=”、“+=”或是“？=”。第二个语法是针对于make命令行带入的变量，或是系统环境变量。</variable-assignment></p>
<p>这个特性非常的有用，当我们设置了这样一个变量，这个变量会作用到由这个目标所引发的所有的规则中去。如：</p>
<pre><code>prog : CFLAGS = -g
prog : prog.o foo.o bar.o
        $(CC) $(CFLAGS) prog.o foo.o bar.o

prog.o : prog.c
        $(CC) $(CFLAGS) prog.c

foo.o : foo.c
        $(CC) $(CFLAGS) foo.c

bar.o : bar.c
        $(CC) $(CFLAGS) bar.c</code></pre>
<p>在这个示例中，不管全局的$(CFLAGS)的值是什么，在prog目标，以及其所引发的所有规则中（prog.o foo.o bar.o的规则），$(CFLAGS)的值都是“-g”</p>
<p>九、模式变量</p>
<p>在GNU的make中，还支持模式变量（Pattern-specific Variable），通过上面的目标变量中，我们知道，变量可以定义在某个目标上。模式变量的好处就是，我们可以给定一种“模式”，可以把变量定义在符合这种模式的所有目标上。</p>
<p>我们知道，make的“模式”一般是至少含有一个“%”的，所以，我们可以以如下方式给所有以[.o]结尾的目标定义目标变量：</p>
<pre><code>%.o : CFLAGS = -O</code></pre>
<p>同样，模式变量的语法和“目标变量”一样：</p>
<pre><code>&lt;pattern ...&gt;; : &lt;variable-assignment&gt;;

&lt;pattern ...&gt;; : override &lt;variable-assignment&gt;;</code></pre>
<p>override同样是针对于系统环境传入的变量，或是make命令行指定的变量。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake条件判断（六）</title>
    <url>/2020/05/27/cmake%E6%9D%A1%E4%BB%B6%E5%88%A4%E6%96%AD/</url>
    <content><![CDATA[<p>使用条件判断<br>——————</p>
<p>使用条件判断，可以让make根据运行时的不同情况选择不同的执行分支。条件表达式可以是比较变量的值，或是比较变量和常量的值。</p>
<p>一、示例</p>
<p>下面的例子，判断$(CC)变量是否“gcc”，如果是的话，则使用GNU函数编译目标。</p>
<pre><code>libs_for_gcc = -lgnu
normal_libs =

foo: $(objects)
ifeq ($(CC),gcc)
        $(CC) -o foo $(objects) $(libs_for_gcc)
else
        $(CC) -o foo $(objects) $(normal_libs)
endif</code></pre>
<p>可见，在上面示例的这个规则中，目标“foo”可以根据变量“$(CC)”值来选取不同的函数库来编译程序。</p>
<p>我们可以从上面的示例中看到三个关键字：ifeq、else和endif。ifeq的意思表示条件语句的开始，并指定一个条件表达式，表达式包含两个参数，以逗号分隔，表达式以圆括号括起。else表示条件表达式为假的情况。endif表示一个条件语句的结束，任何一个条件表达式都应该以endif结束。</p>
<p>当我们的变量$(CC)值是“gcc”时，目标foo的规则是：</p>
<pre><code>foo: $(objects)
        $(CC) -o foo $(objects) $(libs_for_gcc)</code></pre>
<p>而当我们的变量$(CC)值不是“gcc”时（比如“cc”），目标foo的规则是：</p>
<pre><code>foo: $(objects)
        $(CC) -o foo $(objects) $(normal_libs)</code></pre>
<p>当然，我们还可以把上面的那个例子写得更简洁一些：</p>
<pre><code>libs_for_gcc = -lgnu
normal_libs =

ifeq ($(CC),gcc)
  libs=$(libs_for_gcc)
else
  libs=$(normal_libs)
endif

foo: $(objects)
        $(CC) -o foo $(objects) $(libs)</code></pre>
<p>二、语法</p>
<p>条件表达式的语法为：</p>
<pre><code>&lt;conditional-directive&gt;;
&lt;text-if-true&gt;;
endif</code></pre>
<p>以及：</p>
<pre><code>&lt;conditional-directive&gt;;
&lt;text-if-true&gt;;
else
&lt;text-if-false&gt;;
endif</code></pre>
<p>其中<conditional-directive>;表示条件关键字，如“ifeq”。这个关键字有四个。</conditional-directive></p>
<p>第一个是我们前面所见过的“ifeq”</p>
<pre><code>ifeq (&lt;arg1&gt;;, &lt;arg2&gt; 
ifeq &#39;&lt;arg1&gt;;&#39; &#39;&lt;arg2&gt;;&#39; 
ifeq &quot;&lt;arg1&gt;;&quot; &quot;&lt;arg2&gt;;&quot; 
ifeq &quot;&lt;arg1&gt;;&quot; &#39;&lt;arg2&gt;;&#39; 
ifeq &#39;&lt;arg1&gt;;&#39; &quot;&lt;arg2&gt;;&quot; </code></pre>
<p>比较参数“arg1”和“arg2”的值是否相同。当然，参数中我们还可以使用make的函数。如：</p>
<pre><code>ifeq ($(strip $(foo)),)
&lt;text-if-empty&gt;;
endif</code></pre>
<p>这个示例中使用了“strip”函数，如果这个函数的返回值是空（Empty），那么<text-if-empty>;就生效。</text-if-empty></p>
<p>第二个条件关键字是“ifneq”。语法是：</p>
<pre><code>ifneq (&lt;arg1&gt;;, &lt;arg2&gt; 
ifneq &#39;&lt;arg1&gt;;&#39; &#39;&lt;arg2&gt;;&#39; 
ifneq &quot;&lt;arg1&gt;;&quot; &quot;&lt;arg2&gt;;&quot; 
ifneq &quot;&lt;arg1&gt;;&quot; &#39;&lt;arg2&gt;;&#39; 
ifneq &#39;&lt;arg1&gt;;&#39; &quot;&lt;arg2&gt;;&quot; </code></pre>
<p>其比较参数“arg1”和“arg2”的值是否相同，如果不同，则为真。和“ifeq”类似。</p>
<p>第三个条件关键字是“ifdef”。语法是：</p>
<pre><code>ifdef &lt;variable-name&gt;; </code></pre>
<p>如果变量<variable-name>;的值非空，那到表达式为真。否则，表达式为假。当然，<variable-name>;同样可以是一个函数的返回值。注意，ifdef只是测试一个变量是否有值，其并不会把变量扩展到当前位置。还是来看两个例子：</variable-name></variable-name></p>
<pre><code>示例一：
bar =
foo = $(bar)
ifdef foo
frobozz = yes
else
frobozz = no
endif

示例二：
foo =
ifdef foo
frobozz = yes
else
frobozz = no
endif</code></pre>
<p>第一个例子中，“$(frobozz)”值是“yes”，第二个则是“no”。</p>
<p>第四个条件关键字是“ifndef”。其语法是：</p>
<pre><code>ifndef &lt;variable-name&gt;;</code></pre>
<p>这个我就不多说了，和“ifdef”是相反的意思。</p>
<p>在<conditional-directive>;这一行上，多余的空格是被允许的，但是不能以[Tab]键做为开始（不然就被认为是命令）。而注释符“#”同样也是安全的。“else”和“endif”也一样，只要不是以[Tab]键开始就行了。</conditional-directive></p>
<p>特别注意的是，make是在读取Makefile时就计算条件表达式的值，并根据条件表达式的值来选择语句，所以，你最好不要把自动化变量（如“$@”等）放入条件表达式中，因为自动化变量是在运行时才有的。</p>
<p>而且，为了避免混乱，make不允许把整个条件语句分成两部分放在不同的文件中。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake链接库（八）</title>
    <url>/2020/05/29/cmake%E9%93%BE%E6%8E%A5%E5%BA%93%20/</url>
    <content><![CDATA[<p>GNU开发工具——CMake模块</p>
<p>一、find_packge高级功能</p>
<h3 id="1-find-package的模式"><a href="#1-find-package的模式" class="headerlink" title="1. find_package的模式"></a>1. find_package的模式</h3><p>find_package()有Module模式(基本用法)和Config模式(完全用法），其中Module模式是基础，Config模式则提供复杂高级功能。<br>find_package是否使用Config模式可以通过下列条件判断：<br>（1）find_package()中指定CONFIG关键字<br>（2）find_package()中指定NO_MODULE关键字<br>（3）find_package()中使用了不再Module模式下所有支持配置的关键字</p>
<h3 id="2-find-package工作流程"><a href="#2-find-package工作流程" class="headerlink" title="2. find_package工作流程"></a>2. find_package工作流程</h3><p>GNU开发工具——CMake模块</p>
<h3 id="3-find-package的Module模式"><a href="#3-find-package的Module模式" class="headerlink" title="3. find_package的Module模式"></a>3. find_package的Module模式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">find_package(&lt;PackageName&gt; [version] [EXACT] [QUIET] [MODULE] [REQUIRED] [[COMPONENTS] [components...]] [OPTIONAL_COMPONENTS components...] [NO_POLICY_SCOPE])</span><br></pre></td></tr></table></figure>
<ul>
<li>version和EXACT，可选，version指定版本，如果指定就必须检查找到的包的版本是否和version兼容。如果指定EXACT则表示必须完全匹配的版本而不是兼容版本就可以。</li>
<li>QUIET，可选字段，表示如果查找失败，不会在屏幕进行输出（但如果指定了REQUIRED字段，则QUIET无效，仍然会输出查找失败提示语）。</li>
<li>MODULE，可选字段。前面提到说“如果Module模式查找失败则回退到Config模式进行查找”，但是假如设定了MODULE选项，那么就只在Module模式查找，如果Module模式下查找失败并不回落到Config模式查找。</li>
<li>REQUIRED可选字段。表示一定要找到包，找不到的话就立即停掉整个cmake。而如果不指定REQUIRED则cmake会继续执行。</li>
<li>COMPONENTS，components:可选字段，表示查找的包中必须要找到的组件(components），如果有任何一个找不到就算失败，类似于REQUIRED，导致cmake停止执行。<br>Module模式下需要查找到名为FindPackageName.cmake的文件。<br>先在CMAKE_MODULE_PATH变量对应的路径中查找。如果路径为空，或者路径中查找失败，则在cmake module directory（cmake安装时的Modules目录，比如/usr/local/share/cmake/Modules）查找。</li>
</ul>
<h3 id="4-find-package的Config模式"><a href="#4-find-package的Config模式" class="headerlink" title="4. find_package的Config模式"></a>4. find_package的Config模式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">find_package(&lt;PackageName&gt; [version] [EXACT] [QUIET]</span><br><span class="line">[REQUIRED] [[COMPONENTS] [components...]]</span><br><span class="line">[CONFIG|NO_MODULE]</span><br><span class="line">[NO_POLICY_SCOPE]</span><br><span class="line">[NAMES name1 [name2 ...]]</span><br><span class="line">[CONFIGS config1 [config2 ...]]</span><br><span class="line">[HINTS path1 [path2 ... ]]</span><br><span class="line">[PATHS path1 [path2 ... ]]</span><br><span class="line">[PATH_SUFFIXES suffix1 [suffix2 ...]]</span><br><span class="line">[NO_DEFAULT_PATH]</span><br><span class="line">[NO_PACKAGE_ROOT_PATH]</span><br><span class="line">[NO_CMAKE_PATH]</span><br><span class="line">[NO_CMAKE_ENVIRONMENT_PATH]</span><br><span class="line">[NO_SYSTEM_ENVIRONMENT_PATH]</span><br><span class="line">[NO_CMAKE_PACKAGE_REGISTRY]</span><br><span class="line">[NO_CMAKE_BUILDS_PATH] # Deprecated; does nothing.</span><br><span class="line">[NO_CMAKE_SYSTEM_PATH]</span><br><span class="line">[NO_CMAKE_SYSTEM_PACKAGE_REGISTRY]</span><br><span class="line">[CMAKE_FIND_ROOT_PATH_BOTH |</span><br><span class="line">ONLY_CMAKE_FIND_ROOT_PATH |</span><br><span class="line">NO_CMAKE_FIND_ROOT_PATH])</span><br></pre></td></tr></table></figure>
<p>Config模式下的查找顺序，比Module模式下要多得多，新版本的CMake比老版本的有更多的查找顺序（新增的在最优先的查找顺序）。Config模式下需要查找到名为lower-case-package-name-config.cmake或PackageNameConfig.cmake文件。查找顺序如下：<br>（1）名为PackageName_ROOT的cmake变量或环境变量。CMake3.12新增。设定CMP0074 Policy来关闭。如果定义了PackageName_DIR cmake变量，那么PackageName_ROOT 不起作用。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cmake_minimum_required(VERSION 3.13)</span><br><span class="line">project(OpenCVDemo)</span><br><span class="line">set(OpenCV_ROOT &quot;&#x2F;usr&#x2F;local&#x2F;lib&#x2F;opencv_249&#x2F;build&quot;)</span><br><span class="line">set(OpenCV_DIR &quot;&#x2F;usr&#x2F;local&#x2F;lib&#x2F;opencv_300&#x2F;build&quot;)</span><br><span class="line"></span><br><span class="line">find_package(OpenCV QUIET</span><br><span class="line">    NO_MODULE</span><br><span class="line">    NO_DEFAULT_PATH</span><br><span class="line">    NO_CMAKE_PATH</span><br><span class="line">    NO_CMAKE_ENVIRONMENT_PATH</span><br><span class="line">    NO_SYSTEM_ENVIRONMENT_PATH</span><br><span class="line">    NO_CMAKE_PACKAGE_REGISTRY</span><br><span class="line">    NO_CMAKE_BUILDS_PATH</span><br><span class="line">    NO_CMAKE_SYSTEM_PATH</span><br><span class="line">    NO_CMAKE_SYSTEM_PACKAGE_REGISTRY</span><br><span class="line">)</span><br><span class="line">message(STATUS &quot;OpenCV library status:&quot;)</span><br><span class="line">message(STATUS &quot;    version: $&#123;OpenCV_VERSION&#125;&quot;)</span><br><span class="line">message(STATUS &quot;    libraries: $&#123;OpenCV_LIBS&#125;&quot;)</span><br><span class="line">message(STATUS &quot;    include path: $&#123;OpenCV_INCLUDE_DIRS&#125;&quot;)</span><br></pre></td></tr></table></figure>
<p>上述代码会找到opencv300，OpenCV_DIR变量的值有效OpenCV_ROOT变量无效。<br>（2）cmake特定的缓存变量</p>
<p>CMAKE_PREFIX_PATH<br>CMAKE_FRAMEWORK_PATH<br>CMAKE_APPBUNDLE_PATH<br>可以通过设定NO_CMAKE_PATH来关闭特定缓存变量的查找顺序<br>（3）CMake特定的环境变量</p>
<p>PackageName_DIR<br>CMAKE_PREFIX_PATH<br>CMAKE_FRAMEWORK_PATH<br>CMAKE_APPBUNDLE_PATH<br>可以通过NO_CMAKE_ENVIRONMENT_PATH来跳过。<br>（4）HINT字段指定的路径<br>（5）搜索标准的系统环境变量PATH。<br>其中如果是以/bin或者/sbin结尾的，会自动转化为其父目录。<br>通过指定NO_SYSTEM_ENVIRONMENT_PATH来跳过。<br>（6）存储在CMake的”User Package Registry”(用户包注册表)中的路径。<br>通过设定NO_CMAKE_PACKAGE_REGISTRY，或设定CMAKE_FIND_PACKAGE_NO_PACKAGE_REGISTRY为true，来避开。<br>（7）设定为当前系统定义的cmake变量：<br>CMAKE_SYSTEM_PREFIX_PATH<br>CMAKE_SYSTEM_FRAMEWORK_PATH<br>CMAKE_SYSTEM_APPBUNDLE_PATH<br>通过设定NO_CMAKE_SYSTEM_PATH来跳过。<br>（8）在cmake的”System Package Registry”(系统包注册表）中查找。<br>通过设定NO_CMAKE_SYSTEM_PACKAGE_REGISTRY跳过，或者通过设定CMAKE_FIND_PACKAGE_NO_SYSTEM_PACKAGE_REGISTRY为true避开。<br>（9）从PATHS字段指定的路径中查找。</p>
<h1 id="二-CMake自定义模块"><a href="#二-CMake自定义模块" class="headerlink" title="二. CMake自定义模块"></a>二. CMake自定义模块</h1><h3 id="1-find-xxx指令"><a href="#1-find-xxx指令" class="headerlink" title="1. find_xxx指令"></a>1. find_xxx指令</h3><p>FIND_FILE(VAR name path1 path2 …)<br>VAR变量代表找到的文件全路径，包含文件名。<br>FIND_LIBRARY(VAR name path1 path2 …)<br>VAR变量代表找到的库全路径，包含库文件名。<br>FIND_PATH(VAR name path1 path2 …)<br>VAR变量代表包含文件的路径<br>FIND_PROGRAM(VAR name path1 path2 …)<br>VAR变量代表包含程序的全路径<br>FIND_PACKAGE(&lt;name&gt; [major.minor] [QUIET] [NO_MODULE] [[REQUIRED | COMPONENTS] [componets …]])<br>用来调用预定义在CMAKE_MODULE_PATH下的Findname.cmake模块，可以自己定义Findname模块，通过SET(CMAKE_MODULE_PATH dir)将其放入工程的某个目录供工程使用。</p>
<p>FIND_LIBRARY(libX X11 /usr/lib)<br>IF (NOT libx)<br>    MESSAGE(FATAL_ERROR “libX not found”)<br>ENDIF(NOT libX)<br>2、系统预定义模块使用</p>
<p>系统预定义了各种模块，通常需要使用INCLUDE指令显式的调用, 但FIND_PACKAGE指令是一个特例，可以直接调用预定义的模块。<br>每一个模块都会定义以下3个变量：</p>
<p><name>_FOUND<br><name>_INCLUDE_DIR or <name>_INCLUDES<br><name>_LIBRARY or <name>_LIBRARIES<br>因此，可以通过name_FOUND来判断模块是否被找到，如果没有找到，按照工程的需要关闭某些特性、给出提醒或者中止构建。<br>对于系统预定义的Findname.cmake模块，使用方法如下：</name></name></name></name></name></p>
<p>FIND_PACKAGE(NAME)<br>IF(NAME_FOUND)<br>    INCLUDE_DIRECTORIES(${NAME_INCLUDE_DIR})<br>    TARGET_LINK_LIBRARIES(targetname ${NAME_LIBRARY})<br>ELSE(NAME_FOUND)<br>    MESSAGE(FATAL_ERROR ”NAME library not found”)<br>ENDIF(NAME_FOUND)<br>工程实践中使用示例如下：</p>
<p>#通过<name>_FOUND来控制工程特性：<br>SET(SOURCES Viewer.cpp)<br>SET(OptionalSources)<br>SET(OptionalLibs)<br>FIND_PACKAGE(JPEG)<br>IF(JPEG_FOUND)<br>    SET(OptionalSources ${OptionalSources} JPEGView.cpp)<br>    INCLUDE_DIRECTORIES( ${JPEG_INCLUDE_DIR} )<br>    SET(optionalLibs ${OptionalLibs} ${JPEG_LIBRARIES} )<br>    ADD_DEFINITIONS(-DENABLE_JPEG_SUPPORT)<br>ENDIF(JPEG_FOUND)</name></p>
<p>IF(PNG_FOUND)<br>    SET(OptionalSources ${OptionalSources} PNGView.cpp)<br>    INCLUDE_DIRECTORIES( ${PNG_INCLUDE_DIR} )<br>    SET(OptionalLibs ${OptionalLibs} ${PNG_LIBRARIES} )<br>    ADD_DEFINITIONS(-DENABLE_PNG_SUPPORT)<br>ENDIF(PNG_FOUND)</p>
<p>ADD_EXECUTABLE(viewer ${SOURCES} ${OptionalSources} )<br>TARGET_LINK_LIBRARIES(Viewer ${OptionalLibs})<br>#通过判断系统是否提供了JPEG、PNG模块来决定程序是否支持JPEG、PNG功能。<br>3、自定义模块实现</p>
<p>本文基于libHello动态库，编写一个FindHelo.cmake模块。<br>创建一个FindHello项目目录，进入FindHello。<br>创建一个Hello目录，进入Hello目录，创建FindHelo.cmake文件，FindHelo.cmake文件如下：</p>
<p>#查找hello库头文件的安装路径<br>FIND_PATH(HELLO_INCLUDE_DIR Hello.h /usr/local/include/hello)<br>#查找hello库的安装路径<br>FIND_LIBRARY(HELLO_LIBRARY NAMES hello /usr/local/lib/libhello.so)</p>
<p>IF (HELLO_INCLUDE_DIR AND HELLO_LIBRARY)<br>    SET(HELLO_FOUND TRUE)<br>ENDIF (HELLO_INCLUDE_DIR AND HELLO_LIBRARY)</p>
<p>IF (HELLO_FOUND)<br>    IF (NOT HELLO_FIND_QUIETLY)<br>    MESSAGE(STATUS “Found Hello: ${HELLO_LIBRARY}”)<br>    ENDIF (NOT HELLO_FIND_QUIETLY)<br>ELSE (HELLO_FOUND)<br>    IF (HELLO_FIND_REQUIRED)<br>    MESSAGE(FATAL_ERROR “Could not find hello library”)<br>    ENDIF (HELLO_FIND_REQUIRED)<br>ENDIF (HELLO_FOUND)<br>返回FindHello目录，创建main.cpp文件：</p>
<p>#include &lt;Hello.h&gt;</p>
<p>int main(int argc, char* argv[])<br>{<br>    Hello hello;<br>    hello.Print();<br>    return 0;<br>}<br>创建工程CMakeLists.txt文件如下：</p>
<p>cmake_minimum_required(VERSION 2.8)<br>PROJECT(FindHello)</p>
<h1 id="设置自定义模块路径"><a href="#设置自定义模块路径" class="headerlink" title="设置自定义模块路径"></a>设置自定义模块路径</h1><p>SET(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} “${PROJECT_SOURCE_DIR}/Hello/“)<br>MESSAGE(STATUS “CMAKE_MODULE_PATH “ ${PROJECT_SOURCE_DIR}/Hello)<br>#查找自定义Hello模块<br>FIND_PACKAGE(Hello)<br>IF(HELLO_FOUND)<br>    ADD_EXECUTABLE(hello main.cpp)<br>    INCLUDE_DIRECTORIES(${HELLO_INCLUDE_DIR})<br>    MESSAGE(STATUS “LINK “ ${HELLO_LIBRARY})<br>    TARGET_LINK_LIBRARIES(hello ${HELLO_LIBRARY})<br>ELSE(HELLO_FOUND)<br>    MESSAGE(STATUS “Could not find hello library”)<br>ENDIF(HELLO_FOUND)<br>创建build目录，进行build目录进行构建。<br>cmake ../<br>make<br>./hello<br>结果打印出Hello world.</p>
<h2 id="初次使用cmake-module链接库"><a href="#初次使用cmake-module链接库" class="headerlink" title="初次使用cmake module链接库"></a>初次使用cmake module链接库</h2><p>说来惭愧，没有自己单独写过特别大型的文程序，所以一般在使用CMakelists进行编译的时候所有东西都写在一个文件了。但是我们使用过很多库应该发现他们都有一个文件夹叫cmake或者cmake_modules之类的。这类文件夹一般包含很多.cmake文件，这类文件把本来该写在CMakeLists.txt里的内容分散开来，用来找特定的库之类的。就好像我们写一个大型程序需要把很多功能分成特定的函数一样。</p>
<p>首先我有一个CMakeLists.txt文件，里面的可执 行程序需要用到一个叫libbayesopt.a的静态库，库的位置位于</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;usr&#x2F;local&#x2F;lib.</span><br></pre></td></tr></table></figure>
<p>我们使用一些有名的库的时候，他好像自己就能找到，不需要多费精神，比如OpenCv，我们只需要在CMakeLists.txt里添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">find_package(OpenCV REQUIRED)</span><br></pre></td></tr></table></figure>
<p>并把某可执行文件链接到${OpenCv_LIBS}就可以了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">add_executable(abc abc.cpp)</span><br><span class="line">target_link_libraries(abc $&#123;OpenCv_LIBS&#125;)</span><br></pre></td></tr></table></figure>
<p>可是一些不知名的库，你直接使用find_package(<lib_name> REQUIRED)是找不到库的。因为其实都不知道那么lib_name是什么。比如我那么libbayesopt.a，我在CMakeLists.txt里应该叫它Bayesopt呢还是bayesopt呢？反正试了诸多名字，都是不能直接找到的。比如说我填写的是</lib_name></p>
<p>find_pakcage(Bayesopt REQUIRED)<br>在编译CMakeLists.txt时就会出现下面的错误:</p>
<p>CMake Error at bayesopt_compass/CMakeLists.txt:20 (find_package):<br>  By not providing “FindBayesopt.cmake” in CMAKE_MODULE_PATH this project has<br>  asked CMake to find a package configuration file provided by “Bayesopt”,<br>  but CMake did not find one.</p>
<p>  Could not find a package configuration file provided by “Bayesopt” with any<br>  of the following names:</p>
<pre><code>BayesoptConfig.cmake
bayesopt-config.cmake</code></pre>
<p>  Add the installation prefix of “Bayesopt” to CMAKE_PREFIX_PATH or set<br>  “Bayesopt_DIR” to a directory containing one of the above files.  If<br>  “Bayesopt” provides a separate development package or SDK, be sure it has<br>  been installed.<br>其实不用仔细看，大概就是程序找不到Bayesopt是个什么玩意儿，想让你写一个叫BayesoptConfig.cmake或者bayesopt-config.cmake的文件来找到这个Bayesopt。那么接下来怎么做呢？</p>
<ol>
<li>在和CMakeLists.txt相同位置建立一个叫cmake_modules的文件夹，在里面创建一个叫BayesoptConfig.cmake的文件。注意根据上面error的信息我们只有两种命名选择，这也增加了规范性。</li>
<li>在BayesoptConfig.cmake中写入下面内容。<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Try to find bayesopt library</span><br><span class="line">find_library(Bayesopt_LIBRARIES NAMES bayesopt)</span><br><span class="line">find_path(Bayesopt_INCLUDE_PATH NAMES bayesopt&#x2F;bayesopt.h)</span><br></pre></td></tr></table></figure>
第一行是注释。<br>第二行要求寻找一个叫(NAMES)libbayesopt.a或者.so的库。并在CMakeLists中命名为Bayesopt_LIBRARIES。<br>可以看到我们只是指定了bayesopt，并不是指定找libbayesopt.a。这是程序默认的。任何库的名字都是lib开头。你只需要指定lib后的那串字符就可以了。</li>
</ol>
<p>第三行指定该库对应的头文件的位置，并在CMakeLists中命名为Bayesopt_INCLUDE_PATH.<br>这两个find函数会在/usr/local/lib和/usr/local/include里找相应的库，应该(注意我这儿说的应该，因为我还没自己尝试过找/usr/lib里的库)也会在/usr/lib这类文件夹里找相应的库。<br>注意有些同学在编译时命名库在usr/local/lib里电脑却找不到，这时候你得考虑一下自己添加寻找目录。打开terminal输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export LD_LIBRARY_PATH&#x3D;$LD_LIBRARY_PATH:&#x2F;usr&#x2F;local&#x2F;lib</span><br><span class="line">即可。或者要永久有效的话</span><br><span class="line"></span><br><span class="line">gedit ~&#x2F;.bashrc</span><br><span class="line">打开一个叫bashrc的文件，在里面最后面添加上面的那行代码。之后保存退出再</span><br><span class="line"></span><br><span class="line">source ~&#x2F;.bashrc</span><br><span class="line">即可。你可以这样指定电脑去任何目录寻找库文件</span><br></pre></td></tr></table></figure>
<ol start="3">
<li>在你的CMakeLists.txt中，定义好最小cmake版本需求和项目名称后就可以输入我们cmake_modules的位置<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cmake_minimum_required(VERSION 2.8.3)</span><br><span class="line">project(bayesopt_compass)</span><br><span class="line">list(APPEND CMAKE_MODULE_PATH &quot;$&#123;PROJECT_SOURCE_DIR&#125;&#x2F;cmake_modules&#x2F;&quot;)</span><br><span class="line">find_package(Bayesopt REQUIRED)</span><br><span class="line">...</span><br><span class="line">add_executable(abc abc.cpp)</span><br><span class="line">target_link_libraries(abc $&#123;Bayesopt_LIBRARIES&#125;)</span><br></pre></td></tr></table></figure>
list那一行告诉cmake在哪儿去找我们的modules。之后输入我们要找的package名称。这里正如前面所说的，我们需要把Bayesopt这个名字和FindXXX.cmake中的XXX对应起来。最后在可执行文件添加库链接的时候，那个库的名字和FindBayesopt.cmake中的find_library中定义的名字一样。(其实个人试了一下，链接到${Bayesopt_LIBRARY}也是可以的，好像不区分单复数)。可以参考一下<a href="https://blog.csdn.net/dbzhang800/article/details/6329314%E9%87%8C%E7%9A%84%E5%86%85%E5%AE%B9">https://blog.csdn.net/dbzhang800/article/details/6329314里的内容</a><br>保存CMakeLists.txt之后就能正常编译了。</li>
</ol>
<p>暴力链接库</p>
<p>如果你实在不想写.cmake文件，但是又要链接到一个你不知道find_package(<name> REQUIRED)中name是何物的库，你可以用一种很暴力的方法。<br>比如你知道你要链接的库的位置就是在/usr/local/lib/libabc.a，你可以在target_link_libraries中直接添加这个路径</name></p>
<p>cmake_minimum_required(VERSION 2.8.3)<br>project(your_project)<br>…<br>add_executable(def def.cpp)<br>target_link_libraries(def /usr/local/lib/libabc.a)<br>这种方法也是work的！不仅不用写cmake，而且连find_package这个语句都省略了，怎么样是不是简单又暴力！不过这样做的话看起来也就没那么专业了hhhh，而且以后别人如果使用你的程序来扩展也会比较麻烦，如果你写的程序只想自己或少数几个人弄懂，这也不失为一个办法。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/09/15/hello-world/</url>
    <content><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p>
<a id="more"></a>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure>

<p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>
]]></content>
  </entry>
  <entry>
    <title>kafka的基本概念与使用</title>
    <url>/2021/04/21/kafka%E7%9A%84%E5%9F%BA%E6%9C%AC%E6%A6%82%E5%BF%B5%E4%B8%8E%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[]]></content>
      <categories>
        <category>Kafka</category>
      </categories>
      <tags>
        <tag>Kafka</tag>
      </tags>
  </entry>
  <entry>
    <title>read-committed下GAP锁问题</title>
    <url>/2021/04/16/read-committed%E4%B8%8BGAP%E9%94%81%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>最近在定位一个数据库偶尔出现死锁的问题， 现象就是数据库在read-committed隔离级别下出现GAP锁，导致部分insert语句发生死锁、业务侧超时。</p>
<p>在这里记录下排查过程和结论，同时讨论以下几个问题</p>
<ul>
<li>数据库各隔离级别及GAP锁的定义</li>
<li>GAP锁为什么会出现在read-committed隔离级别下</li>
<li>从哪些角度能避免死锁问题（在当前场景下）</li>
</ul>
<h1 id="问题背景"><a href="#问题背景" class="headerlink" title="问题背景"></a>问题背景</h1><p>先讲一下背景，业务中对数据库的操作分为读、更新、插入三类， 其中因为对数据库更新某个key的特定field的value时，因为无法判断该值是否已经写入到数据库中了， 所以需要使用<code>on duplicate key</code> 语句。<br>如果想避免该语句的话，也有几种方案可以实现此功能， 这个在后面的部分来讨论，大致来说就是将一条 <code>on duplicate key</code> 拆分为<code>update</code> 和 <code>insert</code>，需要的时候再使用 insert。</p>
<h2 id="执行语句"><a href="#执行语句" class="headerlink" title="执行语句"></a>执行语句</h2><p>业务中所有的数据库插入语句都是这种格式，执行语句中具体拼接的数量可以动态配置，默认情况下同key的不同field才会进行合并，大概5-10个field合并为一个执行语句。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">insert into table (&#96;key&#96;,&#96;originalKey&#96;,&#96;field&#96;,&#96;value&#96;)</span><br><span class="line">values</span><br><span class="line">(),</span><br><span class="line">...</span><br><span class="line">ON DUPLICATE KEY UPDATE &#96;value&#96; &#x3D; VALUES(value), &#96;expire&#96;&#x3D; VALUES(expire), &#96;modifyTime&#96; &#x3D; NOW()</span><br></pre></td></tr></table></figure>
<h3 id="为什么不用replace？"><a href="#为什么不用replace？" class="headerlink" title="为什么不用replace？"></a>为什么不用replace？</h3><p><code>on duplicate key</code> 与  <code>replace</code>的性能差异(有时间了再补充相关文档)<br><code>replace</code>在检测的key冲突是执行的具体语句为 <code>delete</code> + <code>insert</code>，该操作将额外进行两次索引操作。</p>
<h2 id="建表语句"><a href="#建表语句" class="headerlink" title="建表语句"></a>建表语句</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CREATE DATABASE &#96;profile_storage&#96; &#x2F;*!40100 DEFAULT CHARACTER SET utf8mb4 *&#x2F;;</span><br><span class="line">CREATE TABLE &#96;storage_cluster&#96; (</span><br><span class="line">  &#96;id&#96; bigint(20) NOT NULL AUTO_INCREMENT,</span><br><span class="line">  &#96;key&#96; varchar(128) COLLATE utf8mb4_bin NOT NULL COMMENT &#39;存储key&#39;,</span><br><span class="line">  &#96;originalKey&#96; varchar(255) COLLATE utf8mb4_bin NOT NULL COMMENT &#39;原始key&#39;,</span><br><span class="line">  &#96;field&#96; varchar(128) COLLATE utf8mb4_bin NOT NULL COMMENT &#39;存储列名&#39;,</span><br><span class="line">  &#96;value&#96; blob NOT NULL COMMENT &#39;value, json&#39;,</span><br><span class="line">  &#96;expire&#96; bigint(20) NOT NULL COMMENT &#39;写入时的有效时间，清理结合modifyTime+expire即可&#39;,</span><br><span class="line">  &#96;modifyTime&#96; timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,</span><br><span class="line">  PRIMARY KEY (&#96;id&#96;),</span><br><span class="line">  UNIQUE KEY &#96;key_field&#96; (&#96;key&#96;,&#96;field&#96;),</span><br><span class="line">  KEY &#96;modify_time&#96; (&#96;modifyTime&#96;)</span><br><span class="line">) ENGINE&#x3D;InnoDB DEFAULT CHARSET&#x3D;utf8mb4 COLLATE&#x3D;utf8mb4_bin COMMENT&#x3D;&#39;画像数据存储&#39;;</span><br></pre></td></tr></table></figure>
<h2 id="隔离级别"><a href="#隔离级别" class="headerlink" title="隔离级别"></a>隔离级别</h2><p>tx_isolation = READ-COMMITTED</p>
<h1 id="排查过程"><a href="#排查过程" class="headerlink" title="排查过程"></a>排查过程</h1><h2 id="使用腾讯云DBbrain"><a href="#使用腾讯云DBbrain" class="headerlink" title="使用腾讯云DBbrain"></a>使用腾讯云DBbrain</h2><p>我们使用的腾讯云的MySQL，腾讯云提供的数据库智能管家DBbrain可以很方便的查看当前和历史时刻的数据库状态。刚好这两天发生了一次deadlock，通过这个截图可以看下DBbrain的总览。<br><img src="/2021/04/16/read-committed%E4%B8%8BGAP%E9%94%81%E9%97%AE%E9%A2%98/DBbrain_deadlock.png"></p>
<p>当点击死锁字段时会自动跳转到错误日志界面（包含事件详情和死锁快照）， 下面是死锁快照部分的具体日志</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">------------------------</span><br><span class="line">2021-04-18 11:18:47 0x7ff915060700</span><br><span class="line">*** (1) TRANSACTION:</span><br><span class="line">TRANSACTION 101037787573, ACTIVE 0 sec inserting</span><br><span class="line">mysql tables in use 1, locked 1</span><br><span class="line">LOCK WAIT 2 lock struct(s), heap size 1136, 1 row lock(s), undo log entries 2</span><br><span class="line">MySQL thread id 3527624187, OS thread handle 140694797604608, query id 72846449145 10.0.129.132 smonline update</span><br><span class="line">INSERT INTO &#96;storage_cluster&#96; ( &#96;id&#96;,&#96;key&#96;,&#96;originalKey&#96;,&#96;field&#96;,&#96;value&#96;,&#96;expire&#96; ) VALUES ( &#39;0&#39;, &#39;15de83fc9a9f9834d8f4d7bfa9ff4713&#39;, &#39;profile:smid:2020091521544905a3711528ba4a2e159d6f2230360a5001b6047b1f8904e4&#39;, &#39;b_device_manufacturer&#39;, &#39;\&quot;xiaomi\&quot;&#39;, 15552000 ) , ( &#39;0&#39;, &#39;15de83fc9a9f9834d8f4d7bfa9ff4713&#39;, &#39;profile:smid:2020091521544905a3711528ba4a2e159d6f2230360a5001b6047b1f8904e4&#39;, &#39;device_score&#39;, &#39;199&#39;, 15552000 ) , ( &#39;0&#39;, &#39;15de83fc9a9f9834d8f4d7bfa9ff4713&#39;, &#39;profile:smid:2020091521544905a3711528ba4a2e159d6f2230360a5001b6047b1f8904e4&#39;, &#39;last_active_time&#39;, &#39;1618715927103&#39;, 15552000 ) ON DUPLICATE KEY UPDATE &#96;value&#96; &#x3D; VALUES(value), &#96;expire&#96;&#x3D; VALUES(expire), &#96;modifyTime&#96; &#x3D; NOW()</span><br><span class="line">*** (1) WAITING FOR THIS LOCK TO BE GRANTED:</span><br><span class="line">RECORD LOCKS space id 43 page no 27774151 n bits 328 index key_field of table &#96;profile_storage&#96;.&#96;storage_cluster&#96; trx id 101037787573 lock_mode X waiting</span><br><span class="line">Record lock, heap no 2 PHYSICAL RECORD: n_fields 3; compact format; info bits 0</span><br><span class="line"> 0: len 30; hex 313564653833666339613966393833346438663464376266613966663437; asc 15de83fc9a9f9834d8f4d7bfa9ff47; (total 32 bytes);</span><br><span class="line"> 1: len 12; hex 6465766963655f73636f7265; asc device_score;;</span><br><span class="line"> 2: len 8; hex 8000000e95317813; asc      1x ;;</span><br><span class="line"></span><br><span class="line">*** (2) TRANSACTION:</span><br><span class="line">TRANSACTION 101037787572, ACTIVE 0 sec inserting</span><br><span class="line">mysql tables in use 1, locked 1</span><br><span class="line">4 lock struct(s), heap size 1136, 3 row lock(s), undo log entries 1</span><br><span class="line">MySQL thread id 3527607500, OS thread handle 140707776300800, query id 72846449143 10.0.129.31 smonline update</span><br><span class="line">INSERT INTO &#96;storage_cluster&#96; ( &#96;id&#96;,&#96;key&#96;,&#96;originalKey&#96;,&#96;field&#96;,&#96;value&#96;,&#96;expire&#96; ) VALUES ( &#39;0&#39;, &#39;15de83fc9a9f9834d8f4d7bfa9ff4713&#39;, &#39;profile:smid:2020091521544905a3711528ba4a2e159d6f2230360a5001b6047b1f8904e4&#39;, &#39;bssid&#39;, &#39;\&quot;020000000000\&quot;&#39;, 172800 ) ON DUPLICATE KEY UPDATE &#96;value&#96; &#x3D; VALUES(value), &#96;expire&#96;&#x3D; VALUES(expire), &#96;modifyTime&#96; &#x3D; NOW()</span><br><span class="line">*** (2) HOLDS THE LOCK(S):</span><br><span class="line">RECORD LOCKS space id 43 page no 27774151 n bits 328 index key_field of table &#96;profile_storage&#96;.&#96;storage_cluster&#96; trx id 101037787572 lock_mode X</span><br><span class="line">Record lock, heap no 2 PHYSICAL RECORD: n_fields 3; compact format; info bits 0</span><br><span class="line"> 0: len 30; hex 313564653833666339613966393833346438663464376266613966663437; asc 15de83fc9a9f9834d8f4d7bfa9ff47; (total 32 bytes);</span><br><span class="line"> 1: len 12; hex 6465766963655f73636f7265; asc device_score;;</span><br><span class="line"> 2: len 8; hex 8000000e95317813; asc      1x ;;</span><br><span class="line"></span><br><span class="line">*** (2) WAITING FOR THIS LOCK TO BE GRANTED:</span><br><span class="line">RECORD LOCKS space id 43 page no 27774151 n bits 328 index key_field of table &#96;profile_storage&#96;.&#96;storage_cluster&#96; trx id 101037787572 lock_mode X locks gap before rec insert intention waiting</span><br><span class="line">Record lock, heap no 2 PHYSICAL RECORD: n_fields 3; compact format; info bits 0</span><br><span class="line"> 0: len 30; hex 313564653833666339613966393833346438663464376266613966663437; asc 15de83fc9a9f9834d8f4d7bfa9ff47; (total 32 bytes);</span><br><span class="line"> 1: len 12; hex 6465766963655f73636f7265; asc device_score;;</span><br><span class="line"> 2: len 8; hex 8000000e95317813; asc      1x ;;</span><br><span class="line"></span><br><span class="line">*** WE ROLL BACK TRANSACTION (1)</span><br></pre></td></tr></table></figure>
<h2 id="使用MySQL日志"><a href="#使用MySQL日志" class="headerlink" title="使用MySQL日志"></a>使用MySQL日志</h2><p>这部分内容同样可以通过MySQL本身的错误日志查看<a href="https://dev.mysql.com/doc/refman/5.7/en/error-log.html">MySQL - The Error Log</a>）</p>
<h1 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h1><p>通过日志描述可以清楚定位发生死锁的两条语句和死锁详情</p>
<h2 id="为什么出现并发？"><a href="#为什么出现并发？" class="headerlink" title="为什么出现并发？"></a>为什么出现并发？</h2><p>业务中该数据库实例仅由画像引擎使用，画像引擎向上层的业务模块提供实体标签的查询和记录。 由于上层不同模块同时对同一个实体进行写操作，导致画像引擎部分发生 同  <code>UNIQUE KEY</code>的并发<code>INSERT</code> 操作</p>
<h2 id="出现的是什么锁？"><a href="#出现的是什么锁？" class="headerlink" title="出现的是什么锁？"></a>出现的是什么锁？</h2><p>死锁快照的最后一部分有这样一段描述</p>
<blockquote>
<p> <code>trx id 101037787572 lock_mode X locks gap before rec insert intention waiting</code></p>
</blockquote>
<p>引用1 中有对该错误出现原因的详细分析，文档中有对该错误的定义<a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-locking.html#innodb-insert-intention-locks">Insert Intention Locks</a></p>
<blockquote>
<p>An insert intention lock is a type of gap lock set by INSERT operations prior to row insertion</p>
</blockquote>
<p>可以看到Insert Intention Locks属于Gap锁的一种，但是MySQL官方文档中对gap-locks定义<a href="https://dev.mysql.com/doc/refman/8.0/en/innodb-locking.html#innodb-gap-locks">innodb-gap-locks</a>中有说明RC级别可以禁用Gap锁，那为什么还会出现这个问题呢。</p>
<blockquote>
<p> Gap locking can be disabled explicitly. This occurs if you change the transaction isolation level to READ COMMITTED. Under these circumstances, gap locking is disabled for searches and index scans and is used only for foreign-key constraint checking and duplicate-key checking.</p>
</blockquote>
<h2 id="为什么会现Gap锁"><a href="#为什么会现Gap锁" class="headerlink" title="为什么会现Gap锁"></a>为什么会现Gap锁</h2><blockquote>
<p> lock_mode X locks gap before rec insert intention waiting</p>
</blockquote>
<ul>
<li><p>假设一个存在如下这样一个数据前提（具体信息可以在引用1中查看），当我们执行事务1按照order_type=2条件更新，因此先使用索引idx_order_type定位到order_type=2的记录并加锁(ROW LOCK)，再根据二级索引上包含的主键索引值找到表上order_id=2和order_id=4的记录并加锁(ROW LOCK)，加锁如下：<br><img src="/2021/04/16/read-committed%E4%B8%8BGAP%E9%94%81%E9%97%AE%E9%A2%98/ROW_LOCK.png"></p>
</li>
<li><p>当我们同时执行事务2按照order_id=3条件更新，先根据主键定位到order_id=3并加锁(ROW LOCK)，然后根据主键中数据(order_type=1+ order_id=3)到索引idx_order_type上找到满足条件的记录并加锁，UPDATE操作将数据(1,3)更新为(2,3)，因此会将索引idx_order_type上记录(1,3)标记为删除，然后在记录(2,2)和(2,4)之间插入新记录(2,3),在插入记录前，为防止其他事务在该物理位置上插入其他数据，需要先在索引idx_order_type上申请记录(2,2)和(2,4)之间插入意向锁(Insert Intention Gap Lock),其加锁如下：<br><img src="/2021/04/16/read-committed%E4%B8%8BGAP%E9%94%81%E9%97%AE%E9%A2%98/Insert_intention_Gap_Lock.png"></p>
</li>
<li><p>而由于索引记录(2,4)上已被事务1加锁（X LOCK+ ROW LOCK）,因此导致加插入意向锁(Insert Intention Gap Lock)被阻塞，处于“lock_mode X locks gap before rec insert intention waiting”的等待状态。</p>
</li>
</ul>
<h3 id="RR及以上隔离级别为什么会出现-加锁策略"><a href="#RR及以上隔离级别为什么会出现-加锁策略" class="headerlink" title="RR及以上隔离级别为什么会出现 - 加锁策略"></a>RR及以上隔离级别为什么会出现 - 加锁策略</h3><p>对于这个错误的具体定义是：在事务插入数据过程中，为防止其他事务向索引上该位置插入数据，会在插入之前先申请插入意向范围锁，而如果申请插入意向范围锁被阻塞，则事务处于gap before rec insert intention waiting的等待状态。这篇文章中很形象的介绍了MySQL中的各类锁：<a href="https://cloud.tencent.com/developer/article/1798075">说说你是怎么解决MySQL死锁的</a></p>
<p>我们先看下 官方定义中insert 语句的加锁策略：</p>
<blockquote>
<p>“INSERT sets an exclusive lock on the inserted row. This lock is an index-record lock, not a next-key lock (that is, there is no gap lock) and does not prevent other sessions from inserting into the gap before the inserted row.<br>Prior to inserting the row, a type of gap lock called an insert intention gap lock is set. This lock signals the intent to insert in such a way that multiple transactions inserting into the same index gap need not wait for each other if they are not inserting at the same position within the gap. “</p>
</blockquote>
<p>我们知道事务执行insert的时候会申请一把插入意向锁(Insert Intention Lock)。在多个会话并发写入不同数据记录至同一索引间隙的时候，并不需要等待其他事务完成，不会发生锁等待。<br>假设有一个索引记录包含键值4和7，不同的会话分别插入5和6，每个事务都会产生一个加在4-7之间的插入意向锁，获取在插入行上的排它锁，但是不会被互相锁住，因为数据行并不冲突。</p>
<h3 id="当前情况为什么会出现-补充说明"><a href="#当前情况为什么会出现-补充说明" class="headerlink" title="当前情况为什么会出现 - 补充说明"></a>当前情况为什么会出现 - 补充说明</h3><p> 但是如果遇到唯一键呢？<br>( Under these circumstances指的就是 transaction isolation level READ COMMITTED) </p>
<blockquote>
<p> Under these circumstances, gap locking is disabled for searches and index scans and is used only for foreign-key constraint checking and duplicate-key checking.</p>
</blockquote>
<p>同时insert加锁策略中也有描述</p>
<blockquote>
<p>If a duplicate-key error occurs, a shared lock on the duplicate index record is set. This use of a shared lock can result in deadlock should there be multiple sessions trying to insert the same row if another session already has an exclusive lock.</p>
</blockquote>
<p>对于insert操作来说，若发生唯一约束冲突，则需要对冲突的唯一索引加上S Next-key Lock。从这里会发现，即使是RC事务隔离级别，也同样会存在Next-Key Lock锁，从而阻塞并发。然而，文档没有说明的是，对于检测到冲突的唯一索引，等待线程在获得S Lock之后，还需要对下一个记录进行加锁，在源码中由函数row_ins_scan_sec_index_for_duplicate进行判断。</p>
<h2 id="普通索引和唯一索引的区别"><a href="#普通索引和唯一索引的区别" class="headerlink" title="普通索引和唯一索引的区别"></a>普通索引和唯一索引的区别</h2><p>为什么在RC级别下Gap会与唯一索引有关呢？ 唯一索引有普通索引有什么区别？ 能否通过将唯一索引替换为普通索引来解决此问题？<br>// TODO </p>
<h1 id="结论"><a href="#结论" class="headerlink" title="结论"></a>结论</h1><p>虽然Gap锁只作用在隔离级别为RR及以上的数据库上， 但是不意味着隔离级别为RC 级别的不会使用。 RC级别在进行外键约束检测和唯一键约束检测的时候，会使用到Gap锁， 而正是这个 duplicate-key checking 导致了上文出现的死锁发生。</p>
<h2 id="优化方法"><a href="#优化方法" class="headerlink" title="优化方法"></a>优化方法</h2><h3 id="拆分执行语句-减小合并记录数量"><a href="#拆分执行语句-减小合并记录数量" class="headerlink" title="拆分执行语句 - 减小合并记录数量"></a>拆分执行语句 - 减小合并记录数量</h3><h3 id="拆分执行语句-update-insert"><a href="#拆分执行语句-update-insert" class="headerlink" title="拆分执行语句 - update + insert"></a>拆分执行语句 - update + insert</h3><h1 id="同类问题和相关文档"><a href="#同类问题和相关文档" class="headerlink" title="同类问题和相关文档"></a>同类问题和相关文档</h1><p>这里记录一下排查问题过程中浏览到的一些文档，其实官方文档永远是做好的参考手册，笔记中的引用也尽量去找对应的官方说明了，但是在没有找到用好官方文档的技巧之前，大佬翻译和图解过的内容看起来会简单一些。</p>
<ol>
<li><a href="https://www.cnblogs.com/gaogao67/p/11042853.html">MySQL Lock–gap before rec insert intention waiting</a></li>
<li><a href="https://www.cnblogs.com/crazylqy/p/7689447.html">初步理解MySQL的gap锁</a></li>
<li><a href="http://www.kokojia.com/article/42839.html">数据库：MySQL 普通索引和唯一索引的区别</a></li>
<li><a href="https://www.cnblogs.com/zhjh256/p/6051839.html">mysql 5.6 read-committed隔离级别下并发插入唯一索引导致死锁一例（GAP锁的问题）</a></li>
<li><a href="https://www.cnblogs.com/LBSer/p/5183300.html">mysql死锁问题分析</a></li>
</ol>
]]></content>
      <categories>
        <category>MySQL</category>
      </categories>
      <tags>
        <tag>GAP Deadlock</tag>
      </tags>
  </entry>
  <entry>
    <title>travel</title>
    <url>/2020/09/15/travel/</url>
    <content><![CDATA[<p><img src="/2020/09/15/travel/082.jpeg"></p>
]]></content>
      <tags>
        <tag>about</tag>
      </tags>
  </entry>
  <entry>
    <title>vcpkg使用示例</title>
    <url>/2021/04/10/vcpkg%E4%BD%BF%E7%94%A8%E7%A4%BA%E4%BE%8B/</url>
    <content><![CDATA[<p>尝试一次之后： 泪流满面， 后悔自己昨天还在熬夜编译一个第三方库文件</p>
<h3 id="官方文档"><a href="#官方文档" class="headerlink" title="官方文档"></a>官方文档</h3><ul>
<li><a href="https://docs.microsoft.com/zh-cn/cpp/build/vcpkg?view=msvc-160">Microsoft vcpkg：适用于C++ 的包管理器</a></li>
<li><a href="https://github.com/Microsoft/vcpkg">github microsoft/vcpkg</a></li>
<li><a href="https://github.com/microsoft/vcpkg/blob/master/README_zh_CN.md">vcpkg/README_zh_CN.md</a></li>
<li><a href="https://github.com/microsoft/vcpkg/blob/master/docs/examples/installing-and-using-packages.md">vcpkg/docs/examples/installing-and-using-packages</a></li>
</ul>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p>示例项目中依赖nlohmann_json、 gflags两个第三方库文件，其中</p>
<ul>
<li>nlohmann_json 仅需要导入头文件</li>
<li>gflags 需要引入头文件，同时链接动态库文件<br>该项目主要功能为通过gflags功能读取配置文件中的参数，将参数值放入json对象中，并打印到屏幕<br><a href="https://github.com/MoonlitAlley/vc_test">项目链接</a></li>
</ul>
<h4 id="目录结构"><a href="#目录结构" class="headerlink" title="目录结构"></a>目录结构</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">» tree -L 1</span><br><span class="line">.</span><br><span class="line">├── CMakeLists.txt</span><br><span class="line">├── flags.conf</span><br><span class="line">├── main.cpp</span><br><span class="line">└── vcpkg</span><br><span class="line"></span><br><span class="line">2 directories, 3 files</span><br></pre></td></tr></table></figure>

<p>cat flags.conf</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">--storage_config&#x3D;test_storage</span><br></pre></td></tr></table></figure>
<h4 id="如何编写CMakeLists-txt"><a href="#如何编写CMakeLists-txt" class="headerlink" title="如何编写CMakeLists.txt"></a>如何编写CMakeLists.txt</h4><blockquote>
<p>在CMakeLists.txt中直接设置CMAKE_TOOLCHAIN_FILE，此种方式可无需设置 CMAKE_TOOLCHAIN_FILE 即可使用vcpkg，且更容易完成配置工作。<br>文件中的find_package与target_link_libraries均有vcpkg直接提供，用户只需要复制到自己的CMakeLists.txt文件中即可使用</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;&gt; cat CMakeLists.txt</span><br><span class="line">cmake_minimum_required(VERSION 3.17)</span><br><span class="line">set(CMAKE_TOOLCHAIN_FILE $&#123;CMAKE_CURRENT_SOURCE_DIR&#125;&#x2F;vcpkg&#x2F;scripts&#x2F;buildsystems&#x2F;vcpkg.cmake</span><br><span class="line">        CACHE STRING &quot;Vcpkg toolchain file&quot;)</span><br><span class="line"></span><br><span class="line">project(vc_test)</span><br><span class="line">set(CMAKE_CXX_STANDARD 14)</span><br><span class="line"></span><br><span class="line">find_package(nlohmann_json CONFIG REQUIRED)</span><br><span class="line">find_package(gflags CONFIG REQUIRED)</span><br><span class="line"></span><br><span class="line">add_executable(vc_test main.cpp)</span><br><span class="line">target_link_libraries(vc_test PRIVATE nlohmann_json nlohmann_json::nlohmann_json)</span><br><span class="line">target_link_libraries(vc_test PRIVATE gflags_static)</span><br></pre></td></tr></table></figure>


<p>源代码文件如下（gflags用法可以参考官方库文档）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;&gt; cat main.cpp</span><br><span class="line">#include &lt;iostream&gt;</span><br><span class="line">#include &quot;nlohmann&#x2F;json.hpp&quot;</span><br><span class="line">#include &quot;gflags&#x2F;gflags.h&quot;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; DECLARE_string(storage_config);</span><br><span class="line">DEFINE_string(storage_config, &quot;test_default&quot;, &quot;storage_config&quot;);</span><br><span class="line"></span><br><span class="line">int main(int argc, char** argv) &#123;</span><br><span class="line">    nlohmann::json json_value &#x3D; nlohmann::json::object();</span><br><span class="line">    std::cout &lt;&lt; argv &lt;&lt; std::endl;</span><br><span class="line">    gflags::ParseCommandLineFlags(&amp;argc, &amp;argv, true);</span><br><span class="line"></span><br><span class="line">    json_value[FLAGS_storage_config] &#x3D; &quot;get_any_storage&quot;;</span><br><span class="line">    json_value[&quot;test_key&quot;] &#x3D; &quot;test_value&quot;;</span><br><span class="line">    std::cout &lt;&lt; json_value.dump() &lt;&lt; std::endl;</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>

<h4 id="执行及输出结果"><a href="#执行及输出结果" class="headerlink" title="执行及输出结果"></a>执行及输出结果</h4><p>项目执行需设置flags参数路径<br><img src="/2021/04/10/vcpkg%E4%BD%BF%E7%94%A8%E7%A4%BA%E4%BE%8B/vc_test_build.png"><br>执行结果</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;Users&#x2F;momboguo&#x2F;Downloads&#x2F;workspace&#x2F;ishumei&#x2F;arch&#x2F;vc-test&#x2F;cmake-build-debug&#x2F;vc_test -flagfile&#x3D;..&#x2F;flags.conf</span><br><span class="line">0x7ffeebd27a48</span><br><span class="line">&#123;&quot;test_key&quot;:&quot;test_value&quot;,&quot;test_storage&quot;:&quot;get_any_storage&quot;&#125;</span><br><span class="line"></span><br><span class="line">Process finished with exit code 0</span><br></pre></td></tr></table></figure>


<h3 id="vcpkg介绍"><a href="#vcpkg介绍" class="headerlink" title="vcpkg介绍"></a>vcpkg介绍</h3><h4 id="在-macOS-上复制和设置-vcpkg"><a href="#在-macOS-上复制和设置-vcpkg" class="headerlink" title="在 macOS 上复制和设置 vcpkg"></a>在 macOS 上复制和设置 vcpkg</h4><ol>
<li>在“终端”窗口中，为 vcpkg 的克隆实例创建目录。 如果打算为不同的生成目标安装库，最好在目录名称中包含目标。 建议使用短路径名称（不含空格）（如 ./macos 或 ./iot），否则，某些端口生成系统可能会出现路径问题 。 在“终端”窗口中，切换到刚刚创建的目录。</li>
<li>从 GitHub 克隆 vcpkg 存储库：<a href="https://github.com/Microsoft/vcpkg%E3%80%82">https://github.com/Microsoft/vcpkg。</a><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;microsoft&#x2F;vcpkg</span><br></pre></td></tr></table></figure>
 此命令在 vcpkg 子目录中创建存储库的本地副本。 此位置是此 vcpkg 克隆的 vcpkg 根目录。</li>
<li>接下来，切换到 vcpkg 根目录，并运行 vcpkg 引导程序命令：<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.&#x2F;bootstrap-vcpkg.sh</span><br></pre></td></tr></table></figure>
 引导程序将使用编译器、工具和标准库的位置配置 vcpkg。</li>
</ol>
<h4 id="CLion-中使用-vcpkg"><a href="#CLion-中使用-vcpkg" class="headerlink" title="CLion 中使用 vcpkg"></a>CLion 中使用 vcpkg</h4><p>打开 Toolchains 设置 (File &gt; Settings on Windows and Linux, CLion &gt; Preferences on macOS)， 并打开 CMake 设置 (Build, Execution, Deployment &gt; CMake)。 最后在 CMake options 中添加以下行:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-DCMAKE_TOOLCHAIN_FILE&#x3D;[vcpkg root]&#x2F;scripts&#x2F;buildsystems&#x2F;vcpkg.cmake</span><br></pre></td></tr></table></figure>
<p>遗憾的是，您必须手动将此选项加入每个项目配置文件中。</p>
<h4 id="将-vcpkg-作为一个子模块"><a href="#将-vcpkg-作为一个子模块" class="headerlink" title="将 vcpkg 作为一个子模块"></a>将 vcpkg 作为一个子模块</h4><p>当您希望将vcpkg作为一个子模块加入到您的工程中时， 您可以在第一个 project() 调用之前将以下内容添加到 CMakeLists.txt 中， 而无需将 CMAKE_TOOLCHAIN_FILE 传递给cmake调用。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set(CMAKE_TOOLCHAIN_FILE $&#123;CMAKE_CURRENT_SOURCE_DIR&#125;&#x2F;vcpkg&#x2F;scripts&#x2F;buildsystems&#x2F;vcpkg.cmake CACHE STRING &quot;Vcpkg toolchain file&quot;)</span><br></pre></td></tr></table></figure>
<p>使用此种方式可无需设置 CMAKE_TOOLCHAIN_FILE 即可使用vcpkg，且更容易完成配置工作。</p>
<h4 id="安装第三方库"><a href="#安装第三方库" class="headerlink" title="安装第三方库"></a>安装第三方库</h4><p>使用vcpkg安装json、gflags库（傻瓜式操作）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vcpkg(master) » .&#x2F;vcpkg install nlohmann-json</span><br><span class="line">Computing installation plan...</span><br><span class="line">The following packages are already installed:</span><br><span class="line">    nlohmann-json[core]:x64-osx -&gt; 3.9.1</span><br><span class="line">Package nlohmann-json:x64-osx is already installed</span><br><span class="line"></span><br><span class="line">Total elapsed time: 778 us</span><br><span class="line"></span><br><span class="line">The package nlohmann-json:x64-osx provides CMake targets:</span><br><span class="line"></span><br><span class="line">    find_package(nlohmann_json CONFIG REQUIRED)</span><br><span class="line">    target_link_libraries(main PRIVATE nlohmann_json nlohmann_json::nlohmann_json)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">vcpkg(master) » .&#x2F;vcpkg install gflags</span><br><span class="line">Computing installation plan...</span><br><span class="line">The following packages are already installed:</span><br><span class="line">    gflags[core]:x64-osx -&gt; 2.2.2-1</span><br><span class="line">Package gflags:x64-osx is already installed</span><br><span class="line"></span><br><span class="line">Total elapsed time: 367 us</span><br><span class="line"></span><br><span class="line">The package gflags:x64-osx provides CMake targets:</span><br><span class="line"></span><br><span class="line">    find_package(gflags CONFIG REQUIRED)</span><br><span class="line">    target_link_libraries(main PRIVATE gflags_static gflags::gflags_static)</span><br></pre></td></tr></table></figure>

<h4 id="目录结构-1"><a href="#目录结构-1" class="headerlink" title="目录结构"></a>目录结构</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vcpkg(master) » tree -L 1</span><br><span class="line">.</span><br><span class="line">├── CHANGELOG.md</span><br><span class="line">├── CONTRIBUTING.md</span><br><span class="line">├── LICENSE.txt</span><br><span class="line">├── NOTICE.txt</span><br><span class="line">├── README.md</span><br><span class="line">├── README_es.md</span><br><span class="line">├── README_fr.md</span><br><span class="line">├── README_ko_KR.md</span><br><span class="line">├── README_zh_CN.md</span><br><span class="line">├── bootstrap-vcpkg.bat</span><br><span class="line">├── bootstrap-vcpkg.sh</span><br><span class="line">├── buildtrees</span><br><span class="line">├── docs</span><br><span class="line">├── downloads</span><br><span class="line">├── installed</span><br><span class="line">├── packages</span><br><span class="line">├── ports</span><br><span class="line">├── scripts</span><br><span class="line">├── toolsrc</span><br><span class="line">├── triplets</span><br><span class="line">├── vcpkg</span><br><span class="line">└── versions</span><br><span class="line"></span><br><span class="line">10 directories, 12 files</span><br></pre></td></tr></table></figure>

<h4 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h4><p>搜索</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vcpkg(master) » .&#x2F;vcpkg search json</span><br><span class="line">arrow[json]                           JSON file support</span><br><span class="line">bitserializer        0.10             Core part of C++ 17 library for serialization to JSON, XML, YAML</span><br><span class="line">bitserializer[cpprestjson-archive]    Module for support JSON (implementation based on the CppRestSDK library)</span><br><span class="line">bitserializer[pugixml-archive]        Module for support XML (implementation based on the PugiXml library)</span><br><span class="line">bitserializer[rapidjson-archive]      Module for support JSON (implementation based on the RapidJson library)</span><br><span class="line">bitserializer[rapidyaml-archive]      Module for support YAML (implementation based on the RapidYaml library)</span><br><span class="line">bitserializer-cpp... alias            Deprecated alias for bitserializer-cpprestjson</span><br><span class="line">bitserializer-rap... alias            Deprecated alias for bitserializer-rapidjson</span><br><span class="line">boost-json           1.75.0           Boost json module</span><br><span class="line">cereal               1.3.0            a header-only C++11 serialization library (built in support for binary, XML an...</span><br><span class="line">cjson                2019-11-30-1     Ultralightweight JSON parser in ANSI C</span><br><span class="line">cjson[utils]                          Enable building the cJSON_Utils library</span><br><span class="line">cpprestsdk           2.10.17          C++11 JSON, REST, and OAuth library</span><br><span class="line">cpprestsdk[brotli]                    Brotli compression support</span><br><span class="line">cpprestsdk[compression]               HTTP Compression support</span><br><span class="line">cpprestsdk[default-features]          Features installed by default</span><br><span class="line">cpprestsdk[websockets]                Websockets support</span><br><span class="line">jansson              2.13.1           Jansson is a C library for encoding, decoding and manipulating JSON data</span><br><span class="line">jsmn                 2019-04-27       A minimalistic JSON parser in C.</span><br><span class="line">json-c               2019-09-10#1     A JSON implementation in C</span><br><span class="line">json-dto             0.2.11           A small header-only library for converting data between json representation an...</span><br><span class="line">json-schema-valid... 2.1.0            This is a C++ library for validating JSON documents based on a JSON Schema. Th...</span><br><span class="line">json-spirit          4.1.0-1          json parser using boost library</span><br><span class="line">json11               2017-06-20-2     json11 is a tiny JSON library for C++11, providing JSON parsing and serializat...</span><br><span class="line">json5-parser         1.0.0#2          An enhancement of the JSON Spirit C++ library to understand json5.</span><br><span class="line">jsoncons             0.159.0          A C++, header-only library for constructing JSON and JSON-like text and binary...</span><br><span class="line">jsoncpp              1.9.4            jsoncpp is an implementation of a JSON reader and writer in C++. JSON (JavaScr...</span><br><span class="line">jsonnet              0.16.0#1         Jsonnet - The data templating language</span><br><span class="line">jwt-cpp              0.4.0            A header only library for creating and validating json web tokens in c++</span><br><span class="line">minitrace            2019.02.06       Simple C&#x2F;C++ library for producing JSON traces suitable for Chrome&#39;s built-in ...</span><br><span class="line">msgpack              3.3.0            MessagePack is an efficient binary serialization format, which lets you exchan...</span><br><span class="line">msgpack11            0.0.10-1         msgpack11 is a tiny MsgPack library for C++11, providing MsgPack parsing and s...</span><br><span class="line">nlohmann-json        3.9.1            JSON for Modern C++</span><br><span class="line">parson               2020-09-14       a lighweight json library written in C</span><br><span class="line">picojson             1.3.0-1          A header-file-only, JSON parser serializer in C++.</span><br><span class="line">rapidjson            2020-09-14       A fast JSON parser&#x2F;generator for C++ with both SAX&#x2F;DOM style API &lt;http:&#x2F;&#x2F;rapid...</span><br><span class="line">sajson               2018-09-21       Lightweight, extremely high-performance JSON parser for C++11</span><br><span class="line">simdjson             0.7.1            A extremely fast JSON library that can parse gigabytes of JSON per second</span><br><span class="line">sqlcipher[json1]                      enable JSON functionality for sqlite3</span><br><span class="line">sqlite3[json1]                        enable JSON functionality for sqlite3</span><br><span class="line">taocpp-json          2020-09-14       C++ header-only JSON library</span><br><span class="line">valijson             2018-11-17-1     Header-only C++ library for JSON Schema validation</span><br><span class="line">yajl                 2.1.0-1          Yet Another JSON Library</span><br><span class="line"></span><br><span class="line">If your library is not listed, please open an issue at and&#x2F;or consider making a pull request:</span><br><span class="line">    https:&#x2F;&#x2F;github.com&#x2F;Microsoft&#x2F;vcpkg&#x2F;issues</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>已安装</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vcpkg(master) » .&#x2F;vcpkg list</span><br><span class="line">sqlite3:x86-windows         3.32.1           SQLite is a software library that implements a se...</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>c++</category>
      </categories>
      <tags>
        <tag>c++、包管理</tag>
      </tags>
  </entry>
  <entry>
    <title>偏安一隅</title>
    <url>/2021/01/31/%E5%81%8F%E5%AE%89%E4%B8%80%E9%9A%85/</url>
    <content><![CDATA[<p>出发：朝阳区来广营去往箭扣长城路上<br>mey be 范崎路<br><img src="/2021/01/31/%E5%81%8F%E5%AE%89%E4%B8%80%E9%9A%85/QQ20210131-3.jpg"></p>
<p>反正从我来到这个城市以后，没有再现过“苍穹之下”的天气了<br><img src="/2021/01/31/%E5%81%8F%E5%AE%89%E4%B8%80%E9%9A%85/QQ20210131-0.jpg"><br><img src="/2021/01/31/%E5%81%8F%E5%AE%89%E4%B8%80%E9%9A%85/QQ20210131-1.jpg"><br>写满了安静</p>
]]></content>
      <categories>
        <category>剪影</category>
      </categories>
      <tags>
        <tag>北京</tag>
      </tags>
  </entry>
  <entry>
    <title>六艺</title>
    <url>/2020/10/09/%E5%85%AD%E8%89%BA/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>travel</title>
    <url>/2020/09/15/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<p>基础搭建</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https:&#x2F;&#x2F;www.jianshu.com&#x2F;p&#x2F;cbf8ba8af532</span><br></pre></td></tr></table></figure>
<p>官方手册</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http:&#x2F;&#x2F;theme-next.iissnan.com&#x2F;getting-started.html</span><br></pre></td></tr></table></figure>

<p>阅读全文</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!-- more --&gt;</span><br></pre></td></tr></table></figure>

<p>添加搜索</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https:&#x2F;&#x2F;www.jianshu.com&#x2F;p&#x2F;112f3eae2098</span><br></pre></td></tr></table></figure>

<p>添加评论功能</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https:&#x2F;&#x2F;www.jianshu.com&#x2F;p&#x2F;83cf3e358c8e</span><br><span class="line"></span><br><span class="line">https:&#x2F;&#x2F;blog.csdn.net&#x2F;jiunian_2761&#x2F;article&#x2F;details&#x2F;97388997</span><br></pre></td></tr></table></figure>
<p><img src="/2020/09/15/%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/comments.png"></p>
<p>1.新建一个页面，命名为categories:</p>
<p>hexo new page categories</p>
<p>之所以命名为categories的原因是在next主题的配置文件中，categories是关键词。</p>
<p>2.编辑新建界面，将页面类型设置为categories，主题将会在这个页面上显示所有的分类：</p>
<hr>
<p>title: categories<br>date: 2018-03-02 12:33:16<br>type: “categories”</p>
<hr>
<p>这个步骤很有意思，编辑新建界面是什么？当然hexo熟悉一些自然会知道，但是小萌新还是比较懵逼的。实际上调用hexo new page xxx后，会在/source/categories/目录下生成一个index.md文档，在此文档头部加上上面这段即可。实际上，index.md里只需要有这个声明即可，其他内容并不会显示出来，写了也没用。</p>
<p>PS. 无论是page，还是post的文章，都是以.md格式结尾，在hexo g的过程中会产生对应的.html文档，然后hexo d到Github上的也只是html文档，不是.md格式的文档。</p>
<p>另外就是，需要注意一点：如果有启用多说 或者 Disqus 评论，默认页面也会带有评论。需要关闭的话，请添加字段 comments 并将值设置为 false，如：</p>
<hr>
<p>title: categories<br>date: 2018-03-02 12:33:16<br>type: “categories”<br>comments: false</p>
<hr>
]]></content>
      <categories>
        <category>搭建</category>
      </categories>
      <tags>
        <tag>about</tag>
      </tags>
  </entry>
  <entry>
    <title>面经</title>
    <url>/2021/05/01/%E9%9D%A2%E7%BB%8F/</url>
    <content><![CDATA[<p>趁着五一假期梳理一下自己这段时间两家公司的面试经历，一个是自己比较比较熟悉的百度， 一个是时代新生滴滴。</p>
<p>简单插一句，我觉得这段时间的面试先不说技术问题的难度，单从个人角度进行分析还是比较舒服的。 首先是自己没有一定要离开当前公司，出去面试也仅仅是对自己的一个测试，这一点帮助我很大程度上降低了面试的压力，至少可以说在面试的过程中什么话都能说、都敢表达。 然后是运气也确实比较好，自己自毕业以来绝大部分时间都是在处理业务上的问题，很多专业技能点都没有深入学习过，这次面试过程中面试官提到的问题恰好都是自己熟悉的内容（虽然自己答的不好，但是起码听懂了面试官的问题）。</p>
<p>我准备用对话的方式记录面试中的所有内容，一方面是考虑能最大程度的还原面试过程，一方面是想未来自己强大起来之后再来看这些试题答卷时，自己会有怎样一种感受。</p>
<p>首先是百度</p>
<h2 id="百度"><a href="#百度" class="headerlink" title="百度"></a>百度</h2><p>我习惯在面试官允许的前提下对面试进行录音，主要目的是结束之后再听一下自己的表达有没有逻辑混乱、吐字不清的情况，以及有没有其他技术能力上需要学习的地方。</p>
<p>百度的面试官还是都比较尊重面试者的，我是第一面和HR相关的视频面试， 二面和三面下班后直接去的百度科技园面试。<br>不得不说，百度的楼是真的大、工位是真的大、小姐姐是真的多，小姐姐是真的漂亮， 小姐姐是真的甜，特别的符合我的气质。</p>
<p>首先是全局变量</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const 自我介绍 &#x3D; </span><br><span class="line">面试官你好，我叫xxx，2019年毕业于xxxx, 在校期间多门专业课去得专业前30%的成绩，同时在校期间加入创新创业实验室，简历部分最后的校园经历和奖项部分就是对实验室内容的介绍。 我在大四下学期的时候加入QQ炫舞的技术支持中心实习，主要部分一些内部工具的研发与维护，这段时间对我来说最大的一个收获就是养成了一个良好的文档习惯，这也在现在的工作和生活中给了我很大帮助。毕业后到现在我一直在xxxx的基础架构部，主要负责公司xx业务与xx业务几个基础引擎的设计、研发工作。这段时间对我来说最大的一个成长就是我能把很多理论知识运用到实际工作中了，同时也开阔了自己的技术视野。</span><br></pre></td></tr></table></figure>
<h3 id="一面：1h10min（技术面试：专业基础知识、项目、算法）"><a href="#一面：1h10min（技术面试：专业基础知识、项目、算法）" class="headerlink" title="一面：1h10min（技术面试：专业基础知识、项目、算法）"></a>一面：1h10min（技术面试：专业基础知识、项目、算法）</h3><h3 id="二面：1h40min（技术面试：实际工作能力、算法、代码）"><a href="#二面：1h40min（技术面试：实际工作能力、算法、代码）" class="headerlink" title="二面：1h40min（技术面试：实际工作能力、算法、代码）"></a>二面：1h40min（技术面试：实际工作能力、算法、代码）</h3><ul>
<li><p>问：我是你的二面面试官，之前从一面那里已经了解了一部分内容，现在咱们开始今天的面试吧，你先稍等下，我再过下简历。</p>
</li>
<li><p>答：好的，那我这边先做一下自我介绍吧</p>
</li>
<li><p>问：好的</p>
</li>
<li><p>答：自我介绍</p>
</li>
<li><p>问：嗯，好。 其实我这一面关注的更多的还是你的技术能力，你这个有点像是HR面试， 不过没关系的。那我这边也介绍一下吧， 我叫xx，我的问题可能会多一点，你这边如果有部分答不上来了也没关系，另外我在面试过程中可能会一直用着电脑记录一些信息什么的，这个你不要特别在意。</p>
</li>
<li><p>答：好的。</p>
</li>
<li><p>问：你有使用过<code>kill</code>命令么？ 这个是干嘛用的？</p>
</li>
<li><p>答：用过的，<code>kill</code>命令常用来结束一些任务，不如模块上线是我们需要先停止当前进程，然后再启动新版本。 但是这种停止任务或者程序时不推荐或者说不应该使用<code>kill -9</code>，我们应该用<code>kill -term</code> 或者<code>kill - hug</code>等其他信号量来停止任务或者平滑退出。</p>
</li>
<li><p>问：看来是比较熟哈，那再深问一下。 你有什么办法停掉同一个进程里的不同线程么？ <code>process &#123;thread1, thread2, thread3&#125;</code>, 比如我要停掉<code>thread3</code>.</p>
</li>
<li><p>答：有，可以用刚才提到的信号量机制，让这个线程监听到特定的信号量时进行退出。</p>
</li>
<li><p>问：那是用信号量的时候需要注意哪些问题？</p>
</li>
<li><p>答：需要注意你所使用的信号量没有被该进程的其他线程所监听，以免发生一些非预期问题。所以如果要使用类似于term类的通用信号量时要先做一下确认。</p>
</li>
<li><p>问：除了信号量，还有其他通信方式么？</p>
</li>
<li><p>答：进程执行可以用信号量、socket， 线程执行可以共享内存</p>
</li>
<li><p>问：进程之间能用共享内存来通信么？</p>
</li>
<li><p>答：我觉得是不可以的，因为系统资源分配的最小单位就是进程。 但是我们线上有过一次调优，就是说程序从磁盘加载某些内容比较慢，所以会事先把资源放到特定的一个目录下，那个目录就是用内存来实现的，不同进程又都可以访问那块内存。 我不知道这种算不算进程间的共享内存通信</p>
</li>
<li><p>问：哪个目录？为什么要有这个目录</p>
</li>
<li><p>答：这个我就不清楚了，这个是在公司的一个他人负责的项目技术设计时了解到的内容</p>
</li>
<li><p>问：嗯，来看下一题，有这样一个文件，你帮我写一下shell命令获取一些信息，一个是baidu.com的出现次数，另一个是baidu.com的权值总和</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Xxxx@baidu.com, 41</span><br><span class="line">Aaaa@sina.com,83</span><br><span class="line">Qqqq@qq.com,32</span><br><span class="line">sdfa@baidu.com,54</span><br></pre></td></tr></table></figure></li>
<li><p>答：第一个是：<br><code>grep baidu file | wc -l</code><br>第二个是：<br><code>grep baidu file｜awk -F”，” ”｛print $2｝”｜sum</code></p>
</li>
<li><p>问：不好意思，应该是没有sum这个命令的</p>
</li>
<li><p>答：哦哦，那可以在awk中进行for累加，最后 end print就可以了，然后又写了一遍。。。</p>
</li>
<li><p>问：再写一条你觉得是你写过比较复杂的shell， 至少比这个问题要复杂一点</p>
</li>
<li><p>答：</p>
</li>
<li><p>问：当你在浏览器地址栏输入这个地址，按下回车之后，都发生了什么事情？<br><code>https://www.baidu.com</code></p>
</li>
<li><p>答：首先是域名解析，解析拿到ip地址之后像目标服务器建立链接</p>
</li>
<li><p>问：直接就进行域名解析了么？</p>
</li>
<li><p>答：不是，会首先检查本地的<code>host</code>文件看是否有配置好的域名解析地址，然后可能还有一个是<code>know_hosts</code>文件，看历史是否解析过这个域名。 如果都解析不了在调用域名服务器进行查询。</p>
</li>
<li><p>问：</p>
</li>
</ul>
<ul>
<li>问：你知道C++有很多版本么？</li>
<li>答：嗯嗯，像现在常用的C11，然后还有C14、C17、C20、C21</li>
<li>问：前面应该还有C98等等， 你能说几个C11及之后的你常用的特性么？</li>
<li>答：</li>
</ul>
<ul>
<li><p>答：</p>
</li>
<li><p>问：我听一面说你这边是Go和C++都有使用，他问了C++的，我这再问些Go相关的吧</p>
</li>
<li><p>答：好的，我可以试着做一下</p>
</li>
<li><p>问：你看下这到的的输出是什么</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    v :&#x3D; 3</span><br><span class="line">    p :&#x3D; &amp;v</span><br><span class="line">    fmt.Println(*p)</span><br><span class="line">    changePointer(p)</span><br><span class="line">    fmt.Println(*p)</span><br><span class="line">&#125;</span><br><span class="line">func changePointer(p *int) &#123;</span><br><span class="line">    v :&#x3D; 5</span><br><span class="line">    p &#x3D; &amp;v</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>答：是3和5么？</p>
</li>
<li><p>问：为什么</p>
</li>
<li><p>答：</p>
</li>
<li><p>问：答案其实不对，这个是因为Go的特性导致的，再看这一题的执行结果是什么</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">func main() &#123;</span><br><span class="line">    runtime.GOMAXPROCES(1)</span><br><span class="line">    flag :&#x3D; false;</span><br><span class="line"></span><br><span class="line">    go func() &#123;</span><br><span class="line">        flag &#x3D; true</span><br><span class="line">    &#125;()</span><br><span class="line"></span><br><span class="line">    for (!flag) &#123;</span><br><span class="line">    &#125;</span><br><span class="line">    fmt.Println(&quot;finish!&quot;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li><p>答：这个题有两个情况。</p>
</li>
<li><p>问：你写个函数实现一下类似于linux的路径切换功能，比如我输入1，实际就是到了输出1这个路径下。注意不是调用系统的cd命令，是自己处理这个输入，并返回给我这个结果字符串</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">输入1: &#x2F;home&#x2F;work&#x2F;..&#x2F;log</span><br><span class="line">输出1: &#x2F;home&#x2F;log</span><br><span class="line"></span><br><span class="line">输入1: &#x2F;home&#x2F;work&#x2F;bicheng&#x2F;log</span><br><span class="line">输出2: &#x2F;home&#x2F;work&#x2F;log</span><br><span class="line"></span><br><span class="line">func FindPath(path string) string &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">1. 用go和C++分别实现</span><br><span class="line">2. 使用时间复杂度、空间复杂度都小于等于 O(n)的方法实现</span><br></pre></td></tr></table></figure>
</li>
<li><p>答：//待补充</p>
</li>
<li><p>问：再看一题，你按照我这个条件，找一下在一个数组中有几对这种数组，你可以直接写出一种优解，或者先尝试写一个简单解法来拿个基础分。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">条件：  nums[i] &#x3D;&#x3D; nums[j] &amp;&amp; j &gt; i</span><br><span class="line">输入: [1,2,1,1,3,3]</span><br><span class="line">输出: 4</span><br><span class="line">解释： 在输入数组中存在 &#123;0,2&#125; &#123;0,3&#125; &#123;2,3&#125; &#123;4,5&#125; 四种下标组合符合条件的</span><br><span class="line"></span><br><span class="line">1. 用go和C++分别实现</span><br></pre></td></tr></table></figure></li>
<li><p>答：</p>
</li>
<li><p>问：使用时间复杂度、空间复杂度都小于等于 O(n)的方法实现</p>
</li>
<li><p>答：这个题跟我之前做过的一个叫做<a href>两数之和</a>leetcode第一题特别像，可能需要借助于哈希表来实现。主要思路就是… 。(我觉得在你真的开始写之前最好先跟面试官讲一下思路，一方面是确认一下大致解法有没有问题，另一方面是先让面试官有个思想准备，别看了我像xx堆砌的代码之后崩溃)</p>
</li>
<li><p>问：嗯嗯，可以</p>
</li>
</ul>
<h3 id="三面：40min（用人经理面试：项目理解、工作习惯、新工作规划）"><a href="#三面：40min（用人经理面试：项目理解、工作习惯、新工作规划）" class="headerlink" title="三面：40min（用人经理面试：项目理解、工作习惯、新工作规划）"></a>三面：40min（用人经理面试：项目理解、工作习惯、新工作规划）</h3><p>这部分内容我就不在赘述我的答案了</p>
<ul>
<li>问：我先做下自我介绍哈</li>
<li>答：</li>
<li>问：为什么考虑从上一家公司离职呢？</li>
<li>答：人员流动</li>
<li>问：你对百度了解多少呢？</li>
<li>答：</li>
<li>问：</li>
<li>答：</li>
<li>问：你对哪个部门比较感兴趣</li>
<li>答：</li>
<li>问：我看你的简历中提到了基础架构， 你是怎么理解基础架构这个部分的呢？ 或者说哪些内容是基础架构的工作？</li>
<li>答：<h3 id="四面：40min（HRBP面试：职业规划、换工作原因、3-5年职业规划、期望薪资）"><a href="#四面：40min（HRBP面试：职业规划、换工作原因、3-5年职业规划、期望薪资）" class="headerlink" title="四面：40min（HRBP面试：职业规划、换工作原因、3-5年职业规划、期望薪资）"></a>四面：40min（HRBP面试：职业规划、换工作原因、3-5年职业规划、期望薪资）</h3><h3 id="五面：40min（HR面试：行为-amp-性格测试、现工作状态）"><a href="#五面：40min（HR面试：行为-amp-性格测试、现工作状态）" class="headerlink" title="五面：40min（HR面试：行为&amp;性格测试、现工作状态）"></a>五面：40min（HR面试：行为&amp;性格测试、现工作状态）</h3></li>
</ul>
]]></content>
      <categories>
        <category>随笔</category>
      </categories>
      <tags>
        <tag>随笔</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake书写规则（三）</title>
    <url>/2020/05/27/cmake%E4%B9%A6%E5%86%99%E8%A7%84%E5%88%99/</url>
    <content><![CDATA[<h2 id="书写规则"><a href="#书写规则" class="headerlink" title="书写规则"></a>书写规则</h2><p>规则包含两个部分，一个是依赖关系，一个是生成目标的方法。</p>
<p>在Makefile中，规则的顺序是很重要的，因为，Makefile中只应该有一个最终目标，其它的目标都是被这个目标所连带出来的，所以一定要让make知道你的最终目标是什么。<br>一般来说，定义在Makefile中的目标可能会有很多，但是第一条规则中的目标将被确立为最终的目标。<br>如果第一条规则中的目标有很多个，那么，第一个目标会成为最终的目标。make所完成的也就是这个目标。<br>好了，还是让我们来看一看如何书写规则。</p>
<h3 id="1-规则举例"><a href="#1-规则举例" class="headerlink" title="1. 规则举例"></a>1. 规则举例</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">foo.o : foo.c defs.h       # foo模块</span><br><span class="line">    cc -c -g foo.c</span><br></pre></td></tr></table></figure>
<p>看到这个例子，各位应该不是很陌生了，前面也已说过，foo.o是我们的目标，foo.c和defs.h是目标所依赖的源文件，而只有一个命令“cc -c -g foo.c”（以Tab键开头）。这个规则告诉我们两件事：</p>
<ol>
<li>文件的依赖关系，foo.o依赖于foo.c和defs.h的文件，如果foo.c和defs.h的文件日期要比foo.o文件日期要新，或是foo.o不存在，那么依赖关系发生。</li>
<li>如果生成（或更新）foo.o文件。也就是那个cc命令，其说明了，如何生成foo.o这个文件。（当然foo.c文件include了defs.h文件）</li>
</ol>
<h3 id="2-规则的语法"><a href="#2-规则的语法" class="headerlink" title="2. 规则的语法"></a>2. 规则的语法</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">targets : prerequisites</span><br><span class="line">    command</span><br><span class="line">    ...</span><br><span class="line"> 或是这样： </span><br><span class="line"> targets : prerequisites ; command</span><br><span class="line">    command</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>

<p>targets是文件名，以空格分开，可以使用通配符。一般来说，我们的目标基本上是一个文件，但也有可能是多个文件。</p>
<p>command是命令行，如果其不与“target:prerequisites”在一行，那么，必须以[Tab键]开头，如果和prerequisites在一行，那么可以用分号做为分隔。（见上）</p>
<p>prerequisites也就是目标所依赖的文件（或依赖目标）。如果其中的某个文件要比目标文件要新，那么，目标就被认为是“过时的”，被认为是需要重生成的。这个在前面已经讲过了。</p>
<p>如果命令太长，你可以使用反斜框（‘\’）作为换行符。make对一行上有多少个字符没有限制。<br>规则告诉make两件事，文件的依赖关系和如何成成目标文件。<br>一般来说，make会以UNIX的标准Shell，也就是/bin/sh来执行命令。</p>
<h3 id="3-在规则中使用通配符"><a href="#3-在规则中使用通配符" class="headerlink" title="3. 在规则中使用通配符"></a>3. 在规则中使用通配符</h3><p>如果我们想定义一系列比较类似的文件，我们很自然地就想起使用通配符。make支持三各通配符：“*”，“?”和“[…]”。这是和Unix的B-Shell是相同的。</p>
<p>波浪号（“<del>”）字符在文件名中也有比较特殊的用途。如果是“</del>/test”，这就表示当前用户的$HOME目录下的test目录。<br>而“~hchen/test”则表示用户hchen的宿主目录下的test目录。（这些都是Unix下的小知识了，make也支持）而在Windows或是MS-DOS下，用户没有宿主目录，那么波浪号所指的目录则根据环境变量“HOME”而定。</p>
<p>通配符代替了你一系列的文件，如“*.c”表示所以后缀为c的文件。一个需要我们注意的是，如果我们的文件名中有通配符，如：“<em>”，那么可以用转义字符“\”，如“\</em>”来表示真实的“*”字符，而不是任意长度的字符串。</p>
<p>好吧，还是先来看几个例子吧：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">clean:</span><br><span class="line">    rm -f *.o</span><br></pre></td></tr></table></figure>
<p>上面这个例子我不不多说了，这是操作系统Shell所支持的通配符。这是在命令中的通配符。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print: *.c</span><br><span class="line">    lpr -p $?</span><br><span class="line">    touch print</span><br></pre></td></tr></table></figure>
<p>上面这个例子说明了通配符也可以在我们的规则中，目标print依赖于所有的[.c]文件。其中的“$?”是一个自动化变量，我会在后面给你讲述。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; *.o</span><br></pre></td></tr></table></figure>
<p>上面这个例子，表示了，通符同样可以用在变量中。并不是说[*.o]会展开，不！objects的值就是“*.o”。Makefile中的变量其实就是C/C++中的宏。如果你要让通配符在变量中展开，也就是让objects的值是所有[.o]的文件名的集合，那么，你可以这样：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects :&#x3D; $(wildcard *.o)</span><br></pre></td></tr></table></figure>
<p>这种用法由关键字“wildcard”指出，关于Makefile的关键字，我们将在后面讨论。</p>
<h3 id="4-文件搜寻"><a href="#4-文件搜寻" class="headerlink" title="4. 文件搜寻"></a>4. 文件搜寻</h3><p>在一些大的工程中，有大量的源文件，我们通常的做法是把这许多的源文件分类，并存放在不同的目录中。所以，当make需要去找寻文件的依赖关系时，你可以在文件前加上路径，但最好的方法是把一个路径告诉make，让make在自动去找。</p>
<p>Makefile文件中的特殊变量“VPATH”就是完成这个功能的，如果没有指明这个变量，make只会在当前的目录中去找寻依赖文件和目标文件。如果定义了这个变量，那么，make就会在当当前目录找不到的情况下，到所指定的目录中去找寻文件了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">VPATH &#x3D; src:..&#x2F;headers</span><br></pre></td></tr></table></figure>
<p>上面的的定义指定两个目录，“src”和“../headers”，make会按照这个顺序进行搜索。目录由“冒号”分隔。（当然，当前目录永远是最高优先搜索的地方）</p>
<p>另一个设置文件搜索路径的方法是使用make的“vpath”关键字（注意，它是全小写的），这不是变量，这是一个make的关键字，这和上面提到的那个VPATH变量很类似，但是它更为灵活。它可以指定不同的文件在不同的搜索目录中。这是一个很灵活的功能。它的使用方法有三种：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath &lt;pattern&gt;; &lt;directories&gt;;</span><br></pre></td></tr></table></figure>
<p>为符合模式<pattern>;的文件指定搜索目录<directories>;。</directories></pattern></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath &lt;pattern&gt;;</span><br></pre></td></tr></table></figure>
<p>清除符合模式<pattern>;的文件的搜索目录。</pattern></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath</span><br></pre></td></tr></table></figure>
<p>清除所有已被设置好了的文件搜索目录。</p>
<p>vapth使用方法中的<pattern>;需要包含“%”字符。“%”的意思是匹配零或若干字符，例如，“%.h”表示所有以“.h”结尾的文件。<pattern>;指定了要搜索的文件集，而<directories>;则指定了<pattern>;的文件集的搜索的目录。例如：</pattern></directories></pattern></pattern></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath %.h ..&#x2F;headers</span><br></pre></td></tr></table></figure>
<p>该语句表示，要求make在“../headers”目录下搜索所有以“.h”结尾的文件。（如果某文件在当前目录没有找到的话）</p>
<p>我们可以连续地使用vpath语句，以指定不同搜索策略。如果连续的vpath语句中出现了相同的<pattern>;，或是被重复了的<pattern>;，那么，make会按照vpath语句的先后顺序来执行搜索。如：</pattern></pattern></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath %.c foo</span><br><span class="line"> vpath %   blish</span><br><span class="line">vpath %.c bar</span><br></pre></td></tr></table></figure>
<p>其表示“.c”结尾的文件，先在“foo”目录，然后是“blish”，最后是“bar”目录。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vpath %.c foo:bar</span><br><span class="line">vpath %   blish</span><br></pre></td></tr></table></figure>
<p>而上面的语句则表示“.c”结尾的文件，先在“foo”目录，然后是“bar”目录，最后才是“blish”目录。</p>
<h3 id="5-伪目标"><a href="#5-伪目标" class="headerlink" title="5. 伪目标"></a>5. 伪目标</h3><p>最早先的一个例子中，我们提到过一个“clean”的目标，这是一个“伪目标”，</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">clean:</span><br><span class="line">    rm *.o temp</span><br></pre></td></tr></table></figure>
<p>正像我们前面例子中的“clean”一样，即然我们生成了许多文件编译文件，我们也应该提供一个清除它们的“目标”以备完整地重编译而用。 （以“make clean”来使用该目标）</p>
<p>因为，我们并不生成“clean”这个文件。“伪目标”并不是一个文件，只是一个标签，由于“伪目标”不是文件，所以make无法生成它的依赖关系和决定它是否要执行。我们只有通过显示地指明这个“目标”才能让其生效。当然，“伪目标”的取名不能和文件名重名，不然其就失去了“伪目标”的意义了。</p>
<p>当然，为了避免和文件重名的这种情况，我们可以使用一个特殊的标记“.PHONY”来显示地指明一个目标是“伪目标”，向make说明，不管是否有这个文件，这个目标就是“伪目标”。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.PHONY : clean</span><br></pre></td></tr></table></figure>
<p>只要有这个声明，不管是否有“clean”文件，要运行“clean”这个目标，只有“make clean”这样。于是整个过程可以这样写：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.PHONY: clean</span><br><span class="line">clean:</span><br><span class="line">    rm *.o temp</span><br></pre></td></tr></table></figure>
<p>伪目标一般没有依赖的文件。但是，我们也可以为伪目标指定所依赖的文件。伪目标同样可以作为“默认目标”，只要将其放在第一个。一个示例就是，如果你的Makefile需要一口气生成若干个可执行文件，但你只想简单地敲一个make完事，并且，所有的目标文件都写在一个Makefile中，那么你可以使用“伪目标”这个特性：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">all : prog1 prog2 prog3</span><br><span class="line">.PHONY : all</span><br><span class="line"></span><br><span class="line">prog1 : prog1.o utils.o</span><br><span class="line">        cc -o prog1 prog1.o utils.o</span><br><span class="line"></span><br><span class="line">prog2 : prog2.o</span><br><span class="line">        cc -o prog2 prog2.o</span><br><span class="line"></span><br><span class="line">prog3 : prog3.o sort.o utils.o</span><br><span class="line">        cc -o prog3 prog3.o sort.o utils.o</span><br></pre></td></tr></table></figure>
<p>我们知道，Makefile中的第一个目标会被作为其默认目标。我们声明了一个“all”的伪目标，其依赖于其它三个目标。由于伪目标的特性是，总是被执行的，所以其依赖的那三个目标就总是不如“all”这个目标新。所以，其它三个目标的规则总是会被决议。也就达到了我们一口气生成多个目标的目的。“.PHONY : all”声明了“all”这个目标为“伪目标”。</p>
<p>随便提一句，从上面的例子我们可以看出，目标也可以成为依赖。所以，伪目标同样也可成为依赖。看下面的例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.PHONY: cleanall cleanobj cleandiff</span><br><span class="line"></span><br><span class="line">cleanall : cleanobj cleandiff</span><br><span class="line">        rm program</span><br><span class="line"></span><br><span class="line">cleanobj :</span><br><span class="line">        rm *.o</span><br><span class="line"></span><br><span class="line">cleandiff :</span><br><span class="line">        rm *.diff</span><br></pre></td></tr></table></figure>
<p>“make clean”将清除所有要被清除的文件。“cleanobj”和“cleandiff”这两个伪目标有点像“子程序”的意思。我们可以输入“make cleanall”和“make cleanobj”和“make cleandiff”命令来达到清除不同种类文件的目的。</p>
<h3 id="6-多目标"><a href="#6-多目标" class="headerlink" title="6. 多目标"></a>6. 多目标</h3><p>Makefile的规则中的目标可以不止一个，其支持多目标，有可能我们的多个目标同时依赖于一个文件，并且其生成的命令大体类似。于是我们就能把其合并起来。当然，多个目标的生成规则的执行命令是同一个，这可能会给我们带来麻烦，不过好在我们的可以使用一个自动化变量“$@”（关于自动化变量，将在后面讲述），这个变量表示着目前规则中所有的目标的集合，这样说可能很抽象，还是看一个例子吧。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bigoutput littleoutput : text.g</span><br><span class="line">    generate text.g -$(subst output,,$@) &gt;; $@</span><br></pre></td></tr></table></figure>
<p>上述规则等价于：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bigoutput : text.g</span><br><span class="line">    generate text.g -big &gt;; bigoutput</span><br><span class="line">littleoutput : text.g</span><br><span class="line">    generate text.g -little &gt;; littleoutput</span><br></pre></td></tr></table></figure>
<p>其中，-$(subst output,,$@)中的“$”表示执行一个Makefile的函数，函数名为subst，后面的为参数。关于函数，将在后面讲述。这里的这个函数是截取字符串的意思，“$@”表示目标的集合，就像一个数组，“$@”依次取出目标，并执于命令。</p>
<h3 id="7-静态模式"><a href="#7-静态模式" class="headerlink" title="7. 静态模式"></a>7. 静态模式</h3><p>静态模式可以更加容易地定义多目标的规则，可以让我们的规则变得更加的有弹性和灵活。我们还是先来看一下语法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;targets ...&gt;;: &lt;target-pattern&gt;;: &lt;prereq-patterns ...&gt;;</span><br><span class="line">    &lt;commands&gt;;</span><br><span class="line">    ...</span><br></pre></td></tr></table></figure>
<p>targets定义了一系列的目标文件，可以有通配符。是目标的一个集合。<br>target-parrtern是指明了targets的模式，也就是的目标集模式。<br>prereq-parrterns是目标的依赖模式，它对target-parrtern形成的模式再进行一次依赖目标的定义。</p>
<p>这样描述这三个东西，可能还是没有说清楚，还是举个例子来说明一下吧。如果我们的<target-parrtern>;定义成“%.o”，意思是我们的<target>;集合中都是以“.o”结尾的，而如果我们的<prereq-parrterns>;定义成“%.c”，意思是对<target-parrtern>;所形成的目标集进行二次定义，其计算方法是，取<target-parrtern>;模式中的“%”（也就是去掉了[.o]这个结尾），并为其加上[.c]这个结尾，形成的新集合。</target-parrtern></target-parrtern></prereq-parrterns></target></target-parrtern></p>
<p>所以，我们的“目标模式”或是“依赖模式”中都应该有“%”这个字符，如果你的文件名中有“%”那么你可以使用反斜杠“\”进行转义，来标明真实的“%”字符。</p>
<p>看一个例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; foo.o bar.o</span><br><span class="line"></span><br><span class="line">all: $(objects)</span><br><span class="line"></span><br><span class="line">$(objects): %.o: %.c</span><br><span class="line">        $(CC) -c $(CFLAGS) $&lt; -o $@</span><br></pre></td></tr></table></figure>
<p>上面的例子中，指明了我们的目标从$object中获取，“%.o”表明要所有以“.o”结尾的目标，也就是“foo.o bar.o”，也就是变量$object集合的模式，而依赖模式“%.c”则取模式“%.o”的“%”，也就是“foo bar”，并为其加下“.c”的后缀，于是，我们的依赖目标就是“foo.c bar.c”。而命令中的“$&lt;”和“$@”则是自动化变量，“$&lt;”表示所有的依赖目标集（也就是“foo.c bar.c”），“$@”表示目标集（也就是“foo.o bar.o”）。于是，上面的规则展开后等价于下面的规则：</p>
<pre><code>foo.o : foo.c
        $(CC) -c $(CFLAGS) foo.c -o foo.o
bar.o : bar.c
        $(CC) -c $(CFLAGS) bar.c -o bar.o</code></pre>
<p>试想，如果我们的“%.o”有几百个，那种我们只要用这种很简单的“静态模式规则”就可以写完一堆规则，实在是太有效率了。“静态模式规则”的用法很灵活，如果用得好，那会一个很强大的功能。再看一个例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">files &#x3D; foo.elc bar.o lose.o</span><br><span class="line"></span><br><span class="line">$(filter %.o,$(files)): %.o: %.c</span><br><span class="line">        $(CC) -c $(CFLAGS) $&lt; -o $@</span><br><span class="line">$(filter %.elc,$(files)): %.elc: %.el</span><br><span class="line">        emacs -f batch-byte-compile $&lt;</span><br></pre></td></tr></table></figure>
<p>$(filter %.o,$(files))表示调用Makefile的filter函数，过滤“$filter”集，只要其中模式为“%.o”的内容。其的它内容，我就不用多说了吧。这个例字展示了Makefile中更大的弹性。</p>
<h3 id="8-自动生成依赖性"><a href="#8-自动生成依赖性" class="headerlink" title="8. 自动生成依赖性"></a>8. 自动生成依赖性</h3><p>在Makefile中，我们的依赖关系可能会需要包含一系列的头文件，比如，如果我们的main.c中有一句“#include “defs.h””，那么我们的依赖关系应该是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">main.o : main.c defs.h</span><br></pre></td></tr></table></figure>
<p>但是，如果是一个比较大型的工程，你必需清楚哪些C文件包含了哪些头文件，并且，你在加入或删除头文件时，也需要小心地修改Makefile，这是一个很没有维护性的工作。为了避免这种繁重而又容易出错的事情，我们可以使用C/C++编译的一个功能。大多数的C/C++编译器都支持一个“-M”的选项，即自动找寻源文件中包含的头文件，并生成一个依赖关系。例如，如果我们执行下面的命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cc -M main.c</span><br><span class="line"></span><br><span class="line">其输出是：</span><br><span class="line"></span><br><span class="line">main.o : main.c defs.h</span><br></pre></td></tr></table></figure>
<p>于是由编译器自动生成的依赖关系，这样一来，你就不必再手动书写若干文件的依赖关系，而由编译器自动生成了。需要提醒一句的是，如果你使用GNU的C/C++编译器，你得用“-MM”参数，不然，“-M”参数会把一些标准库的头文件也包含进来。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">gcc -M main.c的输出是：</span><br><span class="line"></span><br><span class="line">main.o: main.c defs.h &#x2F;usr&#x2F;include&#x2F;stdio.h &#x2F;usr&#x2F;include&#x2F;features.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;sys&#x2F;cdefs.h &#x2F;usr&#x2F;include&#x2F;gnu&#x2F;stubs.h \</span><br><span class="line">     &#x2F;usr&#x2F;lib&#x2F;gcc-lib&#x2F;i486-suse-linux&#x2F;2.95.3&#x2F;include&#x2F;stddef.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;bits&#x2F;types.h &#x2F;usr&#x2F;include&#x2F;bits&#x2F;pthreadtypes.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;bits&#x2F;sched.h &#x2F;usr&#x2F;include&#x2F;libio.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;_G_config.h &#x2F;usr&#x2F;include&#x2F;wchar.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;bits&#x2F;wchar.h &#x2F;usr&#x2F;include&#x2F;gconv.h \</span><br><span class="line">     &#x2F;usr&#x2F;lib&#x2F;gcc-lib&#x2F;i486-suse-linux&#x2F;2.95.3&#x2F;include&#x2F;stdarg.h \</span><br><span class="line">     &#x2F;usr&#x2F;include&#x2F;bits&#x2F;stdio_lim.h</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">gcc -MM main.c的输出则是：</span><br><span class="line"></span><br><span class="line">main.o: main.c defs.h</span><br></pre></td></tr></table></figure>
<p>那么，编译器的这个功能如何与我们的Makefile联系在一起呢。因为这样一来，我们的Makefile也要根据这些源文件重新生成，让Makefile自已依赖于源文件？这个功能并不现实，不过我们可以有其它手段来迂回地实现这一功能。GNU组织建议把编译器为每一个源文件的自动生成的依赖关系放到一个文件中，为每一个“name.c”的文件都生成一个“name.d”的Makefile文件，[.d]文件中就存放对应[.c]文件的依赖关系。</p>
<p>于是，我们可以写出[.c]文件和[.d]文件的依赖关系，并让make自动更新或自成[.d]文件，并把其包含在我们的主Makefile中，这样，我们就可以自动化地生成每个文件的依赖关系了。</p>
<p>这里，我们给出了一个模式规则来产生[.d]文件：</p>
<pre><code>%.d: %.c
        @set -e; rm -f $@; \
         $(CC) -M $(CPPFLAGS) $&lt; &gt;; $@.$$$$; \
         sed &#39;s,\($*\)\.o[ :]*,\1.o $@ : ,g&#39; &lt; $@.$$$$ &gt;; $@; \
         rm -f $@.$$$$</code></pre>
<p>这个规则的意思是，所有的[.d]文件依赖于[.c]文件，“rm -f $@”的意思是删除所有的目标，也就是[.d]文件，第二行的意思是，为每个依赖文件“$&lt;”，也就是[.c]文件生成依赖文件，“$@”表示模式“%.d”文件，如果有一个C文件是name.c，那么“%”就是“name”，“$$$$”意为一个随机编号，第二行生成的文件有可能是“name.d.12345”，第三行使用sed命令做了一个替换，关于sed命令的用法请参看相关的使用文档。第四行就是删除临时文件。</p>
<p>总而言之，这个模式要做的事就是在编译器生成的依赖关系中加入[.d]文件的依赖，即把依赖关系：</p>
<pre><code>main.o : main.c defs.h</code></pre>
<p>转成：</p>
<pre><code>main.o main.d : main.c defs.h</code></pre>
<p>于是，我们的[.d]文件也会自动更新了，并会自动生成了，当然，你还可以在这个[.d]文件中加入的不只是依赖关系，包括生成的命令也可一并加入，让每个[.d]文件都包含一个完赖的规则。一旦我们完成这个工作，接下来，我们就要把这些自动生成的规则放进我们的主Makefile中。我们可以使用Makefile的“include”命令，来引入别的Makefile文件（前面讲过），例如：</p>
<pre><code>sources = foo.c bar.c

include $(sources:.c=.d)</code></pre>
<p>上述语句中的“$(sources:.c=.d)”中的“.c=.d”的意思是做一个替换，把变量$(sources)所有[.c]的字串都替换成[.d]，关于这个“替换”的内容，在后面我会有更为详细的讲述。当然，你得注意次序，因为include是按次来载入文件，最先载入的[.d]文件中的目标会成为默认目标。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake使用函数（七）</title>
    <url>/2020/05/27/cmake%E4%BD%BF%E7%94%A8%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p>使用函数<br>————</p>
<p>在Makefile中可以使用函数来处理变量，从而让我们的命令或是规则更为的灵活和具有智能。make所支持的函数也不算很多，不过已经足够我们的操作了。函数调用后，函数的返回值可以当做变量来使用。</p>
<p>一、函数的调用语法</p>
<p>函数调用，很像变量的使用，也是以“$”来标识的，其语法如下：</p>
<pre><code>$(&lt;function&gt;; &lt;arguments&gt;</code></pre>
<p>或是</p>
<pre><code>$&#123;&lt;function&gt;; &lt;arguments&gt;;&#125;</code></pre>
<p>这里，<function>;就是函数名，make支持的函数不多。<arguments>;是函数的参数，参数间以逗号“,”分隔，而函数名和参数之间以“空格”分隔。函数调用以“$”开头，以圆括号或花括号把函数名和参数括起。感觉很像一个变量，是不是？函数中的参数可以使用变量，为了风格的统一，函数和变量的括号最好一样，如使用“$(subst a,b,$(x))”这样的形式，而不是“$(subst a,b,${x})”的形式。因为统一会更清楚，也会减少一些不必要的麻烦。</arguments></function></p>
<p>还是来看一个示例：</p>
<pre><code>comma:= ,
empty:=
space:= $(empty) $(empty)
foo:= a b c
bar:= $(subst $(space),$(comma),$(foo))</code></pre>
<p>在这个示例中，$(comma)的值是一个逗号。$(space)使用了$(empty)定义了一个空格，$(foo)的值是“a b c”，$(bar)的定义用，调用了函数“subst”，这是一个替换函数，这个函数有三个参数，第一个参数是被替换字串，第二个参数是替换字串，第三个参数是替换操作作用的字串。这个函数也就是把$(foo)中的空格替换成逗号，所以$(bar)的值是“a,b,c”。</p>
<p>二、字符串处理函数</p>
<p>$(subst <from>;,<to>;,<text> </text></to></from></p>
<pre><code>名称：字符串替换函数——subst。
功能：把字串&lt;text&gt;;中的&lt;from&gt;;字符串替换成&lt;to&gt;;。
返回：函数返回被替换过后的字符串。

示例：

    $(subst ee,EE,feet on the street)，

    把“feet on the street”中的“ee”替换成“EE”，返回结果是“fEEt on the strEEt”。</code></pre>
<p>$(patsubst <pattern>;,<replacement>;,<text> </text></replacement></pattern></p>
<pre><code>名称：模式字符串替换函数——patsubst。
功能：查找&lt;text&gt;;中的单词（单词以“空格”、“Tab”或“回车”“换行”分隔）是否符合模式&lt;pattern&gt;;，如果匹配的话，则以&lt;replacement&gt;;替换。这里，&lt;pattern&gt;;可以包括通配符“%”，表示任意长度的字串。如果&lt;replacement&gt;;中也包含“%”，那么，&lt;replacement&gt;;中的这个“%”将是&lt;pattern&gt;;中的那个“%”所代表的字串。（可以用“\”来转义，以“\%”来表示真实含义的“%”字符）
返回：函数返回被替换过后的字符串。

示例：

    $(patsubst %.c,%.o,x.c.c bar.c)

    把字串“x.c.c bar.c”符合模式[%.c]的单词替换成[%.o]，返回结果是“x.c.o bar.o”

备注：

    这和我们前面“变量章节”说过的相关知识有点相似。如：

    “$(var:&lt;pattern&gt;;=&lt;replacement&gt;”
     相当于
    “$(patsubst &lt;pattern&gt;;,&lt;replacement&gt;;,$(var))”，

     而“$(var: &lt;suffix&gt;;=&lt;replacement&gt;”
     则相当于
     “$(patsubst %&lt;suffix&gt;;,%&lt;replacement&gt;;,$(var))”。

     例如有：objects = foo.o bar.o baz.o，
     那么，“$(objects:.o=.c)”和“$(patsubst %.o,%.c,$(objects))”是一样的。</code></pre>
<p>$(strip <string></string></p>
<pre><code>名称：去空格函数——strip。
功能：去掉&lt;string&gt;;字串中开头和结尾的空字符。
返回：返回被去掉空格的字符串值。
示例：

    $(strip a b c )

    把字串“a b c ”去到开头和结尾的空格，结果是“a b c”。</code></pre>
<p>$(findstring <find>;,<in></in></find></p>
<pre><code>名称：查找字符串函数——findstring。
功能：在字串&lt;in&gt;;中查找&lt;find&gt;;字串。
返回：如果找到，那么返回&lt;find&gt;;，否则返回空字符串。
示例：

    $(findstring a,a b c)
    $(findstring a,b c)

    第一个函数返回“a”字符串，第二个返回“”字符串（空字符串）</code></pre>
<p>$(filter &lt;pattern…&gt;;,<text></text></p>
<pre><code>名称：过滤函数——filter。
功能：以&lt;pattern&gt;;模式过滤&lt;text&gt;;字符串中的单词，保留符合模式&lt;pattern&gt;;的单词。可以有多个模式。
返回：返回符合模式&lt;pattern&gt;;的字串。
示例：

    sources := foo.c bar.c baz.s ugh.h
    foo: $(sources)
            cc $(filter %.c %.s,$(sources)) -o foo

    $(filter %.c %.s,$(sources))返回的值是“foo.c bar.c baz.s”。</code></pre>
<p>$(filter-out &lt;pattern…&gt;;,<text></text></p>
<pre><code>名称：反过滤函数——filter-out。
功能：以&lt;pattern&gt;;模式过滤&lt;text&gt;;字符串中的单词，去除符合模式&lt;pattern&gt;;的单词。可以有多个模式。
返回：返回不符合模式&lt;pattern&gt;;的字串。
示例：

    objects=main1.o foo.o main2.o bar.o
    mains=main1.o main2.o

    $(filter-out $(mains),$(objects)) 返回值是“foo.o bar.o”。</code></pre>
<p>$(sort <list></list></p>
<pre><code>名称：排序函数——sort。
功能：给字符串&lt;list&gt;;中的单词排序（升序）。
返回：返回排序后的字符串。
示例：$(sort foo bar lose)返回“bar foo lose” 。
备注：sort函数会去掉&lt;list&gt;;中相同的单词。</code></pre>
<p>$(word <n>;,<text>;)</text></n></p>
<pre><code>名称：取单词函数——word。
功能：取字符串&lt;text&gt;;中第&lt;n&gt;;个单词。（从一开始）
返回：返回字符串&lt;text&gt;;中第&lt;n&gt;;个单词。如果&lt;n&gt;;比&lt;text&gt;;中的单词数要大，那么返回空字符串。
示例：$(word 2, foo bar baz)返回值是“bar”。</code></pre>
<p>$(wordlist <s>;,<e>;,<text>;)  </text></e></s></p>
<pre><code>名称：取单词串函数——wordlist。
功能：从字符串&lt;text&gt;;中取从&lt;s&gt;;开始到&lt;e&gt;;的单词串。&lt;s&gt;;和&lt;e&gt;;是一个数字。
返回：返回字符串&lt;text&gt;;中从&lt;s&gt;;到&lt;e&gt;;的单词字串。如果&lt;s&gt;;比&lt;text&gt;;中的单词数要大，那么返回空字符串。如果&lt;e&gt;;大于&lt;text&gt;;的单词数，那么返回从&lt;s&gt;;开始，到&lt;text&gt;;结束的单词串。
示例： $(wordlist 2, 3, foo bar baz)返回值是“bar baz”。</code></pre>
<p>$(words <text>;)</text></p>
<pre><code>名称：单词个数统计函数——words。
功能：统计&lt;text&gt;;中字符串中的单词个数。
返回：返回&lt;text&gt;;中的单词数。
示例：$(words, foo bar baz)返回值是“3”。
备注：如果我们要取&lt;text&gt;;中最后的一个单词，我们可以这样：$(word $(words &lt;text&gt;;),&lt;text&gt;;)。</code></pre>
<p>$(firstword <text>;)</text></p>
<pre><code>名称：首单词函数——firstword。
功能：取字符串&lt;text&gt;;中的第一个单词。
返回：返回字符串&lt;text&gt;;的第一个单词。
示例：$(firstword foo bar)返回值是“foo”。
备注：这个函数可以用word函数来实现：$(word 1,&lt;text&gt;;)。</code></pre>
<p>以上，是所有的字符串操作函数，如果搭配混合使用，可以完成比较复杂的功能。这里，举一个现实中应用的例子。我们知道，make使用“VPATH”变量来指定“依赖文件”的搜索路径。于是，我们可以利用这个搜索路径来指定编译器对头文件的搜索路径参数CFLAGS，如：</p>
<pre><code>override CFLAGS += $(patsubst %,-I%,$(subst :, ,$(VPATH)))

如果我们的“$(VPATH)”值是“src:../headers”，那么“$(patsubst %,-I%,$(subst :, ,$(VPATH)))”将返回“-Isrc -I../headers”，这正是cc或gcc搜索头文件路径的参数。</code></pre>
<p>三、文件名操作函数</p>
<p>下面我们要介绍的函数主要是处理文件名的。每个函数的参数字符串都会被当做一个或是一系列的文件名来对待。</p>
<p>$(dir &lt;names…&gt;;) </p>
<pre><code>名称：取目录函数——dir。
功能：从文件名序列&lt;names&gt;;中取出目录部分。目录部分是指最后一个反斜杠（“/”）之前的部分。如果没有反斜杠，那么返回“./”。
返回：返回文件名序列&lt;names&gt;;的目录部分。
示例： $(dir src/foo.c hacks)返回值是“src/ ./”。</code></pre>
<p>$(notdir &lt;names…&gt;;) </p>
<pre><code>名称：取文件函数——notdir。
功能：从文件名序列&lt;names&gt;;中取出非目录部分。非目录部分是指最后一个反斜杠（“/”）之后的部分。
返回：返回文件名序列&lt;names&gt;;的非目录部分。
示例： $(notdir src/foo.c hacks)返回值是“foo.c hacks”。</code></pre>
<p>$(suffix &lt;names…&gt;;) </p>
<pre><code>名称：取后缀函数——suffix。
功能：从文件名序列&lt;names&gt;;中取出各个文件名的后缀。
返回：返回文件名序列&lt;names&gt;;的后缀序列，如果文件没有后缀，则返回空字串。
示例：$(suffix src/foo.c src-1.0/bar.c hacks)返回值是“.c .c”。</code></pre>
<p>$(basename &lt;names…&gt;;)</p>
<pre><code>名称：取前缀函数——basename。
功能：从文件名序列&lt;names&gt;;中取出各个文件名的前缀部分。
返回：返回文件名序列&lt;names&gt;;的前缀序列，如果文件没有前缀，则返回空字串。
示例：$(basename src/foo.c src-1.0/bar.c hacks)返回值是“src/foo src-1.0/bar hacks”。</code></pre>
<p>$(addsuffix <suffix>;,&lt;names…&gt;;) </suffix></p>
<pre><code>名称：加后缀函数——addsuffix。
功能：把后缀&lt;suffix&gt;;加到&lt;names&gt;;中的每个单词后面。
返回：返回加过后缀的文件名序列。
示例：$(addsuffix .c,foo bar)返回值是“foo.c bar.c”。</code></pre>
<p>$(addprefix <prefix>;,&lt;names…&gt;;) </prefix></p>
<pre><code>名称：加前缀函数——addprefix。
功能：把前缀&lt;prefix&gt;;加到&lt;names&gt;;中的每个单词后面。
返回：返回加过前缀的文件名序列。
示例：$(addprefix src/,foo bar)返回值是“src/foo src/bar”。</code></pre>
<p>$(join <list1>;,<list2>;)</list2></list1></p>
<pre><code>名称：连接函数——join。
功能：把&lt;list2&gt;;中的单词对应地加到&lt;list1&gt;;的单词后面。如果&lt;list1&gt;;的单词个数要比&lt;list2&gt;;的多，那么，&lt;list1&gt;;中的多出来的单词将保持原样。如果&lt;list2&gt;;的单词个数要比&lt;list1&gt;;多，那么，&lt;list2&gt;;多出来的单词将被复制到&lt;list2&gt;;中。
返回：返回连接过后的字符串。
示例：$(join aaa bbb , 111 222 333)返回值是“aaa111 bbb222 333”。</code></pre>
<p>四、foreach 函数</p>
<p>foreach函数和别的函数非常的不一样。因为这个函数是用来做循环用的，Makefile中的foreach函数几乎是仿照于Unix标准Shell（/bin/sh）中的for语句，或是C-Shell（/bin/csh）中的foreach语句而构建的。它的语法是：</p>
<pre><code>$(foreach &lt;var&gt;;,&lt;list&gt;;,&lt;text&gt;</code></pre>
<p>这个函数的意思是，把参数<list>;中的单词逐一取出放到参数<var>;所指定的变量中，然后再执行<text>;所包含的表达式。每一次<text>;会返回一个字符串，循环过程中，<text>;的所返回的每个字符串会以空格分隔，最后当整个循环结束时，<text>;所返回的每个字符串所组成的整个字符串（以空格分隔）将会是foreach函数的返回值。</text></text></text></text></var></list></p>
<p>所以，<var>;最好是一个变量名，<list>;可以是一个表达式，而<text>;中一般会使用<var>;这个参数来依次枚举<list>;中的单词。举个例子：</list></var></text></list></var></p>
<pre><code>names := a b c d

files := $(foreach n,$(names),$(n).o)</code></pre>
<p>上面的例子中，$(name)中的单词会被挨个取出，并存到变量“n”中，“$(n).o”每次根据“$(n)”计算出一个值，这些值以空格分隔，最后作为foreach函数的返回，所以，$(files)的值是“a.o b.o c.o d.o”。</p>
<p>注意，foreach中的<var>;参数是一个临时的局部变量，foreach函数执行完后，参数<var>;的变量将不在作用，其作用域只在foreach函数当中。</var></var></p>
<p>五、if 函数</p>
<p>if函数很像GNU的make所支持的条件语句——ifeq（参见前面所述的章节），if函数的语法是：</p>
<pre><code>$(if &lt;condition&gt;;,&lt;then-part&gt; </code></pre>
<p>或是</p>
<pre><code>$(if &lt;condition&gt;;,&lt;then-part&gt;;,&lt;else-part&gt;</code></pre>
<p>可见，if函数可以包含“else”部分，或是不含。即if函数的参数可以是两个，也可以是三个。<condition>;参数是if的表达式，如果其返回的为非空字符串，那么这个表达式就相当于返回真，于是，<then-part>;会被计算，否则<else-part>;会被计算。</else-part></then-part></condition></p>
<p>而if函数的返回值是，如果<condition>;为真（非空字符串），那个<then-part>;会是整个函数的返回值，如果<condition>;为假（空字符串），那么<else-part>;会是整个函数的返回值，此时如果<else-part>;没有被定义，那么，整个函数返回空字串。</else-part></else-part></condition></then-part></condition></p>
<p>所以，<then-part>;和<else-part>;只会有一个被计算。</else-part></then-part></p>
<p>六、call函数</p>
<p>call函数是唯一一个可以用来创建新的参数化的函数。你可以写一个非常复杂的表达式，这个表达式中，你可以定义许多参数，然后你可以用call函数来向这个表达式传递参数。其语法是：</p>
<pre><code>$(call &lt;expression&gt;;,&lt;parm1&gt;;,&lt;parm2&gt;;,&lt;parm3&gt;;...)</code></pre>
<p>当make执行这个函数时，<expression>;参数中的变量，如$(1)，$(2)，$(3)等，会被参数<parm1>;，<parm2>;，<parm3>;依次取代。而<expression>;的返回值就是call函数的返回值。例如：</expression></parm3></parm2></parm1></expression></p>
<pre><code>reverse =  $(1) $(2)

foo = $(call reverse,a,b)</code></pre>
<p>那么，foo的值就是“a b”。当然，参数的次序是可以自定义的，不一定是顺序的，如：</p>
<pre><code>reverse =  $(2) $(1)

foo = $(call reverse,a,b)</code></pre>
<p>此时的foo的值就是“b a”。</p>
<p>七、origin函数<br>origin函数不像其它的函数，他并不操作变量的值，他只是告诉你你的这个变量是哪里来的？其语法是：</p>
<pre><code>$(origin &lt;variable&gt;</code></pre>
<p>注意，<variable>;是变量的名字，不应该是引用。所以你最好不要在<variable>;中使用“$”字符。Origin函数会以其返回值来告诉你这个变量的“出生情况”，下面，是origin函数的返回值:</variable></variable></p>
<p>“undefined”</p>
<pre><code>  如果&lt;variable&gt;;从来没有定义过，origin函数返回这个值“undefined”。</code></pre>
<p>“default”</p>
<pre><code>  如果&lt;variable&gt;;是一个默认的定义，比如“CC”这个变量，这种变量我们将在后面讲述。</code></pre>
<p>“environment”</p>
<pre><code>  如果&lt;variable&gt;;是一个环境变量，并且当Makefile被执行时，“-e”参数没有被打开。</code></pre>
<p>“file”</p>
<pre><code>  如果&lt;variable&gt;;这个变量被定义在Makefile中。</code></pre>
<p>“command line”</p>
<pre><code>  如果&lt;variable&gt;;这个变量是被命令行定义的。</code></pre>
<p>“override”</p>
<pre><code>  如果&lt;variable&gt;;是被override指示符重新定义的。</code></pre>
<p>“automatic”</p>
<pre><code>  如果&lt;variable&gt;;是一个命令运行中的自动化变量。关于自动化变量将在后面讲述。</code></pre>
<p>这些信息对于我们编写Makefile是非常有用的，例如，假设我们有一个Makefile其包了一个定义文件Make.def，在Make.def中定义了一个变量“bletch”，而我们的环境中也有一个环境变量“bletch”，此时，我们想判断一下，如果变量来源于环境，那么我们就把之重定义了，如果来源于Make.def或是命令行等非环境的，那么我们就不重新定义它。于是，在我们的Makefile中，我们可以这样写：</p>
<pre><code>ifdef bletch

ifeq &quot;$(origin bletch)&quot; &quot;environment&quot;

bletch = barf, gag, etc.

endif

endif</code></pre>
<p>当然，你也许会说，使用override关键字不就可以重新定义环境中的变量了吗？为什么需要使用这样的步骤？是的，我们用override是可以达到这样的效果，可是override过于粗暴，它同时会把从命令行定义的变量也覆盖了，而我们只想重新定义环境传来的，而不想重新定义命令行传来的。</p>
<p>八、shell函数</p>
<p>shell函数也不像其它的函数。顾名思义，它的参数应该就是操作系统Shell的命令。它和反引号“`”是相同的功能。这就是说，shell函数把执行操作系统命令后的输出作为函数返回。于是，我们可以用操作系统命令以及字符串处理命令awk，sed等等命令来生成一个变量，如：</p>
<pre><code>contents := $(shell cat foo)



files := $(shell echo *.c)</code></pre>
<p>注意，这个函数会新生成一个Shell程序来执行命令，所以你要注意其运行性能，如果你的Makefile中有一些比较复杂的规则，并大量使用了这个函数，那么对于你的系统性能是有害的。特别是Makefile的隐晦的规则可能会让你的shell函数执行的次数比你想像的多得多。</p>
<p>九、控制make的函数</p>
<p>make提供了一些函数来控制make的运行。通常，你需要检测一些运行Makefile时的运行时信息，并且根据这些信息来决定，你是让make继续执行，还是停止。</p>
<p>$(error &lt;text …&gt;</p>
<pre><code>产生一个致命的错误，&lt;text ...&gt;;是错误信息。注意，error函数不会在一被使用就会产生错误信息，所以如果你把其定义在某个变量中，并在后续的脚本中使用这个变量，那么也是可以的。例如：



示例一：

ifdef ERROR_001

$(error error is $(ERROR_001))

endif



示例二：

ERR = $(error found an error!)

.PHONY: err

err: ; $(ERR)



示例一会在变量ERROR_001定义了后执行时产生error调用，而示例二则在目录err被执行时才发生error调用。</code></pre>
<p>$(warning &lt;text …&gt;<br>这个函数很像error函数，只是它并不会让make退出，只是输出一段警告信息，而make继续执行。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
  <entry>
    <title>cmake概述(一)</title>
    <url>/2020/05/27/cmake%E6%A6%82%E8%BF%B0/</url>
    <content><![CDATA[<p>CMAKE系列的这几篇文章是很早之前我从某个前辈的博客中保存下来的，现在想再次整理一下这个入门手册，却发现找不到原博客地址了。 后续有线索了会第一时间补充到文档上的。</p>
<h2 id="一个概述"><a href="#一个概述" class="headerlink" title="一个概述"></a>一个概述</h2><p>什么是makefile？当我们使用Windows系统时，visualstudio或者其他IDE可以自动帮我们完成这个工作，当使用mac开发时，xcode也能自动生成对应的makefile， 但要作一个好的和professional的程序员，makefile还是要懂。特别在Unix下的软件编译，你就不能不自己写makefile了。</p>
<p>会不会写makefile，从一个侧面说明了一个人是否具备完成大型工程的能力。因为，makefile关系到了整个工程的编译规则。一个工程中的源文件不计数，其按类型、功能、模块分别放在若干个目录中，makefile定义了一系列的规则来指定，哪些文件需要先编译，哪些文件需要后编译，哪些文件需要重新编译，甚至于进行更复杂的功能操作，因为makefile就像一个Shell脚本一样，其中也可以执行操作系统的命令。</p>
<p>makefile带来的好处就是——“自动化编译”，一旦写好，只需要一个make命令，整个工程完全自动编译，极大的提高了软件开发的效率。</p>
<p>make是一个命令工具，是一个解释makefile中指令的命令工具，一般来说，大多数的IDE都有这个命令，比如：Delphi的make，Visual C++的nmake，Linux下GNU的make。可见，makefile都成为了一种在工程方面的编译方法。</p>
<p>现在讲述如何写makefile的文章比较少，这是我想写这篇文章的原因。当然，不同产商的make各不相同，也有不同的语法，但其本质都是在“文件依赖性”上做文章，这里，我仅对GNU的make进行讲述，我的环境是RedHat Linux 8.0，make的版本是3.80。必竟，这个make是应用最为广泛的，也是用得最多的。而且其还是最遵循于IEEE 1003.2-1992 标准的（POSIX.2）。</p>
<p>在这篇文档中，将以C/C++的源码作为我们基础，所以必然涉及一些关于C/C++的编译的知识，相关于这方面的内容，还请各位查看相关的编译器的文档。这里所默认的编译器是UNIX下的GCC和CC。</p>
<h2 id="首先理解编译和链接"><a href="#首先理解编译和链接" class="headerlink" title="首先理解编译和链接"></a>首先理解编译和链接</h2><p>在此，我想多说关于程序编译的一些规范和方法，一般来说，无论是C、C++，首先要把源文件编译成中间代码文件，在Windows下也就是 .obj 文件，UNIX下是 .o 文件，即 Object File，这个动作叫做编译（compile）。然后再把大量的Object File合成执行文件，这个动作叫作链接（link）。</p>
<ul>
<li><p>编译时，编译器需要的是语法的正确，函数与变量的声明的正确。对于后者，通常是你需要告诉编译器头文件的所在位置（头文件中应该只是声明，而定义应该放在C/C++文件中），只要所有的语法正确，编译器就可以编译出中间目标文件。一般来说，每个源文件都应该对应于一个中间目标文件（O文件或是OBJ文件）。</p>
</li>
<li><p>链接时，主要是链接函数和全局变量，所以，我们可以使用这些中间目标文件（O文件或是OBJ文件）来链接我们的应用程序。链接器并不管函数所在的源文件，只管函数的中间目标文件（Object File）。</p>
</li>
</ul>
<p>在大多数时候，由于源文件太多，编译生成的中间目标文件太多，而在链接时需要明显地指出中间目标文件名，这对于编译很不方便，所以，我们要给中间目标文件打个包，在Windows下这种包叫“库文件”（Library File)，也就是 .lib 文件，在UNIX下，是Archive File，也就是 .a 文件。</p>
<p>总结一下，源文件首先会生成中间目标文件，再由中间目标文件生成执行文件。在编译时，编译器只检测程序语法，和函数、变量是否被声明。如果函数未被声明，编译器会给出一个警告，但可以生成Object File。而在链接程序时，链接器会在所有的Object File中找寻函数的实现，如果找不到，那到就会报链接错误码（Linker Error），在VC下，这种错误一般是：Link 2001错误，意思说是说，链接器未能找到函数的实现。你需要指定函数的Object File.</p>
<p>好，言归正传，GNU的make有许多的内容，闲言少叙，还是让我们开始吧。</p>
<h2 id="makefile介绍"><a href="#makefile介绍" class="headerlink" title="makefile介绍"></a>makefile介绍</h2><p>make命令执行时，需要一个 Makefile 文件，以告诉make命令需要怎么样的去编译和链接程序。</p>
<p>首先，我们用一个示例来说明Makefile的书写规则。<br>这个示例来源于GNU的make使用手册，在这个示例中，我们的工程有8个C文件，和3个头文件，我们要写一个Makefile来告诉make命令如何编译和链接这几个文件。我们的规则是：<br>    1）如果这个工程没有编译过，那么我们的所有C文件都要编译并被链接。<br>    2）如果这个工程的某几个C文件被修改，那么我们只编译被修改的C文件，并链接目标程序。<br>    3）如果这个工程的头文件被改变了，那么我们需要编译引用了这几个头文件的C文件，并链接目标程序。</p>
<p>只要我们的Makefile写得够好，所有的这一切，我们只用一个make命令就可以完成，make命令会自动智能地根据当前的文件修改的情况来确定哪些文件需要重编译，从而自己编译所需要的文件和链接目标程序。</p>
<h3 id="1-Makefile的规则"><a href="#1-Makefile的规则" class="headerlink" title="1. Makefile的规则"></a>1. Makefile的规则</h3><p>在讲述这个Makefile之前，还是让我们先来粗略地看一看Makefile的规则。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">target ... : prerequisites ...</span><br><span class="line">        command</span><br><span class="line">        ...</span><br><span class="line">        ...</span><br></pre></td></tr></table></figure>
<p>target也就是一个目标文件，可以是Object File，也可以是执行文件。还可以是一个标签（Label），对于标签这种特性，在后续的“伪目标”章节中会有叙述。</p>
<ul>
<li>prerequisites就是，要生成那个target所需要的文件或是目标。</li>
<li>command也就是make需要执行的命令。（任意的Shell命令）<br>这是一个文件的依赖关系，也就是说，target这一个或多个的目标文件依赖于prerequisites中的文件，其生成规则定义在command中。<br>说白一点就是说，prerequisites中如果有一个以上的文件比target文件要新的话，command所定义的命令就会被执行。这就是Makefile的规则。也就是Makefile中最核心的内容。</li>
</ul>
<p>说到底，Makefile的东西就是这样一点，好像我的这篇文档也该结束了。呵呵。还不尽然，这是Makefile的主线和核心，但要写好一个Makefile还不够，我会以后面一点一点地结合我的工作经验给你慢慢到来。内容还多着呢。：）</p>
<h3 id="2-一个示例"><a href="#2-一个示例" class="headerlink" title="2. 一个示例"></a>2. 一个示例</h3><p>正如前面所说的，如果一个工程有3个头文件，和8个C文件，我们为了完成前面所述的那三个规则，我们的Makefile应该是下面的这个样子的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">edit : main.o kbd.o command.o display.o \</span><br><span class="line">       insert.o search.o files.o utils.o</span><br><span class="line">        cc -o edit main.o kbd.o command.o display.o \</span><br><span class="line">                   insert.o search.o files.o utils.o</span><br><span class="line"></span><br><span class="line">main.o : main.c defs.h</span><br><span class="line">        cc -c main.c</span><br><span class="line">kbd.o : kbd.c defs.h command.h</span><br><span class="line">        cc -c kbd.c</span><br><span class="line">command.o : command.c defs.h command.h</span><br><span class="line">        cc -c command.c</span><br><span class="line">display.o : display.c defs.h buffer.h</span><br><span class="line">        cc -c display.c</span><br><span class="line">insert.o : insert.c defs.h buffer.h</span><br><span class="line">        cc -c insert.c</span><br><span class="line">search.o : search.c defs.h buffer.h</span><br><span class="line">        cc -c search.c</span><br><span class="line">files.o : files.c defs.h buffer.h command.h</span><br><span class="line">        cc -c files.c</span><br><span class="line">utils.o : utils.c defs.h</span><br><span class="line">        cc -c utils.c</span><br><span class="line">clean :</span><br><span class="line">        rm edit main.o kbd.o command.o display.o \</span><br><span class="line">           insert.o search.o files.o utils.o</span><br></pre></td></tr></table></figure>

<p>反斜杠（\）是换行符的意思。这样比较便于Makefile的易读。我们可以把这个内容保存在文件为“Makefile”或“makefile”的文件中，然后在该目录下直接输入命令“make”就可以生成执行文件edit。如果要删除执行文件和所有的中间目标文件，那么，只要简单地执行一下“make clean”就可以了。</p>
<p>在这个makefile中，目标文件（target）包含：执行文件edit和中间目标文件（*.o），依赖文件（prerequisites）就是冒号后面的那些 .c 文件和 .h文件。每一个 .o 文件都有一组依赖文件，而这些 .o 文件又是执行文件 edit 的依赖文件。依赖关系的实质上就是说明了目标文件是由哪些文件生成的，换言之，目标文件是哪些文件更新的。</p>
<p>在定义好依赖关系后，后续的那一行定义了如何生成目标文件的操作系统命令，一定要以一个Tab键作为开头。记住，make并不管命令是怎么工作的，他只管执行所定义的命令。make会比较targets文件和prerequisites文件的修改日期，如果prerequisites文件的日期要比targets文件的日期要新，或者target不存在的话，那么，make就会执行后续定义的命令。</p>
<p>这里要说明一点的是，clean不是一个文件，它只不过是一个动作名字，有点像C语言中的lable一样，其冒号后什么也没有，那么，make就不会自动去找文件的依赖性，也就不会自动执行其后所定义的命令。要执行其后的命令，就要在make命令后明显得指出这个lable的名字。这样的方法非常有用，我们可以在一个makefile中定义不用的编译或是和编译无关的命令，比如程序的打包，程序的备份，等等。</p>
<h3 id="3-make是如何工作的"><a href="#3-make是如何工作的" class="headerlink" title="3. make是如何工作的"></a>3. make是如何工作的</h3><p>在默认的方式下，也就是我们只输入make命令。那么，</p>
<ol>
<li>make会在当前目录下找名字叫“Makefile”或“makefile”的文件。</li>
<li>如果找到，它会找文件中的第一个目标文件（target），在上面的例子中，他会找到“edit”这个文件，并把这个文件作为最终的目标文件。</li>
<li>如果edit文件不存在，或是edit所依赖的后面的 .o 文件的文件修改时间要比edit这个文件新，那么，他就会执行后面所定义的命令来生成edit这个文件。</li>
<li>如果edit所依赖的.o文件也存在，那么make会在当前文件中找目标为.o文件的依赖性，如果找到则再根据那一个规则生成.o文件。（这有点像一个堆栈的过程）</li>
<li>当然，你的C文件和H文件是存在的啦，于是make会生成 .o 文件，然后再用 .o 文件生成make的终极任务，也就是执行文件edit了。</li>
</ol>
<p>这就是整个make的依赖性，make会一层又一层地去找文件的依赖关系，直到最终编译出第一个目标文件。在找寻的过程中，如果出现错误，比如最后被依赖的文件找不到，那么make就会直接退出，并报错，而对于所定义的命令的错误，或是编译不成功，make根本不理。make只管文件的依赖性，即，如果在我找了依赖关系之后，冒号后面的文件还是不在，那么对不起，我就不工作啦。</p>
<p>通过上述分析，我们知道，像clean这种，没有被第一个目标文件直接或间接关联，那么它后面所定义的命令将不会被自动执行，不过，我们可以显示要make执行。即命令——“make clean”，以此来清除所有的目标文件，以便重编译。</p>
<p>于是在我们编程中，如果这个工程已被编译过了，当我们修改了其中一个源文件，比如file.c，那么根据我们的依赖性，我们的目标file.o会被重编译（也就是在这个依性关系后面所定义的命令），于是file.o的文件也是最新的啦，于是file.o的文件修改时间要比edit要新，所以edit也会被重新链接了（详见edit目标文件后定义的命令）。</p>
<p>而如果我们改变了“command.h”，那么，kdb.o、command.o和files.o都会被重编译，并且，edit会被重链接。</p>
<h3 id="4-makefile中使用变量"><a href="#4-makefile中使用变量" class="headerlink" title="4. makefile中使用变量"></a>4. makefile中使用变量</h3><p>在上面的例子中，先让我们看看edit的规则：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">edit : main.o kbd.o command.o display.o \</span><br><span class="line">            insert.o search.o files.o utils.o</span><br><span class="line">      cc -o edit main.o kbd.o command.o display.o \</span><br><span class="line">                 insert.o search.o files.o utils.o</span><br></pre></td></tr></table></figure>
<p>我们可以看到[.o]文件的字符串被重复了两次，如果我们的工程需要加入一个新的[.o]文件，那么我们需要在两个地方加（应该是三个地方，还有一个地方在clean中）。当然，我们的makefile并不复杂，所以在两个地方加也不累，但如果makefile变得复杂，那么我们就有可能会忘掉一个需要加入的地方，而导致编译失败。所以，为了makefile的易维护，在makefile中我们可以使用变量。</p>
<p>makefile的变量也就是一个字符串，理解成C语言中的宏可能会更好。<br>比如，我们声明一个变量，叫objects, OBJECTS, objs, OBJS, obj, 或是 OBJ，反正不管什么啦，只要能够表示obj文件就行了。我们在makefile一开始就这样定义：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; main.o kbd.o command.o display.o \</span><br><span class="line">         insert.o search.o files.o utils.o</span><br></pre></td></tr></table></figure>
<p>于是，我们就可以很方便地在我们的makefile中以“$(objects)”的方式来使用这个变量了，于是我们的改良版makefile就变成下面这个样子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; main.o kbd.o command.o display.o \</span><br><span class="line">          insert.o search.o files.o utils.o</span><br><span class="line"></span><br><span class="line">edit : $(objects)</span><br><span class="line">        cc -o edit $(objects)</span><br><span class="line">main.o : main.c defs.h</span><br><span class="line">        cc -c main.c</span><br><span class="line">kbd.o : kbd.c defs.h command.h</span><br><span class="line">        cc -c kbd.c</span><br><span class="line">command.o : command.c defs.h command.h</span><br><span class="line">        cc -c command.c</span><br><span class="line">display.o : display.c defs.h buffer.h</span><br><span class="line">        cc -c display.c</span><br><span class="line">insert.o : insert.c defs.h buffer.h</span><br><span class="line">        cc -c insert.c</span><br><span class="line">search.o : search.c defs.h buffer.h</span><br><span class="line">        cc -c search.c</span><br><span class="line">files.o : files.c defs.h buffer.h command.h</span><br><span class="line">        cc -c files.c</span><br><span class="line">utils.o : utils.c defs.h</span><br><span class="line">        cc -c utils.c</span><br><span class="line">clean :</span><br><span class="line">        rm edit $(objects)</span><br></pre></td></tr></table></figure>
<p>于是如果有新的 .o 文件加入，我们只需简单地修改一下 objects 变量就可以了。<br>关于变量更多的话题，我会在后续给你一一道来。</p>
<h3 id="5-make自动推导"><a href="#5-make自动推导" class="headerlink" title="5. make自动推导"></a>5. make自动推导</h3><p>GNU的make很强大，它可以自动推导文件以及文件依赖关系后面的命令，于是我们就没必要去在每一个[.o]文件后都写上类似的命令，因为，我们的make会自动识别，并自己推导命令。</p>
<p>只要make看到一个[.o]文件，它就会自动的把[.c]文件加在依赖关系中，如果make找到一个whatever.o，那么whatever.c，就会是whatever.o的依赖文件。并且 cc -c whatever.c 也会被推导出来，于是，我们的makefile再也不用写得这么复杂。我们的是新的makefile又出炉了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; main.o kbd.o command.o display.o \</span><br><span class="line">          insert.o search.o files.o utils.o</span><br><span class="line"></span><br><span class="line">edit : $(objects)</span><br><span class="line">        cc -o edit $(objects)</span><br><span class="line"></span><br><span class="line">main.o : defs.h</span><br><span class="line">kbd.o : defs.h command.h</span><br><span class="line">command.o : defs.h command.h</span><br><span class="line">display.o : defs.h buffer.h</span><br><span class="line">insert.o : defs.h buffer.h</span><br><span class="line">search.o : defs.h buffer.h</span><br><span class="line">files.o : defs.h buffer.h command.h</span><br><span class="line">utils.o : defs.h</span><br><span class="line"></span><br><span class="line">.PHONY : clean</span><br><span class="line">clean :</span><br><span class="line">        rm edit $(objects)</span><br></pre></td></tr></table></figure>
<p>这种方法，也就是make的“隐晦规则”。</p>
<p>上面文件内容中，“.PHONY”表示，clean是个伪目标文件。<br>关于更为详细的“隐晦规则”和“伪目标文件”，我会在后续给你一一道来。</p>
<h3 id="6-另类风格的makefile"><a href="#6-另类风格的makefile" class="headerlink" title="6. 另类风格的makefile"></a>6. 另类风格的makefile</h3><p>即然我们的make可以自动推导命令，那么我看到那堆[.o]和[.h]的依赖就有点不爽，那么多的重复的[.h]，能不能把其收拢起来，好吧，没有问题，这个对于make来说很容易，谁叫它提供了自动推导命令和文件的功能呢？来看看最新风格的makefile吧。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">objects &#x3D; main.o kbd.o command.o display.o \</span><br><span class="line">          insert.o search.o files.o utils.o</span><br><span class="line"></span><br><span class="line">edit : $(objects)</span><br><span class="line">        cc -o edit $(objects)</span><br><span class="line"></span><br><span class="line">$(objects) : defs.h</span><br><span class="line">kbd.o command.o files.o : command.h</span><br><span class="line">display.o insert.o search.o files.o : buffer.h</span><br><span class="line"></span><br><span class="line">.PHONY : clean</span><br><span class="line">clean :</span><br><span class="line">        rm edit $(objects)</span><br></pre></td></tr></table></figure>
<p>这种风格，让我们的makefile变得很简单，但我们的文件依赖关系就显得有点凌乱了。鱼和熊掌不可兼得。还看你的喜好了。我是不喜欢这种风格的，一是文件的依赖关系看不清楚，二是如果文件一多，要加入几个新的.o文件，那就理不清楚了。</p>
<h3 id="7-清空目标文件的规则"><a href="#7-清空目标文件的规则" class="headerlink" title="7. 清空目标文件的规则"></a>7. 清空目标文件的规则</h3><p>每个Makefile中都应该写一个清空目标文件（.o和执行文件）的规则，这不仅便于重编译，也很利于保持文件的清洁。这是一个“修养”（呵呵，还记得我的《编程修养》吗）。一般的风格都是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">clean:</span><br><span class="line">    rm edit $(objects)</span><br></pre></td></tr></table></figure>
<p>更为稳健的做法是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.PHONY : clean</span><br><span class="line">clean :</span><br><span class="line">    -rm edit $(objects)</span><br></pre></td></tr></table></figure>
<p>在rm命令前面加了一个小减号的意思就是，也许某些文件出现问题，但不要管，继续做后面的事。当然，clean的规则不要放在文件的开头，不然，这就会变成make的默认目标，相信谁也不愿意这样。<br>不成文的规矩是——“clean从来都是放在文件的最后”。</p>
<p>上面就是一个makefile的概貌，也是makefile的基础，下面还有很多makefile的相关细节，准备好了吗？准备好了就来。</p>
]]></content>
      <categories>
        <category>CMAKE入门手册</category>
      </categories>
      <tags>
        <tag>cmake</tag>
      </tags>
  </entry>
</search>
